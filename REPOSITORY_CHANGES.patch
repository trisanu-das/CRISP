diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/MODIFICATION_REPORT.md /mnt/data/crisp_work/CRISP-main/MODIFICATION_REPORT.md
--- /mnt/data/crisp_original/CRISP-main/MODIFICATION_REPORT.md	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/MODIFICATION_REPORT.md	2026-08-02 12:59:07.455730080 +0000
@@ -0,0 +1,209 @@
+# Modification report
+
+This document describes the corrections and extensions applied to the uploaded
+CRISP repository. The modifications were made directly in this repository; no
+upstream GitHub branch was changed.
+
+## Validation status
+
+- `python -m compileall -q crisp scripts train_launcher.py`: passed.
+- `pytest -q`: **249 tests passed, plus 6 subtests**.
+- Launcher routing: `crisp`, `grpo`, `reinforce_pp`, `opsd`, `vacs`,
+  `cibo-v2`, and `sft` are recognized.
+- All four ablation suites complete dry-run command generation.
+- Scaling, EvalPlus, and LiveCodeBench wrappers complete dry-run generation.
+
+No full 7B GPU training or external execution benchmark was run in the
+modification environment. Run the smoke configs before beginning expensive
+experiments.
+
+## 1. Effective-batch reward normalization
+
+The original loop normalized binary rewards independently inside each
+microbatch. Consequently, `[0, 0]` and `[1, 1]` both generated zero
+advantages, even when gradient accumulation was intended to create a larger
+effective batch.
+
+The new two-stage path in `crisp/training/common_loop.py`:
+
+1. collects all rollout microbatches without retaining autograd graphs;
+2. gathers their rewards across accumulation steps and distributed ranks;
+3. computes one mean and standard deviation for the complete effective batch;
+4. scores each stored microbatch with those shared statistics.
+
+The new records are in `crisp/training/batch_types.py`. The corrected path is
+used by CRISP, REINFORCE++, VACS, CIBO-v2, and the single-rollout policy-gradient
+control. `tests/test_effective_batch_loop.py` verifies that `[0,0]` and `[1,1]`
+normalize jointly to `[-1,-1]` and `[1,1]`.
+
+## 2. Policy-gradient objective
+
+The prior code created `old_logp = new_logp.detach()` in the same scoring
+forward and then calculated a PPO ratio. Its numerical value was always one,
+so clipping could never activate and the displayed scalar loss often cancelled
+to zero.
+
+CRISP, REINFORCE++, and GRPO now use their appropriate single-pass policy-
+gradient losses directly. There is no claim of PPO clipping unless stored old
+policy log-probabilities and multiple optimization epochs are implemented in a
+future version.
+
+## 3. CRISP corrections
+
+- Added selectable `cosine`, `linear`, and `constant` lambda schedules.
+- Added `training.use_pcgrad` for a real naive-sum ablation.
+- Added `training.correctness_gate` for an always-on self-distillation
+  ablation.
+- The RL term now scores response tokens only and accepts effective-batch
+  advantages from the shared loop.
+- Empty responses are removed before response-token reductions.
+
+## 4. REINFORCE++ and GRPO corrections
+
+- REINFORCE++ now receives globally normalized effective-batch advantages.
+- Fake PPO clipping was removed.
+- GRPO `k>=2` uses within-prompt group-relative advantages.
+- The historical `grpo_k1` manifest key is explicitly labelled a
+  single-rollout global-policy-gradient budget control, not genuine GRPO.
+- The launcher and ablation runner now forward the intended `k`; the former
+  duplicate `k=1`/`k=8` command bug is removed.
+
+## 5. VACS corrections
+
+- Added configurable `vacs.use_soft_gate` and
+  `vacs.use_asymmetric_projection` switches.
+- The no-soft-gate ablation now uses a true unit gate rather than setting the
+  sigmoid slope to zero, which would produce a constant gate of 0.5.
+- Token credit uses the documented stop-gradient quantity
+  `sg[log p_T - log p_S]` and the sign-aware clipped exponential weight.
+- Adaptive statistics aggregate every accumulated microbatch instead of using
+  only the final microbatch.
+- Distributed ranks globally average those statistics before committing the
+  next-step adaptive state.
+- Documentation calls the implemented statistic gradient energy/squared norm;
+  it is not misrepresented as an exact gradient-variance estimator.
+
+## 6. CIBO-v2 corrections
+
+- CIBO reads `cibo_v2.soft_gate_slope` and
+  `cibo_v2.reward_threshold`, not the VACS section.
+- `cibo_v2.ib_reduction` reaches the loss; `sequence_sum` and `token_mean` are
+  now real, distinct ablations.
+- Added `cibo_v2.use_soft_gate` for a true unit-gate ablation.
+- The information-bottleneck term is forward KL
+  `KL(detached teacher || live student)`.
+- Replaced the gathered-token EMA NLL with full-distribution cross-entropy
+  `H(p_EMA, p_live)`. The EMA target is detached and gradients flow only
+  through the live student.
+- The total gradient comes from one scalar objective; no PC-Grad is applied.
+- The beta controller receives one globally averaged observation per optimizer
+  update, so its dynamics do not depend on `grad_accum_steps`.
+- Its bounded sign controller waits for two post-warmup observations before
+  forming a delta.
+
+## 7. Checkpoint and resume corrections
+
+Common-loop checkpoints now contain:
+
+- adapter/model state;
+- optimizer and scheduler state;
+- global optimizer step;
+- resumable shuffled data-stream state;
+- Python, Torch CPU, and CUDA RNG states;
+- VACS/CIBO method state.
+
+Interrupted or failed runs are written to `interrupted/`; only successfully
+completed runs receive `final/` and the top-level `_SUCCESS` marker.
+
+Distributed runs save rank-specific state files such as
+`training_state_rank_1.pt`. Resuming with a different world size is rejected,
+because it cannot reproduce the original data shards and RNG streams exactly.
+
+## 8. Distributed execution
+
+`crisp/utils/distributed.py` provides explicit reward gathering and gradient
+all-reduction for the repository's manual-gradient methods. Quantized models
+are pinned to each process's `LOCAL_RANK`; `device_map="auto"` is not allowed
+to make every process occupy every GPU.
+
+Launch with:
+
+```bash
+NUM_GPUS=2 METHOD=crisp CONFIG=config/paper_7b.yaml \
+  bash scripts/launch_distributed.sh
+```
+
+Current CRISP/VACS semantics are **local gradient surgery followed by global
+averaging of the final gradients**. This is operationally valid but is not
+identical to globally averaging RL and SD task gradients before surgery. State
+this choice in experimental reporting.
+
+## 9. SFT to RL support
+
+Added the `sft` launcher target and `config/sft_7b.yaml`.
+`crisp/training/sft_train.py` performs completion-only supervised fine-tuning:
+prompt tokens receive label `-100`, while assistant solution tokens contribute
+to cross-entropy.
+
+All RL/SD methods support:
+
+```yaml
+model:
+  initial_adapter: runs/sft_7b/final
+```
+
+The existing adapter is loaded as trainable; a second fresh adapter is not
+stacked on it.
+
+## 10. Added experiment coverage
+
+### HMMT February 2025
+
+`hmmt2025` is registered in the evaluation loader and included in
+`config/paper_7b.yaml`.
+
+### HumanEval+
+
+`scripts/merge_adapter.py` creates a merged checkpoint.
+`scripts/eval_humaneval_plus.py` delegates execution to the separately
+installed official EvalPlus package.
+
+### LiveCodeBench
+
+`scripts/eval_livecodebench.py` delegates to an installed official
+LiveCodeBench environment and makes the release version explicit.
+
+### Model scaling
+
+`config/scaling/` contains matched Qwen2.5-Instruct configurations at 1.5B,
+3B, 7B, and 14B. `scripts/run_scaling.py` runs a serial seed/method matrix.
+These general Qwen2.5 checkpoints form one consistent scaling family; do not
+mix their curve with Qwen2.5-Math checkpoints without labelling the family
+change.
+
+## 11. Evaluation and ablation reliability
+
+- Bootstrap count and bootstrap seed now flow from YAML into evaluation.
+- `item_correctness_std` distinguishes per-item Bernoulli variation from
+  cross-seed uncertainty.
+- The ablation runner validates `_SUCCESS`, metric-file existence, expected
+  metric keys, and finite metric values after every run.
+- The manifest's formerly inert VACS/CIBO ablations now alter actual code
+  paths.
+- `config/paper_7b.yaml` provides a common model/data/optimizer/rollout/eval
+  basis for method comparisons.
+- LoRA dropout is set to zero because rollout and manual scoring forwards use
+  evaluation mode; a nonzero configured dropout would otherwise be misleading.
+
+## 12. External dependencies and remaining caveats
+
+- EvalPlus and LiveCodeBench are intentionally not vendored. Install and pin
+  their official environments separately.
+- Execution-based code benchmarks run generated code and should be isolated
+  from the training runtime.
+- The CIBO KL remains a variational proxy rather than exact mutual
+  information.
+- Fixed-beta and adaptive-beta CIBO results have different theoretical
+  interpretations and remain separate experiment suites.
+- Full-scale results still require multiple seeds, matched training-token
+  budgets, and hardware/runtime reporting.
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/README.md /mnt/data/crisp_work/CRISP-main/README.md
--- /mnt/data/crisp_original/CRISP-main/README.md	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/README.md	2026-08-02 12:59:07.335730077 +0000
@@ -4,12 +4,13 @@
 (CRISP stands for "Critic-free Reward-Integrated Self-distillation Policy Optimization"). This repository adds two new methods on top of
 the existing baselines (CRISP, REINFORCE++, OPSD, GRPO):
 
-- **VACS-CRISP** — variable-amplitude credit + asymmetric gradient
-  surgery + soft reward gating + bounded adaptive mixing. See
+- **VACS-CRISP** — variance/gradient-energy-aware causal surgery with
+  token credit, asymmetric gradient projection, soft reward gating, and
+  bounded adaptive mixing. See
   `docs/methods.md` for the math and `docs/ablations.md` for the
   ablation suite.
-- **CIBO-CRISP v2** — single composite loss (`loss_rl+beta*loss_ib
-  alpha*loss_anchor`) with **no** gradient surgery, an EMA-anchored
+- **CIBO-CRISP v2** — single composite loss (`loss_rl + beta*loss_ib
+  + alpha*loss_anchor`) with **no** gradient surgery, an EMA-anchored
   regularizer, and a bounded adaptive-beta controller as an ablation.
 
 ## Status
@@ -22,11 +23,13 @@
 own datasets. The `scripts/run_ablations.py` runner is designed to
 make that straightforward.
 
-The implementation has been verified offline with synthetic
-tensors / fake models at the unit level (see `tests/` and the
-`VALIDATION.md` you'll find after Task 12 completes). Real
-GPU-side smoke runs on a Kaggle notebook or equivalent have not been
-executed from this environment; that is the user's responsibility.
+A detailed migration/change report is in `MODIFICATION_REPORT.md`, and
+extended benchmark instructions are in `docs/extended_experiments.md`.
+
+The implementation is validated by the repository test suite and dry-run
+routing checks. GPU training and benchmark results are intentionally not
+claimed by this repository; run the smoke profile and paper configurations
+on your target hardware before using results in a paper.
 
 ## Project layout
 
@@ -34,11 +37,11 @@
 crisp-vacs-cibo/
 ├── README.md                   # this file
 ├── PROVENANCE.md               # seed commit + provenance notes
-├── train_launcher.py           # method dispatcher (crisp/grpo/opsd/reinforce_pp/vacs/cibo-v2)
+├── train_launcher.py           # dispatcher (crisp/grpo/opsd/reinforce_pp/vacs/cibo-v2/sft)
 ├── predownload.py              # pre-download a model + tokenizer before launching
 ├── config/
-│   ├── crisp_7b.yaml           # copied legacy CRISP 7B config (untouched)
-│   ├── crisp_pilot.yaml        # tiny pilot (untouched; not used here)
+│   ├── crisp_7b.yaml           # corrected full CRISP config
+│   ├── crisp_pilot.yaml        # tiny smoke/pilot config
 │   ├── vacs_7b.yaml            # VACS full method 7B
 │   ├── vacs_kaggle_smoke.yaml  # VACS bounded Kaggle smoke (3 steps, 8 examples)
 │   ├── cibo_v2_7b.yaml         # CIBO v2 FIXED-beta 7B
@@ -70,12 +73,12 @@
 
 | Method       | RL signal             | Self-distillation term      | Surgery             | Beta        |
 |--------------|-----------------------|------------------------------|---------------------|-------------|
-| CRISP        | sequence-mean PPO     | gated forward-KL on teacher  | symmetric PC-Grad   | n/a         |
-| VACS-CRISP   | token-level tanh-credit | gated forward-KL on teacher  | asymmetric PC-Grad protecting RL | n/a |
-| CIBO-CRISP v2| sequence-mean PPO     | single composite loss_ib + EMA anchor | none (scalar loss) | fixed or adaptive |
-| REINFORCE++  | sequence-mean PPO     | none                         | none                | n/a         |
-| OPSD         | none (SD only)        | forward-KL on teacher        | none                | n/a         |
-| GRPO         | group-relative PPO    | none                         | none                | n/a         |
+| CRISP        | global-effective-batch policy gradient | correctness-gated forward KL | symmetric PC-Grad | n/a |
+| VACS-CRISP   | sign-aware token-weighted policy gradient | soft-gated forward KL | asymmetric projection protecting RL | n/a |
+| CIBO-CRISP v2| sign-preserving token policy gradient | forward-KL IB proxy + EMA anchor | none (single scalar loss) | fixed or adaptive |
+| REINFORCE++  | global-effective-batch policy gradient | none | none | n/a |
+| OPSD         | none (SD only) | forward KL, teacher detached | none | n/a |
+| GRPO         | group-relative policy gradient (`k>=2`) | none | none | n/a |
 
 VACS keeps the original CRISP loss shape (loss_rl + lambda * loss_sd)
 and adds four orthogonal improvements: token credit, soft reward
@@ -88,8 +91,8 @@
 ## Setup
 
 This project targets Python 3.11+ and CUDA-capable GPUs. The
-minimum dependencies are `torch`, `transformers`, `peft`, `trl`,
-`sympy`, `pyyaml`, `pytest`, `numpy`, and (for math verification)
+minimum dependencies are `torch`, `transformers`, `datasets`, `peft`,
+`accelerate`, `sympy`, `pyyaml`, `pytest`, `numpy`, and (for math verification)
 `math-verify`. The smoke configs target `Qwen/Qwen2.5-0.5B-Instruct`,
 which fits on a single 16 GB GPU at 4-bit precision.
 
@@ -118,6 +121,15 @@
 
 # CIBO v2 adaptive-beta 7B
 python train_launcher.py cibo-v2 --config config/cibo_v2_adaptive_beta_7b.yaml
+
+# Completion-only SFT, then initialize an RL method from the adapter
+python train_launcher.py sft --config config/sft_7b.yaml
+python train_launcher.py grpo --config config/paper_7b.yaml --k 8 \
+  --override model.initial_adapter=runs/sft_7b/final
+
+# Multi-GPU manual-gradient launch
+NUM_GPUS=2 METHOD=crisp CONFIG=config/paper_7b.yaml \
+  bash scripts/launch_distributed.sh
 ```
 
 For most knobs you can override at the command line:
@@ -145,8 +157,8 @@
 python scripts/kaggle_smoke.py --method cibo-v2 --dry-run
 ```
 
-Successful smoke runs produce: a final `checkpoint.pt`, a
-`metrics.jsonl` containing at least one record with the method's
+Successful smoke runs produce: a `final/` adapter checkpoint, a
+run JSONL containing at least one record with the method's
 required metric key (`vacs/loss_rl` or `cibo/loss_total`), and at
 least one non-empty response in `generations.jsonl`. The runner
 prints a PASS/FAIL report after the run.
@@ -191,8 +203,8 @@
 
 The four suites, in priority order, are:
 
-- `controls`: CRISP, REINFORCE++, OPSD, GRPO (k=1, k=8), CRISP
-  lambda sweep.
+- `controls`: CRISP, REINFORCE++, OPSD, a single-rollout
+  global-policy-gradient control, GRPO `k=8`, and a CRISP lambda sweep.
 - `vacs-core`: VACS full method + five one-factor ablations.
 - `cibo-fixed`: CIBO v2 fixed-beta full method + four one-factor
   ablations.
@@ -207,7 +219,8 @@
 
 - The KL term in both methods is a *variational KL proxy*, not the
   exact information-bottleneck integral. It is computed as
-  `forward_kl(student || teacher)` over the response span, which
+  `forward_kl(teacher || student)` over the response span, with the
+  teacher distribution detached, which
   introduces a known bias. This is documented in the code
   (`crisp/training/objectives.py:forward_kl_per_token`).
 - The CIBO v2 design's theoretical guarantees (variational
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/cibo_v2_7b.yaml /mnt/data/crisp_work/CRISP-main/config/cibo_v2_7b.yaml
--- /mnt/data/crisp_original/CRISP-main/config/cibo_v2_7b.yaml	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/cibo_v2_7b.yaml	2026-08-02 12:50:40.183716805 +0000
@@ -1,44 +1,34 @@
-# CIBO-CRISP v2 full-method 7B config (FIXED beta mode).
-#
-# Identical model / data / optimizer choices as config/vacs_7b.yaml.
-# The CIBO-specific section is `cibo_v2:`. Defaults reproduce the
-# operational plan's "documented fixed-beta mode":
-#   * beta = 0.10 (the design doc's reference value)
-#   * credit_lambda = 0.25 (tanh credit, bounded in [0, 1])
-#   * anchor_alpha = 0.01 (EMA-anchored regularizer)
-#   * ib_reduction = "sequence_sum" (the CIBO default; "token_mean"
-#     is an explicit ablation)
-#   * beta_mode = "fixed" -- this is THE fixed-mode config. An
-#     adaptive-mode config is config/cibo_v2_adaptive_beta_7b.yaml.
-#
-# A ablations.yaml "cibo-fixed" full variant points at this file.
-# One-factor ablations override ONLY cibo_v2.<name> each.
-
 seed: 0
-
 model:
-  name: "Qwen/Qwen2.5-Math-7B-Instruct"
-  torch_dtype: "bfloat16"
-  attn_implementation: "auto"
+  name: Qwen/Qwen2.5-Math-7B-Instruct
+  torch_dtype: bfloat16
+  attn_implementation: auto
   load_in_4bit: false
-  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
   local_files_only: false
-
+  initial_adapter: null
 lora:
   enabled: true
   r: 64
   alpha: 128
-  dropout: 0.05
-  target_modules: ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]
-
+  dropout: 0.0
+  target_modules:
+  - q_proj
+  - k_proj
+  - v_proj
+  - o_proj
+  - gate_proj
+  - up_proj
+  - down_proj
 optimizer:
-  lr: 1.0e-6
+  lr: 1.0e-06
   warmup_steps: 10
   max_grad_norm: 1.0
-  betas: [0.9, 0.999]
-  eps: 1.0e-8
+  betas:
+  - 0.9
+  - 0.999
+  eps: 1.0e-08
   weight_decay: 0.0
-
 training:
   total_steps: 1000
   micro_batch_size: 4
@@ -48,10 +38,13 @@
   save_every: 200
   eval_every: 100
   log_every: 1
-  adv_eps: 1.0e-8
+  adv_eps: 1.0e-08
   pcgrad_eps: 1.0e-12
-  output_dir: "runs/cibo_v2"
-
+  output_dir: runs/cibo_v2
+  resume_from: null
+  lambda_schedule: cosine
+  use_pcgrad: true
+  correctness_gate: true
 rollout:
   k: 1
   max_prompt_length: 512
@@ -59,62 +52,58 @@
   temperature: 1.0
   top_p: 1.0
   do_sample: true
-
 reward:
-  task: "math"
+  task: math
   use_math_verify: true
-
 data:
-  train_dataset: "gsm8k"
-  train_split: "train"
-  eval_datasets: ["math500"]
-  teacher_hint_template: >
-    Internal calibration note (do not reveal or reference this in your response):
-    the correct final answer to this problem is {answer}.
-    Reason step by step as usual and box your final answer.
+  train_dataset: gsm8k
+  train_split: train
+  eval_datasets:
+  - math500
+  teacher_hint_template: 'Internal calibration note (do not reveal or reference this
+    in your response): the correct final answer to this problem is {answer}. Reason
+    step by step as usual and box your final answer.
 
+    '
 logging:
-  backend: "none"
-  run_name: "cibo-v2"
-  wandb_project: "vacs-cibo"
-
+  backend: none
+  run_name: cibo-v2
+  wandb_project: vacs-cibo
 checkpointing:
   save_adapter_only: true
-
 evaluation:
   eval_batch_size: 8
   eval_subset_size: null
   n_bootstrap: 1000
   max_new_tokens: 512
-
-# ---- VACS knobs (kept for compatibility; not used by CIBO v2) ----
+  bootstrap_seed: 0
 vacs:
   token_credit_mix: 0.25
-  token_weight_epsilon: 0.20
+  token_weight_epsilon: 0.2
   sd_weight: 1.0
   soft_gate_slope: 12.0
   reward_threshold: 0.5
   adaptive_mix:
     enabled: true
     ema_decay: 0.95
-    multiplier_min: 0.10
+    multiplier_min: 0.1
     multiplier_max: 5.0
-
-# ---- CIBO v2 knobs ----
+  use_soft_gate: true
+  use_asymmetric_projection: true
 cibo_v2:
   use_soft_gate: true
   credit_lambda: 0.25
-  beta: 0.10
-  beta_mode: "fixed"
+  beta: 0.1
+  beta_mode: fixed
   beta_min: 0.001
   beta_max: 10.0
   beta_step_size: 0.05
   beta_ema_decay: 0.95
   beta_reference_warmup_steps: 20
-  beta_denominator_epsilon: 1.0e-6
-  ib_reduction: "sequence_sum"
+  beta_denominator_epsilon: 1.0e-06
+  ib_reduction: sequence_sum
   soft_gate_slope: 12.0
   reward_threshold: 0.5
   anchor_alpha: 0.01
   anchor_ema_decay: 0.995
-  anchor_storage: "same"
+  anchor_storage: same
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/cibo_v2_adaptive_beta_7b.yaml /mnt/data/crisp_work/CRISP-main/config/cibo_v2_adaptive_beta_7b.yaml
--- /mnt/data/crisp_original/CRISP-main/config/cibo_v2_adaptive_beta_7b.yaml	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/cibo_v2_adaptive_beta_7b.yaml	2026-08-02 12:50:40.175716804 +0000
@@ -1,44 +1,34 @@
-# CIBO-CRISP v2 ADAPTIVE-beta 7B config.
-#
-# Same model / data / optimizer as config/cibo_v2_7b.yaml EXCEPT
-# `cibo_v2.beta_mode` is "adaptive_sign" and the controller's step
-# size is non-zero. The plan calls out that a comparison between
-# fixed-beta and adaptive-beta is invalid if it mixes them under the
-# same label -- this config + cibo_v2_7b.yaml exist precisely so
-# that ablations.yaml can compare them on equal footing.
-#
-# The beta controller's adaptive updates are bounded by:
-#   * beta_step_size = 0.05 (per-step multiplicative change fraction)
-#   * beta_min / beta_max (clamp)
-#   * beta_denominator_epsilon (near-zero delta holds beta stable)
-#   * beta_reference_warmup_steps (reference EMA only built during
-#     warm-up; frozen afterwards)
-
 seed: 0
-
 model:
-  name: "Qwen/Qwen2.5-Math-7B-Instruct"
-  torch_dtype: "bfloat16"
-  attn_implementation: "auto"
+  name: Qwen/Qwen2.5-Math-7B-Instruct
+  torch_dtype: bfloat16
+  attn_implementation: auto
   load_in_4bit: false
-  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
   local_files_only: false
-
+  initial_adapter: null
 lora:
   enabled: true
   r: 64
   alpha: 128
-  dropout: 0.05
-  target_modules: ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]
-
+  dropout: 0.0
+  target_modules:
+  - q_proj
+  - k_proj
+  - v_proj
+  - o_proj
+  - gate_proj
+  - up_proj
+  - down_proj
 optimizer:
-  lr: 1.0e-6
+  lr: 1.0e-06
   warmup_steps: 10
   max_grad_norm: 1.0
-  betas: [0.9, 0.999]
-  eps: 1.0e-8
+  betas:
+  - 0.9
+  - 0.999
+  eps: 1.0e-08
   weight_decay: 0.0
-
 training:
   total_steps: 1000
   micro_batch_size: 4
@@ -48,10 +38,13 @@
   save_every: 200
   eval_every: 100
   log_every: 1
-  adv_eps: 1.0e-8
+  adv_eps: 1.0e-08
   pcgrad_eps: 1.0e-12
-  output_dir: "runs/cibo_v2_adaptive_beta"
-
+  output_dir: runs/cibo_v2_adaptive_beta
+  resume_from: null
+  lambda_schedule: cosine
+  use_pcgrad: true
+  correctness_gate: true
 rollout:
   k: 1
   max_prompt_length: 512
@@ -59,60 +52,58 @@
   temperature: 1.0
   top_p: 1.0
   do_sample: true
-
 reward:
-  task: "math"
+  task: math
   use_math_verify: true
-
 data:
-  train_dataset: "gsm8k"
-  train_split: "train"
-  eval_datasets: ["math500"]
-  teacher_hint_template: >
-    Internal calibration note (do not reveal or reference this in your response):
-    the correct final answer to this problem is {answer}.
-    Reason step by step as usual and box your final answer.
+  train_dataset: gsm8k
+  train_split: train
+  eval_datasets:
+  - math500
+  teacher_hint_template: 'Internal calibration note (do not reveal or reference this
+    in your response): the correct final answer to this problem is {answer}. Reason
+    step by step as usual and box your final answer.
 
+    '
 logging:
-  backend: "none"
-  run_name: "cibo-v2-adaptive"
-  wandb_project: "vacs-cibo"
-
+  backend: none
+  run_name: cibo-v2-adaptive
+  wandb_project: vacs-cibo
 checkpointing:
   save_adapter_only: true
-
 evaluation:
   eval_batch_size: 8
   eval_subset_size: null
   n_bootstrap: 1000
   max_new_tokens: 512
-
+  bootstrap_seed: 0
 vacs:
   token_credit_mix: 0.25
-  token_weight_epsilon: 0.20
+  token_weight_epsilon: 0.2
   sd_weight: 1.0
   soft_gate_slope: 12.0
   reward_threshold: 0.5
   adaptive_mix:
     enabled: true
     ema_decay: 0.95
-    multiplier_min: 0.10
+    multiplier_min: 0.1
     multiplier_max: 5.0
-
-# ---- CIBO v2 ADAPTIVE mode ----
+  use_soft_gate: true
+  use_asymmetric_projection: true
 cibo_v2:
   credit_lambda: 0.25
-  beta: 0.10                # initial value; the controller updates it.
-  beta_mode: "adaptive_sign"
+  beta: 0.1
+  beta_mode: adaptive_sign
   beta_min: 0.001
   beta_max: 10.0
   beta_step_size: 0.05
   beta_ema_decay: 0.95
   beta_reference_warmup_steps: 20
-  beta_denominator_epsilon: 1.0e-6
-  ib_reduction: "sequence_sum"
+  beta_denominator_epsilon: 1.0e-06
+  ib_reduction: sequence_sum
   soft_gate_slope: 12.0
   reward_threshold: 0.5
   anchor_alpha: 0.01
   anchor_ema_decay: 0.995
-  anchor_storage: "same"
+  anchor_storage: same
+  use_soft_gate: true
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/cibo_v2_kaggle_smoke.yaml /mnt/data/crisp_work/CRISP-main/config/cibo_v2_kaggle_smoke.yaml
--- /mnt/data/crisp_original/CRISP-main/config/cibo_v2_kaggle_smoke.yaml	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/cibo_v2_kaggle_smoke.yaml	2026-08-02 12:50:40.167716804 +0000
@@ -1,51 +1,45 @@
-# CIBO-CRISP v2 Kaggle smoke config.
-#
-# Bounded, observable smoke test for the CIBO v2 method. Identical
-# model / data / optimizer settings as vacs_kaggle_smoke.yaml except
-# the method-specific knobs (beta_mode=fixed per the plan) and the
-# output directory. The plan explicitly calls out that the CIBO smoke
-# config must keep `beta_mode: fixed` so the smoke test verifies the
-# fixed-mode plumbing (the adaptive mode is exercised in the larger
-# `cibo-adaptive` ablation suite).
-
 seed: 0
-
 model:
-  name: "Qwen/Qwen2.5-0.5B-Instruct"
-  torch_dtype: "float16"
-  attn_implementation: "auto"
+  name: Qwen/Qwen2.5-0.5B-Instruct
+  torch_dtype: float16
+  attn_implementation: auto
   load_in_4bit: true
-  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
   local_files_only: false
-
+  initial_adapter: null
 lora:
   enabled: true
   r: 8
   alpha: 16
-  dropout: 0.05
-  target_modules: ["q_proj", "v_proj"]
-
+  dropout: 0.0
+  target_modules:
+  - q_proj
+  - v_proj
 optimizer:
-  lr: 1.0e-5
+  lr: 1.0e-05
   warmup_steps: 0
   max_grad_norm: 1.0
-  betas: [0.9, 0.999]
-  eps: 1.0e-8
+  betas:
+  - 0.9
+  - 0.999
+  eps: 1.0e-08
   weight_decay: 0.0
-
 training:
   total_steps: 3
   micro_batch_size: 1
   grad_accum_steps: 1
   clip_epsilon: 0.2
   lambda_max: 1.0
-  save_every: 0           # smoke: never evaluate mid-run
-  eval_every: 0           # smoke: never evaluate mid-run
+  save_every: 0
+  eval_every: 0
   log_every: 1
-  adv_eps: 1.0e-8
+  adv_eps: 1.0e-08
   pcgrad_eps: 1.0e-12
-  output_dir: "runs/kaggle_smoke/cibo_v2"
-
+  output_dir: runs/kaggle_smoke/cibo_v2
+  resume_from: null
+  lambda_schedule: cosine
+  use_pcgrad: true
+  correctness_gate: true
 rollout:
   k: 1
   max_prompt_length: 192
@@ -53,61 +47,57 @@
   temperature: 1.0
   top_p: 1.0
   do_sample: true
-
 reward:
-  task: "math"
+  task: math
   use_math_verify: true
-
 data:
-  train_dataset: "gsm8k"
-  train_split: "train"
-  train_subset_size: 8     # smoke: cap the dataset BEFORE training
-  eval_datasets: []        # smoke: no eval datasets
-  teacher_hint_template: >
-    Internal calibration note (do not reveal or reference this in your response):
-    the correct final answer to this problem is {answer}.
-    Reason step by step as usual and box your final answer.
+  train_dataset: gsm8k
+  train_split: train
+  train_subset_size: 8
+  eval_datasets: []
+  teacher_hint_template: 'Internal calibration note (do not reveal or reference this
+    in your response): the correct final answer to this problem is {answer}. Reason
+    step by step as usual and box your final answer.
 
+    '
 logging:
-  backend: "none"
-  run_name: "kaggle_smoke_cibo_v2"
-
+  backend: none
+  run_name: kaggle_smoke_cibo_v2
 checkpointing:
   save_adapter_only: true
-
 evaluation:
   eval_batch_size: 1
   eval_subset_size: null
   n_bootstrap: 100
   max_new_tokens: 256
-
-# ---- VACS knobs (kept for compatibility; not used by CIBO smoke) ----
+  bootstrap_seed: 0
 vacs:
   token_credit_mix: 0.25
-  token_weight_epsilon: 0.20
+  token_weight_epsilon: 0.2
   sd_weight: 1.0
   soft_gate_slope: 12.0
   reward_threshold: 0.5
   adaptive_mix:
     enabled: true
     ema_decay: 0.95
-    multiplier_min: 0.10
+    multiplier_min: 0.1
     multiplier_max: 5.0
-
-# ---- CIBO v2 knobs (smoke: keep beta_mode=fixed per plan) ----
+  use_soft_gate: true
+  use_asymmetric_projection: true
 cibo_v2:
   credit_lambda: 0.25
-  beta: 0.10
-  beta_mode: "fixed"
+  beta: 0.1
+  beta_mode: fixed
   beta_min: 0.001
   beta_max: 10.0
   beta_step_size: 0.05
   beta_ema_decay: 0.95
   beta_reference_warmup_steps: 20
-  beta_denominator_epsilon: 1.0e-6
-  ib_reduction: "sequence_sum"
+  beta_denominator_epsilon: 1.0e-06
+  ib_reduction: sequence_sum
   soft_gate_slope: 12.0
   reward_threshold: 0.5
   anchor_alpha: 0.01
   anchor_ema_decay: 0.995
-  anchor_storage: "same"
\ No newline at end of file
+  anchor_storage: same
+  use_soft_gate: true
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/crisp_7b.yaml /mnt/data/crisp_work/CRISP-main/config/crisp_7b.yaml
--- /mnt/data/crisp_original/CRISP-main/config/crisp_7b.yaml	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/crisp_7b.yaml	2026-08-02 12:50:40.163716804 +0000
@@ -1,40 +1,29 @@
-# Full-scale target config (README Section 5.2): Qwen2.5-Math-7B-Instruct,
-# LoRA r=64/alpha=128, bf16. micro_batch_size * grad_accum_steps gives the
-# effective batch size REINFORCE++'s global advantage normalization wants to
-# be statistically meaningful (the README calls out 512-1024) -- 16 * 4 = 64
-# below is a conservative starting point for a single modern GPU; raise
-# grad_accum_steps to climb toward that range without raising peak memory.
-#
-# Sized for roughly a 2x A100 80GB / 2x A40 48GB class machine running the
-# model on ONE of the two GPUs (see model/load.py's device placement notes
-# for why this codebase deliberately doesn't shard a single model across
-# GPUs by default). For a single smaller GPU: set model.load_in_4bit: true
-# and reduce micro_batch_size and/or rollout.max_new_tokens. Either way, run
-# config/crisp_pilot.yaml first -- see the top-level README.
-
 seed: 0
-
 model:
-  name: "Qwen/Qwen2.5-Math-7B-Instruct"
-  torch_dtype: "bfloat16"
-  attn_implementation: "auto"
+  name: Qwen/Qwen2.5-Math-7B-Instruct
+  torch_dtype: bfloat16
+  attn_implementation: auto
   load_in_4bit: false
-  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
-  # See config/crisp_pilot.yaml's comment on this -- same idea, bigger download.
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
   local_files_only: false
-
+  initial_adapter: null
 lora:
   enabled: true
   r: 64
   alpha: 128
-  dropout: 0.05
-  target_modules: ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]
-
+  dropout: 0.0
+  target_modules:
+  - q_proj
+  - k_proj
+  - v_proj
+  - o_proj
+  - gate_proj
+  - up_proj
+  - down_proj
 optimizer:
-  lr: 1.0e-6
+  lr: 1.0e-06
   warmup_steps: 20
   max_grad_norm: 1.0
-
 training:
   total_steps: 1500
   micro_batch_size: 16
@@ -44,8 +33,11 @@
   save_every: 100
   eval_every: 100
   log_every: 5
-  output_dir: "runs/crisp_7b"
-
+  output_dir: runs/crisp_7b
+  resume_from: null
+  lambda_schedule: cosine
+  use_pcgrad: true
+  correctness_gate: true
 rollout:
   k: 1
   max_prompt_length: 512
@@ -53,21 +45,26 @@
   temperature: 1.0
   top_p: 1.0
   do_sample: true
-
 reward:
-  task: "math"
+  task: math
   use_math_verify: true
-
 data:
-  train_dataset: "numina_math_cot"
-  train_split: "train"
-  eval_datasets: ["aime2024", "aime2025", "math500"]
-
+  train_dataset: numina_math_cot
+  train_split: train
+  eval_datasets:
+  - aime2024
+  - aime2025
+  - math500
 logging:
-  backend: "none"   # set to "wandb" (and `pip install wandb && wandb login`) to mirror logs there
-  run_name: "crisp_7b"
-
+  backend: none
+  run_name: crisp_7b
 evaluation:
   eval_batch_size: 8
   eval_subset_size: null
   max_new_tokens: 1024
+  bootstrap_seed: 0
+vacs:
+  use_soft_gate: true
+  use_asymmetric_projection: true
+cibo_v2:
+  use_soft_gate: true
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/crisp_pilot.yaml /mnt/data/crisp_work/CRISP-main/config/crisp_pilot.yaml
--- /mnt/data/crisp_original/CRISP-main/config/crisp_pilot.yaml	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/crisp_pilot.yaml	2026-08-02 12:50:40.159716804 +0000
@@ -1,39 +1,26 @@
-# Small-scale pilot config: verifies the whole pipeline (model load, dataset
-# load, rollout generation, reward computation, teacher+student forward
-# pass, PC-Grad step, checkpoint save) end to end, quickly, on a single
-# small/consumer GPU -- or even CPU, just slowly.
-#
-# This is NOT meant to produce a good model. Run this FIRST, before
-# crisp_7b.yaml, to confirm the plumbing is correct on your machine: a
-# handful of steps that complete without a device-mismatch or shape error is
-# worth far more than staring at a 7B config for an hour before finding out
-# the same way. See the top-level README's "Smoke test" section.
-
 seed: 0
-
 model:
-  name: "Qwen/Qwen2.5-0.5B-Instruct"
-  torch_dtype: "bfloat16"
-  attn_implementation: "auto"
+  name: Qwen/Qwen2.5-0.5B-Instruct
+  torch_dtype: bfloat16
+  attn_implementation: auto
   load_in_4bit: false
-  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
-  # If model download is slow or hangs, run `python predownload.py
-  # Qwen/Qwen2.5-0.5B-Instruct` on its own first (see README), then flip
-  # this to true so training never touches the network again.
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
   local_files_only: false
-
+  initial_adapter: null
 lora:
   enabled: true
   r: 16
   alpha: 32
-  dropout: 0.05
-  target_modules: ["q_proj", "k_proj", "v_proj", "o_proj"]
-
+  dropout: 0.0
+  target_modules:
+  - q_proj
+  - k_proj
+  - v_proj
+  - o_proj
 optimizer:
-  lr: 1.0e-5
+  lr: 1.0e-05
   warmup_steps: 5
   max_grad_norm: 1.0
-
 training:
   total_steps: 50
   micro_batch_size: 2
@@ -43,36 +30,36 @@
   save_every: 25
   eval_every: 25
   log_every: 1
-  output_dir: "runs/crisp_pilot"
-
+  output_dir: runs/crisp_pilot
+  resume_from: null
+  lambda_schedule: cosine
+  use_pcgrad: true
+  correctness_gate: true
 rollout:
   k: 1
   max_prompt_length: 256
-  # 128 was too tight: a "reason step by step" system prompt genuinely needs
-  # room to finish and emit eos naturally. Too small a budget here shows up
-  # as response_len_mean pinned exactly at max_new_tokens with ~zero
-  # variance across steps (every rollout hard-truncated mid-reasoning,
-  # never reaching a boxed answer) -- if you see that again after raising
-  # this, the model is being cut off before it can conclude, not stuck.
   max_new_tokens: 256
   temperature: 1.0
   top_p: 1.0
   do_sample: true
-
 reward:
-  task: "math"
+  task: math
   use_math_verify: true
-
 data:
-  train_dataset: "gsm8k"
-  train_split: "train"
-  eval_datasets: ["gsm8k"]
-
+  train_dataset: gsm8k
+  train_split: train
+  eval_datasets:
+  - gsm8k
 logging:
-  backend: "none"
-  run_name: "crisp_pilot"
-
+  backend: none
+  run_name: crisp_pilot
 evaluation:
   eval_batch_size: 4
   eval_subset_size: 20
   max_new_tokens: 256
+  bootstrap_seed: 0
+vacs:
+  use_soft_gate: true
+  use_asymmetric_projection: true
+cibo_v2:
+  use_soft_gate: true
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/paper_7b.yaml /mnt/data/crisp_work/CRISP-main/config/paper_7b.yaml
--- /mnt/data/crisp_original/CRISP-main/config/paper_7b.yaml	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/paper_7b.yaml	2026-08-02 12:50:40.187716805 +0000
@@ -0,0 +1,113 @@
+seed: 0
+model:
+  name: Qwen/Qwen2.5-Math-7B-Instruct
+  torch_dtype: bfloat16
+  attn_implementation: auto
+  load_in_4bit: false
+  trust_remote_code: false
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
+  local_files_only: false
+  initial_adapter: null
+lora:
+  enabled: true
+  r: 64
+  alpha: 128
+  dropout: 0.0
+  target_modules:
+  - q_proj
+  - k_proj
+  - v_proj
+  - o_proj
+  - gate_proj
+  - up_proj
+  - down_proj
+optimizer:
+  lr: 1.0e-06
+  warmup_steps: 20
+  max_grad_norm: 1.0
+  betas:
+  - 0.9
+  - 0.999
+  eps: 1.0e-08
+  weight_decay: 0.0
+training:
+  total_steps: 1000
+  micro_batch_size: 4
+  grad_accum_steps: 4
+  lambda_max: 1.0
+  lambda_schedule: cosine
+  use_pcgrad: true
+  correctness_gate: true
+  resume_from: null
+  save_every: 250
+  eval_every: 0
+  log_every: 5
+  adv_eps: 1.0e-08
+  pcgrad_eps: 1.0e-12
+  output_dir: runs/paper
+rollout:
+  k: 8
+  max_prompt_length: 512
+  max_new_tokens: 512
+  temperature: 1.0
+  top_p: 1.0
+  do_sample: true
+reward:
+  task: math
+  use_math_verify: true
+  code_exec_timeout: 10.0
+data:
+  train_dataset: numina_math_cot
+  train_split: train
+  train_subset_size: null
+  eval_datasets:
+  - gsm8k
+  - math500
+  - aime2024
+  - aime2025
+  - hmmt2025
+  teacher_hint_template: 'Internal calibration note (do not reveal or reference this
+    in your response): the correct final answer to this problem is {answer}. Reason
+    step by step as usual and box your final answer.'
+logging:
+  backend: none
+  wandb_project: crisp-paper
+  run_name: null
+checkpointing:
+  save_adapter_only: true
+evaluation:
+  eval_batch_size: 8
+  eval_subset_size: null
+  n_bootstrap: 2000
+  bootstrap_seed: 0
+  max_new_tokens: 1024
+vacs:
+  token_credit_mix: 0.25
+  token_weight_epsilon: 0.2
+  sd_weight: 1.0
+  soft_gate_slope: 12.0
+  reward_threshold: 0.5
+  use_soft_gate: true
+  use_asymmetric_projection: true
+  adaptive_mix:
+    enabled: true
+    ema_decay: 0.95
+    multiplier_min: 0.1
+    multiplier_max: 5.0
+cibo_v2:
+  credit_lambda: 0.25
+  beta: 0.1
+  beta_mode: fixed
+  beta_min: 0.001
+  beta_max: 10.0
+  beta_step_size: 0.05
+  beta_ema_decay: 0.95
+  beta_reference_warmup_steps: 20
+  beta_denominator_epsilon: 1.0e-06
+  ib_reduction: sequence_sum
+  soft_gate_slope: 12.0
+  reward_threshold: 0.5
+  use_soft_gate: true
+  anchor_alpha: 0.01
+  anchor_ema_decay: 0.995
+  anchor_storage: same
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/scaling/qwen25_14b.yaml /mnt/data/crisp_work/CRISP-main/config/scaling/qwen25_14b.yaml
--- /mnt/data/crisp_original/CRISP-main/config/scaling/qwen25_14b.yaml	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/scaling/qwen25_14b.yaml	2026-08-02 12:50:40.223716806 +0000
@@ -0,0 +1,113 @@
+checkpointing:
+  save_adapter_only: true
+cibo_v2:
+  anchor_alpha: 0.01
+  anchor_ema_decay: 0.995
+  anchor_storage: same
+  beta: 0.1
+  beta_denominator_epsilon: 1.0e-06
+  beta_ema_decay: 0.95
+  beta_max: 10.0
+  beta_min: 0.001
+  beta_mode: fixed
+  beta_reference_warmup_steps: 20
+  beta_step_size: 0.05
+  credit_lambda: 0.25
+  ib_reduction: sequence_sum
+  reward_threshold: 0.5
+  soft_gate_slope: 12.0
+  use_soft_gate: true
+data:
+  eval_datasets:
+  - gsm8k
+  - math500
+  - aime2024
+  - aime2025
+  - hmmt2025
+  teacher_hint_template: 'Internal calibration note (do not reveal or reference this
+    in your response): the correct final answer to this problem is {answer}. Reason
+    step by step as usual and box your final answer.'
+  train_dataset: numina_math_cot
+  train_split: train
+  train_subset_size: null
+evaluation:
+  bootstrap_seed: 0
+  eval_batch_size: 8
+  eval_subset_size: null
+  max_new_tokens: 1024
+  n_bootstrap: 2000
+logging:
+  backend: none
+  run_name: qwen25_14b
+  wandb_project: crisp-paper
+lora:
+  alpha: 128
+  dropout: 0.0
+  enabled: true
+  r: 64
+  target_modules:
+  - q_proj
+  - k_proj
+  - v_proj
+  - o_proj
+  - gate_proj
+  - up_proj
+  - down_proj
+model:
+  attn_implementation: auto
+  initial_adapter: null
+  load_in_4bit: false
+  local_files_only: false
+  name: Qwen/Qwen2.5-14B-Instruct
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
+  torch_dtype: bfloat16
+  trust_remote_code: false
+optimizer:
+  betas:
+  - 0.9
+  - 0.999
+  eps: 1.0e-08
+  lr: 1.0e-06
+  max_grad_norm: 1.0
+  warmup_steps: 20
+  weight_decay: 0.0
+reward:
+  code_exec_timeout: 10.0
+  task: math
+  use_math_verify: true
+rollout:
+  do_sample: true
+  k: 8
+  max_new_tokens: 512
+  max_prompt_length: 512
+  temperature: 1.0
+  top_p: 1.0
+seed: 0
+training:
+  adv_eps: 1.0e-08
+  correctness_gate: true
+  eval_every: 0
+  grad_accum_steps: 4
+  lambda_max: 1.0
+  lambda_schedule: cosine
+  log_every: 5
+  micro_batch_size: 4
+  output_dir: runs/scaling/qwen25_14b
+  pcgrad_eps: 1.0e-12
+  resume_from: null
+  save_every: 250
+  total_steps: 1000
+  use_pcgrad: true
+vacs:
+  adaptive_mix:
+    ema_decay: 0.95
+    enabled: true
+    multiplier_max: 5.0
+    multiplier_min: 0.1
+  reward_threshold: 0.5
+  sd_weight: 1.0
+  soft_gate_slope: 12.0
+  token_credit_mix: 0.25
+  token_weight_epsilon: 0.2
+  use_asymmetric_projection: true
+  use_soft_gate: true
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/scaling/qwen25_1p5b.yaml /mnt/data/crisp_work/CRISP-main/config/scaling/qwen25_1p5b.yaml
--- /mnt/data/crisp_original/CRISP-main/config/scaling/qwen25_1p5b.yaml	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/scaling/qwen25_1p5b.yaml	2026-08-02 12:50:40.203716805 +0000
@@ -0,0 +1,113 @@
+checkpointing:
+  save_adapter_only: true
+cibo_v2:
+  anchor_alpha: 0.01
+  anchor_ema_decay: 0.995
+  anchor_storage: same
+  beta: 0.1
+  beta_denominator_epsilon: 1.0e-06
+  beta_ema_decay: 0.95
+  beta_max: 10.0
+  beta_min: 0.001
+  beta_mode: fixed
+  beta_reference_warmup_steps: 20
+  beta_step_size: 0.05
+  credit_lambda: 0.25
+  ib_reduction: sequence_sum
+  reward_threshold: 0.5
+  soft_gate_slope: 12.0
+  use_soft_gate: true
+data:
+  eval_datasets:
+  - gsm8k
+  - math500
+  - aime2024
+  - aime2025
+  - hmmt2025
+  teacher_hint_template: 'Internal calibration note (do not reveal or reference this
+    in your response): the correct final answer to this problem is {answer}. Reason
+    step by step as usual and box your final answer.'
+  train_dataset: numina_math_cot
+  train_split: train
+  train_subset_size: null
+evaluation:
+  bootstrap_seed: 0
+  eval_batch_size: 8
+  eval_subset_size: null
+  max_new_tokens: 1024
+  n_bootstrap: 2000
+logging:
+  backend: none
+  run_name: qwen25_1p5b
+  wandb_project: crisp-paper
+lora:
+  alpha: 128
+  dropout: 0.0
+  enabled: true
+  r: 64
+  target_modules:
+  - q_proj
+  - k_proj
+  - v_proj
+  - o_proj
+  - gate_proj
+  - up_proj
+  - down_proj
+model:
+  attn_implementation: auto
+  initial_adapter: null
+  load_in_4bit: false
+  local_files_only: false
+  name: Qwen/Qwen2.5-1.5B-Instruct
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
+  torch_dtype: bfloat16
+  trust_remote_code: false
+optimizer:
+  betas:
+  - 0.9
+  - 0.999
+  eps: 1.0e-08
+  lr: 1.0e-06
+  max_grad_norm: 1.0
+  warmup_steps: 20
+  weight_decay: 0.0
+reward:
+  code_exec_timeout: 10.0
+  task: math
+  use_math_verify: true
+rollout:
+  do_sample: true
+  k: 8
+  max_new_tokens: 512
+  max_prompt_length: 512
+  temperature: 1.0
+  top_p: 1.0
+seed: 0
+training:
+  adv_eps: 1.0e-08
+  correctness_gate: true
+  eval_every: 0
+  grad_accum_steps: 4
+  lambda_max: 1.0
+  lambda_schedule: cosine
+  log_every: 5
+  micro_batch_size: 4
+  output_dir: runs/scaling/qwen25_1p5b
+  pcgrad_eps: 1.0e-12
+  resume_from: null
+  save_every: 250
+  total_steps: 1000
+  use_pcgrad: true
+vacs:
+  adaptive_mix:
+    ema_decay: 0.95
+    enabled: true
+    multiplier_max: 5.0
+    multiplier_min: 0.1
+  reward_threshold: 0.5
+  sd_weight: 1.0
+  soft_gate_slope: 12.0
+  token_credit_mix: 0.25
+  token_weight_epsilon: 0.2
+  use_asymmetric_projection: true
+  use_soft_gate: true
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/scaling/qwen25_3b.yaml /mnt/data/crisp_work/CRISP-main/config/scaling/qwen25_3b.yaml
--- /mnt/data/crisp_original/CRISP-main/config/scaling/qwen25_3b.yaml	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/scaling/qwen25_3b.yaml	2026-08-02 12:50:40.207716805 +0000
@@ -0,0 +1,113 @@
+checkpointing:
+  save_adapter_only: true
+cibo_v2:
+  anchor_alpha: 0.01
+  anchor_ema_decay: 0.995
+  anchor_storage: same
+  beta: 0.1
+  beta_denominator_epsilon: 1.0e-06
+  beta_ema_decay: 0.95
+  beta_max: 10.0
+  beta_min: 0.001
+  beta_mode: fixed
+  beta_reference_warmup_steps: 20
+  beta_step_size: 0.05
+  credit_lambda: 0.25
+  ib_reduction: sequence_sum
+  reward_threshold: 0.5
+  soft_gate_slope: 12.0
+  use_soft_gate: true
+data:
+  eval_datasets:
+  - gsm8k
+  - math500
+  - aime2024
+  - aime2025
+  - hmmt2025
+  teacher_hint_template: 'Internal calibration note (do not reveal or reference this
+    in your response): the correct final answer to this problem is {answer}. Reason
+    step by step as usual and box your final answer.'
+  train_dataset: numina_math_cot
+  train_split: train
+  train_subset_size: null
+evaluation:
+  bootstrap_seed: 0
+  eval_batch_size: 8
+  eval_subset_size: null
+  max_new_tokens: 1024
+  n_bootstrap: 2000
+logging:
+  backend: none
+  run_name: qwen25_3b
+  wandb_project: crisp-paper
+lora:
+  alpha: 128
+  dropout: 0.0
+  enabled: true
+  r: 64
+  target_modules:
+  - q_proj
+  - k_proj
+  - v_proj
+  - o_proj
+  - gate_proj
+  - up_proj
+  - down_proj
+model:
+  attn_implementation: auto
+  initial_adapter: null
+  load_in_4bit: false
+  local_files_only: false
+  name: Qwen/Qwen2.5-3B-Instruct
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
+  torch_dtype: bfloat16
+  trust_remote_code: false
+optimizer:
+  betas:
+  - 0.9
+  - 0.999
+  eps: 1.0e-08
+  lr: 1.0e-06
+  max_grad_norm: 1.0
+  warmup_steps: 20
+  weight_decay: 0.0
+reward:
+  code_exec_timeout: 10.0
+  task: math
+  use_math_verify: true
+rollout:
+  do_sample: true
+  k: 8
+  max_new_tokens: 512
+  max_prompt_length: 512
+  temperature: 1.0
+  top_p: 1.0
+seed: 0
+training:
+  adv_eps: 1.0e-08
+  correctness_gate: true
+  eval_every: 0
+  grad_accum_steps: 4
+  lambda_max: 1.0
+  lambda_schedule: cosine
+  log_every: 5
+  micro_batch_size: 4
+  output_dir: runs/scaling/qwen25_3b
+  pcgrad_eps: 1.0e-12
+  resume_from: null
+  save_every: 250
+  total_steps: 1000
+  use_pcgrad: true
+vacs:
+  adaptive_mix:
+    ema_decay: 0.95
+    enabled: true
+    multiplier_max: 5.0
+    multiplier_min: 0.1
+  reward_threshold: 0.5
+  sd_weight: 1.0
+  soft_gate_slope: 12.0
+  token_credit_mix: 0.25
+  token_weight_epsilon: 0.2
+  use_asymmetric_projection: true
+  use_soft_gate: true
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/scaling/qwen25_7b.yaml /mnt/data/crisp_work/CRISP-main/config/scaling/qwen25_7b.yaml
--- /mnt/data/crisp_original/CRISP-main/config/scaling/qwen25_7b.yaml	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/scaling/qwen25_7b.yaml	2026-08-02 12:50:40.215716805 +0000
@@ -0,0 +1,113 @@
+checkpointing:
+  save_adapter_only: true
+cibo_v2:
+  anchor_alpha: 0.01
+  anchor_ema_decay: 0.995
+  anchor_storage: same
+  beta: 0.1
+  beta_denominator_epsilon: 1.0e-06
+  beta_ema_decay: 0.95
+  beta_max: 10.0
+  beta_min: 0.001
+  beta_mode: fixed
+  beta_reference_warmup_steps: 20
+  beta_step_size: 0.05
+  credit_lambda: 0.25
+  ib_reduction: sequence_sum
+  reward_threshold: 0.5
+  soft_gate_slope: 12.0
+  use_soft_gate: true
+data:
+  eval_datasets:
+  - gsm8k
+  - math500
+  - aime2024
+  - aime2025
+  - hmmt2025
+  teacher_hint_template: 'Internal calibration note (do not reveal or reference this
+    in your response): the correct final answer to this problem is {answer}. Reason
+    step by step as usual and box your final answer.'
+  train_dataset: numina_math_cot
+  train_split: train
+  train_subset_size: null
+evaluation:
+  bootstrap_seed: 0
+  eval_batch_size: 8
+  eval_subset_size: null
+  max_new_tokens: 1024
+  n_bootstrap: 2000
+logging:
+  backend: none
+  run_name: qwen25_7b
+  wandb_project: crisp-paper
+lora:
+  alpha: 128
+  dropout: 0.0
+  enabled: true
+  r: 64
+  target_modules:
+  - q_proj
+  - k_proj
+  - v_proj
+  - o_proj
+  - gate_proj
+  - up_proj
+  - down_proj
+model:
+  attn_implementation: auto
+  initial_adapter: null
+  load_in_4bit: false
+  local_files_only: false
+  name: Qwen/Qwen2.5-7B-Instruct
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
+  torch_dtype: bfloat16
+  trust_remote_code: false
+optimizer:
+  betas:
+  - 0.9
+  - 0.999
+  eps: 1.0e-08
+  lr: 1.0e-06
+  max_grad_norm: 1.0
+  warmup_steps: 20
+  weight_decay: 0.0
+reward:
+  code_exec_timeout: 10.0
+  task: math
+  use_math_verify: true
+rollout:
+  do_sample: true
+  k: 8
+  max_new_tokens: 512
+  max_prompt_length: 512
+  temperature: 1.0
+  top_p: 1.0
+seed: 0
+training:
+  adv_eps: 1.0e-08
+  correctness_gate: true
+  eval_every: 0
+  grad_accum_steps: 4
+  lambda_max: 1.0
+  lambda_schedule: cosine
+  log_every: 5
+  micro_batch_size: 4
+  output_dir: runs/scaling/qwen25_7b
+  pcgrad_eps: 1.0e-12
+  resume_from: null
+  save_every: 250
+  total_steps: 1000
+  use_pcgrad: true
+vacs:
+  adaptive_mix:
+    ema_decay: 0.95
+    enabled: true
+    multiplier_max: 5.0
+    multiplier_min: 0.1
+  reward_threshold: 0.5
+  sd_weight: 1.0
+  soft_gate_slope: 12.0
+  token_credit_mix: 0.25
+  token_weight_epsilon: 0.2
+  use_asymmetric_projection: true
+  use_soft_gate: true
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/sft_7b.yaml /mnt/data/crisp_work/CRISP-main/config/sft_7b.yaml
--- /mnt/data/crisp_original/CRISP-main/config/sft_7b.yaml	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/sft_7b.yaml	2026-08-02 12:50:40.195716805 +0000
@@ -0,0 +1,117 @@
+checkpointing:
+  save_adapter_only: true
+cibo_v2:
+  anchor_alpha: 0.01
+  anchor_ema_decay: 0.995
+  anchor_storage: same
+  beta: 0.1
+  beta_denominator_epsilon: 1.0e-06
+  beta_ema_decay: 0.95
+  beta_max: 10.0
+  beta_min: 0.001
+  beta_mode: fixed
+  beta_reference_warmup_steps: 20
+  beta_step_size: 0.05
+  credit_lambda: 0.25
+  ib_reduction: sequence_sum
+  reward_threshold: 0.5
+  soft_gate_slope: 12.0
+  use_soft_gate: true
+data:
+  eval_datasets:
+  - gsm8k
+  - math500
+  - aime2024
+  - aime2025
+  - hmmt2025
+  teacher_hint_template: 'Internal calibration note (do not reveal or reference this
+    in your response): the correct final answer to this problem is {answer}. Reason
+    step by step as usual and box your final answer.'
+  train_dataset: numina_math_cot
+  train_split: train
+  train_subset_size: null
+evaluation:
+  bootstrap_seed: 0
+  eval_batch_size: 8
+  eval_subset_size: null
+  max_new_tokens: 1024
+  n_bootstrap: 2000
+logging:
+  backend: none
+  run_name: sft_7b
+  wandb_project: crisp-paper
+lora:
+  alpha: 128
+  dropout: 0.0
+  enabled: true
+  r: 64
+  target_modules:
+  - q_proj
+  - k_proj
+  - v_proj
+  - o_proj
+  - gate_proj
+  - up_proj
+  - down_proj
+model:
+  attn_implementation: auto
+  initial_adapter: null
+  load_in_4bit: false
+  local_files_only: false
+  name: Qwen/Qwen2.5-Math-7B-Instruct
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
+  torch_dtype: bfloat16
+  trust_remote_code: false
+optimizer:
+  betas:
+  - 0.9
+  - 0.999
+  eps: 1.0e-08
+  lr: 1.0e-06
+  max_grad_norm: 1.0
+  warmup_steps: 20
+  weight_decay: 0.0
+reward:
+  code_exec_timeout: 10.0
+  task: math
+  use_math_verify: true
+rollout:
+  do_sample: true
+  k: 8
+  max_new_tokens: 512
+  max_prompt_length: 512
+  temperature: 1.0
+  top_p: 1.0
+seed: 0
+training:
+  adv_eps: 1.0e-08
+  correctness_gate: true
+  eval_every: 0
+  grad_accum_steps: 4
+  lambda_max: 1.0
+  lambda_schedule: cosine
+  log_every: 5
+  micro_batch_size: 4
+  output_dir: runs/sft_7b
+  pcgrad_eps: 1.0e-12
+  resume_from: null
+  save_every: 250
+  total_steps: 1000
+  use_pcgrad: true
+vacs:
+  adaptive_mix:
+    ema_decay: 0.95
+    enabled: true
+    multiplier_max: 5.0
+    multiplier_min: 0.1
+  reward_threshold: 0.5
+  sd_weight: 1.0
+  soft_gate_slope: 12.0
+  token_credit_mix: 0.25
+  token_weight_epsilon: 0.2
+  use_asymmetric_projection: true
+  use_soft_gate: true
+sft:
+  max_sequence_length: 2048
+  num_train_epochs: 1.0
+  completion_only_loss: true
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/vacs_7b.yaml /mnt/data/crisp_work/CRISP-main/config/vacs_7b.yaml
--- /mnt/data/crisp_original/CRISP-main/config/vacs_7b.yaml	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/vacs_7b.yaml	2026-08-02 12:50:40.179716805 +0000
@@ -1,42 +1,34 @@
-# VACS-CRISP full-method 7B config.
-#
-# Identical model / data / optimizer choices as config/crisp_7b.yaml
-# (the plan's "preserve original training settings" rule). The new
-# section is `vacs:` -- the VACS-specific knobs the ablation manifest
-# will toggle. Defaults reproduce the operational plan:
-#   * token credit mix rho = 0.25
-#   * soft reward gate with slope 12, threshold 0.5
-#   * adaptive mixing ON, EMA decay 0.95, multiplier clamp [0.10, 5.0]
-#
-# A ablations.yaml "vacs-core" full variant points at this file.
-# One-factor ablations (rho=0, no soft gate, fixed mixing, no asym
-# surgery, sd_weight=0) override ONLY one of these keys each.
-
 seed: 0
-
 model:
-  name: "Qwen/Qwen2.5-Math-7B-Instruct"
-  torch_dtype: "bfloat16"
-  attn_implementation: "auto"
+  name: Qwen/Qwen2.5-Math-7B-Instruct
+  torch_dtype: bfloat16
+  attn_implementation: auto
   load_in_4bit: false
-  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
   local_files_only: false
-
+  initial_adapter: null
 lora:
   enabled: true
   r: 64
   alpha: 128
-  dropout: 0.05
-  target_modules: ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]
-
+  dropout: 0.0
+  target_modules:
+  - q_proj
+  - k_proj
+  - v_proj
+  - o_proj
+  - gate_proj
+  - up_proj
+  - down_proj
 optimizer:
-  lr: 1.0e-6
+  lr: 1.0e-06
   warmup_steps: 10
   max_grad_norm: 1.0
-  betas: [0.9, 0.999]
-  eps: 1.0e-8
+  betas:
+  - 0.9
+  - 0.999
+  eps: 1.0e-08
   weight_decay: 0.0
-
 training:
   total_steps: 1000
   micro_batch_size: 4
@@ -46,10 +38,13 @@
   save_every: 200
   eval_every: 100
   log_every: 1
-  adv_eps: 1.0e-8
+  adv_eps: 1.0e-08
   pcgrad_eps: 1.0e-12
-  output_dir: "runs/vacs"
-
+  output_dir: runs/vacs
+  resume_from: null
+  lambda_schedule: cosine
+  use_pcgrad: true
+  correctness_gate: true
 rollout:
   k: 1
   max_prompt_length: 512
@@ -57,61 +52,58 @@
   temperature: 1.0
   top_p: 1.0
   do_sample: true
-
 reward:
-  task: "math"
+  task: math
   use_math_verify: true
-
 data:
-  train_dataset: "gsm8k"
-  train_split: "train"
-  eval_datasets: ["math500"]
-  teacher_hint_template: >
-    Internal calibration note (do not reveal or reference this in your response):
-    the correct final answer to this problem is {answer}.
-    Reason step by step as usual and box your final answer.
+  train_dataset: gsm8k
+  train_split: train
+  eval_datasets:
+  - math500
+  teacher_hint_template: 'Internal calibration note (do not reveal or reference this
+    in your response): the correct final answer to this problem is {answer}. Reason
+    step by step as usual and box your final answer.
 
+    '
 logging:
-  backend: "none"
-  run_name: "vacs"
-  wandb_project: "vacs-cibo"
-
+  backend: none
+  run_name: vacs
+  wandb_project: vacs-cibo
 checkpointing:
   save_adapter_only: true
-
 evaluation:
   eval_batch_size: 8
   eval_subset_size: null
   n_bootstrap: 1000
   max_new_tokens: 512
-
-# ---- VACS-specific knobs (the plan's "vacs" section) ----
+  bootstrap_seed: 0
 vacs:
   token_credit_mix: 0.25
-  token_weight_epsilon: 0.20
+  token_weight_epsilon: 0.2
   sd_weight: 1.0
   soft_gate_slope: 12.0
   reward_threshold: 0.5
   adaptive_mix:
     enabled: true
     ema_decay: 0.95
-    multiplier_min: 0.10
+    multiplier_min: 0.1
     multiplier_max: 5.0
-
-# ---- CIBO v2 knobs (kept for compatibility; not used by VACS) ----
+  use_soft_gate: true
+  use_asymmetric_projection: true
 cibo_v2:
   credit_lambda: 0.25
-  beta: 0.10
-  beta_mode: "fixed"
+  beta: 0.1
+  beta_mode: fixed
   beta_min: 0.001
   beta_max: 10.0
   beta_step_size: 0.05
   beta_ema_decay: 0.95
   beta_reference_warmup_steps: 20
-  beta_denominator_epsilon: 1.0e-6
-  ib_reduction: "sequence_sum"
+  beta_denominator_epsilon: 1.0e-06
+  ib_reduction: sequence_sum
   soft_gate_slope: 12.0
   reward_threshold: 0.5
   anchor_alpha: 0.01
   anchor_ema_decay: 0.995
-  anchor_storage: "same"
+  anchor_storage: same
+  use_soft_gate: true
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/config/vacs_kaggle_smoke.yaml /mnt/data/crisp_work/CRISP-main/config/vacs_kaggle_smoke.yaml
--- /mnt/data/crisp_original/CRISP-main/config/vacs_kaggle_smoke.yaml	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/config/vacs_kaggle_smoke.yaml	2026-08-02 12:50:40.155716804 +0000
@@ -1,62 +1,45 @@
-# VACS Kaggle smoke config.
-#
-# Bounded, observable smoke test for the VACS method. Designed to:
-#   - run end-to-end on a Kaggle single-GPU notebook (T4 / P100) in
-#     well under the 12-hour session budget,
-#   - produce a checkpoint, a metrics.jsonl with vacs/* keys, and a
-#     non-empty response generation (so we can confirm the rollout
-#     pipeline is wired up),
-#   - stay small enough that sparse/zero rewards in 3 steps are NOT a
-#     regression -- the smoke test verifies plumbing, not learning.
-#
-# Compare against config/vacs_7b.yaml (production 7B). The smoke uses a
-# 0.5B model because it fits comfortably in 16GB VRAM with 4-bit
-# quantisation and is small enough that the smoke completes in minutes
-# rather than hours.
-#
-# Pre-flight expectations (see scripts/kaggle_smoke.py):
-#   * cuda available
-#   * free disk > 2GB on the output path
-#   * the config is structurally valid (load_config does this)
-
 seed: 0
-
 model:
-  name: "Qwen/Qwen2.5-0.5B-Instruct"
-  torch_dtype: "float16"
-  attn_implementation: "auto"
+  name: Qwen/Qwen2.5-0.5B-Instruct
+  torch_dtype: float16
+  attn_implementation: auto
   load_in_4bit: true
-  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
+  system_prompt: Please reason step by step, and put your final answer within \boxed{}.
   local_files_only: false
-
+  initial_adapter: null
 lora:
   enabled: true
   r: 8
   alpha: 16
-  dropout: 0.05
-  target_modules: ["q_proj", "v_proj"]
-
+  dropout: 0.0
+  target_modules:
+  - q_proj
+  - v_proj
 optimizer:
-  lr: 1.0e-5
+  lr: 1.0e-05
   warmup_steps: 0
   max_grad_norm: 1.0
-  betas: [0.9, 0.999]
-  eps: 1.0e-8
+  betas:
+  - 0.9
+  - 0.999
+  eps: 1.0e-08
   weight_decay: 0.0
-
 training:
   total_steps: 3
   micro_batch_size: 1
   grad_accum_steps: 1
   clip_epsilon: 0.2
   lambda_max: 1.0
-  save_every: 0           # smoke: never evaluate mid-run
-  eval_every: 0           # smoke: never evaluate mid-run
+  save_every: 0
+  eval_every: 0
   log_every: 1
-  adv_eps: 1.0e-8
+  adv_eps: 1.0e-08
   pcgrad_eps: 1.0e-12
-  output_dir: "runs/kaggle_smoke/vacs"
-
+  output_dir: runs/kaggle_smoke/vacs
+  resume_from: null
+  lambda_schedule: cosine
+  use_pcgrad: true
+  correctness_gate: true
 rollout:
   k: 1
   max_prompt_length: 192
@@ -64,61 +47,57 @@
   temperature: 1.0
   top_p: 1.0
   do_sample: true
-
 reward:
-  task: "math"
+  task: math
   use_math_verify: true
-
 data:
-  train_dataset: "gsm8k"
-  train_split: "train"
-  train_subset_size: 8     # smoke: cap the dataset BEFORE training
-  eval_datasets: []        # smoke: no eval datasets
-  teacher_hint_template: >
-    Internal calibration note (do not reveal or reference this in your response):
-    the correct final answer to this problem is {answer}.
-    Reason step by step as usual and box your final answer.
+  train_dataset: gsm8k
+  train_split: train
+  train_subset_size: 8
+  eval_datasets: []
+  teacher_hint_template: 'Internal calibration note (do not reveal or reference this
+    in your response): the correct final answer to this problem is {answer}. Reason
+    step by step as usual and box your final answer.
 
+    '
 logging:
-  backend: "none"
-  run_name: "kaggle_smoke_vacs"
-
+  backend: none
+  run_name: kaggle_smoke_vacs
 checkpointing:
   save_adapter_only: true
-
 evaluation:
   eval_batch_size: 1
   eval_subset_size: null
   n_bootstrap: 100
   max_new_tokens: 256
-
-# ---- VACS-specific knobs (smoke defaults) ----
+  bootstrap_seed: 0
 vacs:
   token_credit_mix: 0.25
-  token_weight_epsilon: 0.20
+  token_weight_epsilon: 0.2
   sd_weight: 1.0
   soft_gate_slope: 12.0
   reward_threshold: 0.5
   adaptive_mix:
     enabled: true
     ema_decay: 0.95
-    multiplier_min: 0.10
+    multiplier_min: 0.1
     multiplier_max: 5.0
-
-# ---- CIBO v2 knobs (kept for compatibility; not used by VACS smoke) ----
+  use_soft_gate: true
+  use_asymmetric_projection: true
 cibo_v2:
   credit_lambda: 0.25
-  beta: 0.10
-  beta_mode: "fixed"
+  beta: 0.1
+  beta_mode: fixed
   beta_min: 0.001
   beta_max: 10.0
   beta_step_size: 0.05
   beta_ema_decay: 0.95
   beta_reference_warmup_steps: 20
-  beta_denominator_epsilon: 1.0e-6
-  ib_reduction: "sequence_sum"
+  beta_denominator_epsilon: 1.0e-06
+  ib_reduction: sequence_sum
   soft_gate_slope: 12.0
   reward_threshold: 0.5
   anchor_alpha: 0.01
   anchor_ema_decay: 0.995
-  anchor_storage: "same"
\ No newline at end of file
+  anchor_storage: same
+  use_soft_gate: true
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/baselines/grpo/training_loop.py /mnt/data/crisp_work/CRISP-main/crisp/baselines/grpo/training_loop.py
--- /mnt/data/crisp_original/CRISP-main/crisp/baselines/grpo/training_loop.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/baselines/grpo/training_loop.py	2026-08-02 12:44:31.267707150 +0000
@@ -1,39 +1,47 @@
-"""GRPO training entrypoint: `python train_launcher.py grpo --config ... --k 8`"""
+"""GRPO training entrypoint."""
 from __future__ import annotations
 
 import argparse
+import warnings
 from typing import Optional
 
-from crisp.baselines.grpo.training_step import grpo_step
+from crisp.baselines.grpo.training_step import collect_grpo_batch, score_grpo_batch
 from crisp.data.build_dataset import load_train_dataset
 from crisp.data.reward import build_reward_fn
 from crisp.eval.run_eval import make_eval_fn
-from crisp.model.load import attach_lora, load_model_and_tokenizer
+from crisp.model.load import attach_or_load_lora, load_model_and_tokenizer
 from crisp.training.common_loop import run_training_loop
 from crisp.utils.config import load_config
+from crisp.utils.distributed import init_distributed
 from crisp.utils.seeding import set_seed
 
 
 def main(config_path: str, k: int = 8, overrides: Optional[dict] = None) -> None:
     cfg = load_config(config_path, overrides)
+    init_distributed()
     set_seed(cfg.seed)
+    if k < 1:
+        raise ValueError("k must be at least 1")
+    if k == 1:
+        warnings.warn(
+            "k=1 has no non-trivial group-relative baseline; this run is a "
+            "single-rollout global-policy-gradient control, not genuine GRPO.",
+            stacklevel=2,
+        )
 
     model, tokenizer = load_model_and_tokenizer(cfg.model)
-    model, _is_peft = attach_lora(model, cfg.lora)
+    initial_adapter = cfg.model.get("initial_adapter") or cfg.training.get("resume_from")
+    model, _ = attach_or_load_lora(model, cfg.lora, initial_adapter=initial_adapter)
     params = [p for p in model.parameters() if p.requires_grad]
     if not params:
-        raise RuntimeError("No trainable parameters found -- check lora.enabled and model/load.py:attach_lora.")
+        raise RuntimeError("No trainable parameters found.")
 
-    # GRPO samples k rollouts per prompt, so the *number of prompts* per
-    # micro-batch is micro_batch_size / k, keeping the total rollout count
-    # (and therefore compute per step) comparable across k choices and
-    # comparable to CRISP's single-rollout-per-prompt budget when k=1.
-    if cfg.training.micro_batch_size % k != 0:
+    rollout_batch_size = int(cfg.training.micro_batch_size)
+    if rollout_batch_size % k != 0:
         raise ValueError(
-            f"training.micro_batch_size ({cfg.training.micro_batch_size}) must be divisible by "
-            f"k ({k}) so it splits evenly into whole prompt-groups."
+            f"training.micro_batch_size ({rollout_batch_size}) must be divisible by k ({k})."
         )
-    num_prompts_per_batch = cfg.training.micro_batch_size // k
+    cfg.training.micro_batch_size = rollout_batch_size // k
 
     train_dataset = load_train_dataset(
         cfg.data.train_dataset,
@@ -42,27 +50,30 @@
     )
     reward_fn = build_reward_fn(cfg.reward)
 
-    # `common_loop.batches` yields `training.micro_batch_size` *examples*
-    # per micro-batch; GRPO instead wants `num_prompts_per_batch` prompts
-    # (each expanded to k rollouts inside grpo_step). We get this by
-    # temporarily presenting `common_loop` with an effective batch size of
-    # `num_prompts_per_batch` via a config override, rather than
-    # reimplementing batching here.
-    cfg.training.micro_batch_size = num_prompts_per_batch
+    def collect_fn(model_, tokenizer_, prompts, answers, global_step):
+        return collect_grpo_batch(model_, tokenizer_, prompts, answers, reward_fn, cfg, k)
 
-    def step_fn(model, tokenizer, prompts, answers, global_step):
-        return grpo_step(model, tokenizer, prompts, answers, reward_fn, params, cfg, k=k)
+    def score_fn(model_, tokenizer_, collected, advantages, global_step):
+        return score_grpo_batch(model_, tokenizer_, collected, advantages, params, cfg, k)
 
-    eval_fn = make_eval_fn(cfg) if cfg.data.eval_datasets else None
+    default_name = "single_rollout_global_pg" if k == 1 else f"grpo_k{k}"
     run_training_loop(
-        model, tokenizer, params, train_dataset, step_fn, cfg,
-        run_name=cfg.logging.run_name or f"grpo_k{k}", eval_fn=eval_fn,
+        model=model,
+        tokenizer=tokenizer,
+        params=params,
+        train_dataset=train_dataset,
+        step_fn=None,
+        collect_fn=collect_fn,
+        score_fn=score_fn,
+        cfg=cfg,
+        run_name=cfg.logging.run_name or default_name,
+        eval_fn=make_eval_fn(cfg) if cfg.data.eval_datasets else None,
     )
 
 
 if __name__ == "__main__":
     parser = argparse.ArgumentParser()
     parser.add_argument("--config", required=True)
-    parser.add_argument("--k", type=int, default=8, help="rollouts sampled per prompt")
+    parser.add_argument("--k", type=int, default=8)
     args = parser.parse_args()
     main(args.config, k=args.k)
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/baselines/grpo/training_step.py /mnt/data/crisp_work/CRISP-main/crisp/baselines/grpo/training_step.py
--- /mnt/data/crisp_original/CRISP-main/crisp/baselines/grpo/training_step.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/baselines/grpo/training_step.py	2026-08-02 12:44:31.267707150 +0000
@@ -1,94 +1,120 @@
-"""
-GRPO baseline: critic-free, but the advantage is normalized *within* each
-prompt's group of k rollouts, rather than across the whole batch (contrast
-REINFORCE++'s single global normalization). README Section 5.3 lists
-"GRPO (k=1): same rollout budget as CRISP, no distillation" as the critical
-comparison point.
-
-k=1 needs a deliberate, explicitly-flagged design decision: a "group" of
-exactly one rollout has zero variance from its own mean by construction, so
-literally normalizing within it makes the advantage identically zero for
-every example -- not the "biased, high-variance" single-sample estimate the
-method is supposed to produce at k=1 (and the README's own Section 1.2
-trilemma table describes it that way: "biased... unstable", not "no signal
-at all"). So k=1 here falls back to batch-wide normalization instead of a
-degenerate zero, keeping it a meaningful, non-trivial baseline. This is
-called out explicitly rather than left as an implicit edge case, since
-"always exactly zero gradient" is exactly the kind of silent bug this whole
-reimplementation is trying to avoid.
+"""GRPO scoring with explicit group-relative advantages for k>=2.
+
+``k=1`` cannot define a non-trivial group-relative advantage. For backwards
+compatibility it is retained as a *single-rollout global-PG control* and uses
+the effective-batch advantages supplied by ``common_loop``. Paper tables
+should label it accordingly rather than calling it genuine GRPO.
 """
 from __future__ import annotations
 
 import torch
 
+from crisp.training.batch_types import CollectedBatch
 from crisp.training.grad_utils import compute_grads
 from crisp.training.logprobs import ScoredSequence, build_scoring_batch, sequence_log_prob
 from crisp.training.rollout import generate_rollouts
 
 
-def grpo_step(
-    model, tokenizer, prompts: list[str], answers: list[str], reward_fn,
-    params: list[torch.nn.Parameter], cfg, k: int,
-) -> tuple[list[torch.Tensor], dict]:
+def collect_grpo_batch(model, tokenizer, prompts, answers, reward_fn, cfg, k: int) -> CollectedBatch:
     model.eval()
-    sys_prompt = cfg.model.system_prompt
-    num_prompts = len(prompts)
-
     rollouts = generate_rollouts(
-        model, tokenizer, prompts, sys_prompt,
+        model,
+        tokenizer,
+        prompts,
+        cfg.model.system_prompt,
         max_prompt_length=cfg.rollout.max_prompt_length,
         max_new_tokens=cfg.rollout.max_new_tokens,
-        temperature=cfg.rollout.temperature, top_p=cfg.rollout.top_p,
-        do_sample=cfg.rollout.do_sample, num_return_sequences=k,
+        temperature=cfg.rollout.temperature,
+        top_p=cfg.rollout.top_p,
+        do_sample=cfg.rollout.do_sample,
+        num_return_sequences=k,
     )
-    # `rollouts` is ordered [prompt0-sample0..k-1, prompt1-sample0..k-1, ...]
-    # (repeat_interleave semantics -- see training/rollout.py).
-    expanded_answers = [a for a in answers for _ in range(k)]
-    rewards_flat = torch.tensor(
-        [reward_fn(r.response_text, a) for r, a in zip(rollouts, expanded_answers)], dtype=torch.float32
+    expanded_answers = [answer for answer in answers for _ in range(k)]
+    expanded_prompts = [prompt for prompt in prompts for _ in range(k)]
+    rewards = torch.tensor(
+        [reward_fn(r.response_text, a) for r, a in zip(rollouts, expanded_answers)],
+        dtype=torch.float32,
     )
-    rewards_grouped = rewards_flat.view(num_prompts, k)
+    batch = CollectedBatch(
+        expanded_prompts,
+        expanded_answers,
+        rollouts,
+        rewards,
+        metadata={
+            "k": int(k),
+            "num_prompts": len(prompts),
+            "generations": [
+                {"prompt": p, "response_text": r.response_text, "reward": float(reward)}
+                for p, r, reward in zip(expanded_prompts, rollouts, rewards.tolist())
+            ],
+        },
+    )
+    batch.validate()
+    return batch
+
 
+def score_grpo_batch(model, tokenizer, collected, effective_advantages, params, cfg, k: int):
+    valid = [i for i, r in enumerate(collected.rollouts) if len(r.response_ids) > 0]
+    if not valid:
+        raise RuntimeError("Every GRPO rollout was empty.")
+
+    # Group-relative normalization must preserve complete groups. An empty
+    # response is assigned a zero objective contribution rather than deleting
+    # one row and corrupting the [num_prompts, k] grouping.
+    rewards = collected.rewards
+    num_prompts = int(collected.metadata["num_prompts"])
+    if rewards.numel() != num_prompts * k:
+        raise ValueError("GRPO reward count is not num_prompts * k.")
     if k >= 2:
-        group_mean = rewards_grouped.mean(dim=1, keepdim=True)
-        group_std = rewards_grouped.std(dim=1, keepdim=True, unbiased=False)
-        advantages = ((rewards_grouped - group_mean) / (group_std + cfg.training.adv_eps)).reshape(-1)
+        grouped = rewards.view(num_prompts, k)
+        group_mean = grouped.mean(dim=1, keepdim=True)
+        group_std = grouped.std(dim=1, keepdim=True, unbiased=False)
+        advantages = ((grouped - group_mean) / (group_std + cfg.training.adv_eps)).reshape(-1)
     else:
-        # See module docstring: a group of size 1 has no internal variance to
-        # normalize against, so fall back to batch-wide normalization
-        # (structurally similar to REINFORCE++, but computed fresh from this
-        # batch alone) rather than a permanently-zero advantage.
-        flat = rewards_grouped.reshape(-1)
-        advantages = (flat - flat.mean()) / (flat.std(unbiased=False) + cfg.training.adv_eps)
+        advantages = effective_advantages
     advantages = advantages.to(model.device)
 
     seqs = [
-        ScoredSequence(ids=r.prompt_ids + r.response_ids, response_start=len(r.prompt_ids), response_len=len(r.response_ids))
-        for r in rollouts
+        ScoredSequence(
+            ids=r.prompt_ids + r.response_ids,
+            response_start=len(r.prompt_ids),
+            response_len=len(r.response_ids),
+        )
+        for r in collected.rollouts
     ]
-    pad_id = tokenizer.pad_token_id
-    input_ids, attention_mask = build_scoring_batch(seqs, pad_id)
-    input_ids = input_ids.to(model.device)
-    attention_mask = attention_mask.to(model.device)
-    token_log_probs, _ = sequence_log_prob(model, input_ids, attention_mask, seqs)
+    input_ids, attention_mask = build_scoring_batch(seqs, tokenizer.pad_token_id)
+    token_log_probs, _ = sequence_log_prob(
+        model,
+        input_ids.to(model.device),
+        attention_mask.to(model.device),
+        seqs,
+    )
 
-    eps_clip = cfg.training.clip_epsilon
     losses = []
-    for i in range(len(rollouts)):
-        new_lp = token_log_probs[i].mean()
-        old_lp = new_lp.detach()
-        ratio = torch.exp(new_lp - old_lp)
-        unclipped = ratio * advantages[i]
-        clipped = torch.clamp(ratio, 1 - eps_clip, 1 + eps_clip) * advantages[i]
-        losses.append(-torch.min(unclipped, clipped))
+    for advantage, logps in zip(advantages, token_log_probs):
+        if logps.numel() == 0:
+            # Keep the group structure intact but give an empty response no
+            # policy-gradient contribution.
+            losses.append(torch.zeros((), device=model.device, dtype=advantages.dtype))
+        else:
+            losses.append(-advantage * logps.mean())
     loss = torch.stack(losses).mean()
-
     grads = compute_grads(loss, params)
-    metrics = {
-        "reward_mean": rewards_flat.mean().item(),
-        "reward_std": rewards_flat.std(unbiased=False).item(),
-        "loss_rl": loss.item(),
+    return grads, {
+        "reward_mean": float(rewards.mean().item()),
+        "reward_std": float(rewards.std(unbiased=False).item()),
+        "loss_rl": float(loss.item()),
+        "rl_grad_norm": float(torch.cat([g.reshape(-1) for g in grads]).norm().item()),
         "k": float(k),
-        "response_len_mean": sum(len(r.response_ids) for r in rollouts) / len(rollouts),
+        "grpo_is_group_relative": float(k >= 2),
+        "response_len_mean": sum(len(r.response_ids) for r in collected.rollouts) / len(collected.rollouts),
+        "empty_response_fraction": 1.0 - len(valid) / max(len(collected.rollouts), 1),
+        "advantage_abs_mean": float(advantages.abs().mean().item()),
     }
-    return grads, metrics
+
+
+def grpo_step(model, tokenizer, prompts, answers, reward_fn, params, cfg, k: int):
+    collected = collect_grpo_batch(model, tokenizer, prompts, answers, reward_fn, cfg, k)
+    rewards = collected.rewards
+    effective = (rewards - rewards.mean()) / (rewards.std(unbiased=False) + cfg.training.adv_eps)
+    return score_grpo_batch(model, tokenizer, collected, effective, params, cfg, k)
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/baselines/opsd/training_loop.py /mnt/data/crisp_work/CRISP-main/crisp/baselines/opsd/training_loop.py
--- /mnt/data/crisp_original/CRISP-main/crisp/baselines/opsd/training_loop.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/baselines/opsd/training_loop.py	2026-08-02 12:48:09.955712873 +0000
@@ -1,4 +1,4 @@
-"""OPSD training entrypoint: `python train_launcher.py opsd --config ...`"""
+"""OPSD training entrypoint."""
 from __future__ import annotations
 
 import argparse
@@ -8,21 +8,24 @@
 from crisp.data.build_dataset import load_train_dataset
 from crisp.data.reward import build_reward_fn
 from crisp.eval.run_eval import make_eval_fn
-from crisp.model.load import attach_lora, load_model_and_tokenizer
+from crisp.model.load import attach_or_load_lora, load_model_and_tokenizer
 from crisp.training.common_loop import run_training_loop
 from crisp.utils.config import load_config
+from crisp.utils.distributed import init_distributed
 from crisp.utils.seeding import set_seed
 
 
 def main(config_path: str, overrides: Optional[dict] = None) -> None:
     cfg = load_config(config_path, overrides)
+    init_distributed()
     set_seed(cfg.seed)
 
     model, tokenizer = load_model_and_tokenizer(cfg.model)
-    model, _is_peft = attach_lora(model, cfg.lora)
+    initial_adapter = cfg.model.get("initial_adapter") or cfg.training.get("resume_from")
+    model, _ = attach_or_load_lora(model, cfg.lora, initial_adapter=initial_adapter)
     params = [p for p in model.parameters() if p.requires_grad]
     if not params:
-        raise RuntimeError("No trainable parameters found -- check lora.enabled and model/load.py:attach_lora.")
+        raise RuntimeError("No trainable parameters found.")
 
     train_dataset = load_train_dataset(
         cfg.data.train_dataset,
@@ -31,13 +34,18 @@
     )
     reward_fn = build_reward_fn(cfg.reward)
 
-    def step_fn(model, tokenizer, prompts, answers, global_step):
-        return opsd_step(model, tokenizer, prompts, answers, reward_fn, params, cfg)
+    def step_fn(model_, tokenizer_, prompts, answers, global_step):
+        return opsd_step(model_, tokenizer_, prompts, answers, reward_fn, params, cfg)
 
-    eval_fn = make_eval_fn(cfg) if cfg.data.eval_datasets else None
     run_training_loop(
-        model, tokenizer, params, train_dataset, step_fn, cfg,
-        run_name=cfg.logging.run_name or "opsd", eval_fn=eval_fn,
+        model=model,
+        tokenizer=tokenizer,
+        params=params,
+        train_dataset=train_dataset,
+        step_fn=step_fn,
+        cfg=cfg,
+        run_name=cfg.logging.run_name or "opsd",
+        eval_fn=make_eval_fn(cfg) if cfg.data.eval_datasets else None,
     )
 
 
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/baselines/reinforce_pp/training_loop.py /mnt/data/crisp_work/CRISP-main/crisp/baselines/reinforce_pp/training_loop.py
--- /mnt/data/crisp_original/CRISP-main/crisp/baselines/reinforce_pp/training_loop.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/baselines/reinforce_pp/training_loop.py	2026-08-02 12:44:03.379706420 +0000
@@ -1,28 +1,33 @@
-"""REINFORCE++ training entrypoint: `python train_launcher.py reinforce_pp --config ...`"""
+"""REINFORCE++ training entrypoint."""
 from __future__ import annotations
 
 import argparse
 from typing import Optional
 
-from crisp.baselines.reinforce_pp.training_step import reinforce_pp_step
+from crisp.baselines.reinforce_pp.training_step import (
+    collect_reinforce_pp_batch,
+    score_reinforce_pp_batch,
+)
 from crisp.data.build_dataset import load_train_dataset
 from crisp.data.reward import build_reward_fn
 from crisp.eval.run_eval import make_eval_fn
-from crisp.model.load import attach_lora, load_model_and_tokenizer
+from crisp.model.load import attach_or_load_lora, load_model_and_tokenizer
 from crisp.training.common_loop import run_training_loop
 from crisp.utils.config import load_config
+from crisp.utils.distributed import init_distributed
 from crisp.utils.seeding import set_seed
 
 
 def main(config_path: str, overrides: Optional[dict] = None) -> None:
     cfg = load_config(config_path, overrides)
+    init_distributed()
     set_seed(cfg.seed)
-
     model, tokenizer = load_model_and_tokenizer(cfg.model)
-    model, _is_peft = attach_lora(model, cfg.lora)
+    initial_adapter = cfg.model.get("initial_adapter") or cfg.training.get("resume_from")
+    model, _ = attach_or_load_lora(model, cfg.lora, initial_adapter=initial_adapter)
     params = [p for p in model.parameters() if p.requires_grad]
     if not params:
-        raise RuntimeError("No trainable parameters found -- check lora.enabled and model/load.py:attach_lora.")
+        raise RuntimeError("No trainable parameters found.")
 
     train_dataset = load_train_dataset(
         cfg.data.train_dataset,
@@ -31,13 +36,23 @@
     )
     reward_fn = build_reward_fn(cfg.reward)
 
-    def step_fn(model, tokenizer, prompts, answers, global_step):
-        return reinforce_pp_step(model, tokenizer, prompts, answers, reward_fn, params, cfg)
+    def collect_fn(model_, tokenizer_, prompts, answers, global_step):
+        return collect_reinforce_pp_batch(model_, tokenizer_, prompts, answers, reward_fn, cfg)
+
+    def score_fn(model_, tokenizer_, collected, advantages, global_step):
+        return score_reinforce_pp_batch(model_, tokenizer_, collected, advantages, params, cfg)
 
-    eval_fn = make_eval_fn(cfg) if cfg.data.eval_datasets else None
     run_training_loop(
-        model, tokenizer, params, train_dataset, step_fn, cfg,
-        run_name=cfg.logging.run_name or "reinforce_pp", eval_fn=eval_fn,
+        model=model,
+        tokenizer=tokenizer,
+        params=params,
+        train_dataset=train_dataset,
+        step_fn=None,
+        collect_fn=collect_fn,
+        score_fn=score_fn,
+        cfg=cfg,
+        run_name=cfg.logging.run_name or "reinforce_pp",
+        eval_fn=make_eval_fn(cfg) if cfg.data.eval_datasets else None,
     )
 
 
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/baselines/reinforce_pp/training_step.py /mnt/data/crisp_work/CRISP-main/crisp/baselines/reinforce_pp/training_step.py
--- /mnt/data/crisp_original/CRISP-main/crisp/baselines/reinforce_pp/training_step.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/baselines/reinforce_pp/training_step.py	2026-08-02 12:44:03.379706420 +0000
@@ -1,68 +1,95 @@
-"""
-REINFORCE++ baseline: critic-free policy gradient with a single global
-(whole-micro-batch) reward normalization, PPO-clipped. This is exactly
-CRISP's L_RL term in isolation -- i.e. CRISP evaluated at lambda=0 -- kept as
-a fully standalone implementation (rather than calling into crisp_step with
-lambda forced to 0) so it has zero dependency on the teacher/self-
-distillation machinery, matching what the ablation in the README (Section
-5.3: "Does joint training add anything beyond global advantage normalization
-alone?") actually wants to isolate.
-"""
+"""REINFORCE++-style global-effective-batch policy-gradient baseline."""
 from __future__ import annotations
 
 import torch
 
+from crisp.training.batch_types import CollectedBatch
 from crisp.training.grad_utils import compute_grads
 from crisp.training.logprobs import ScoredSequence, build_scoring_batch, sequence_log_prob
 from crisp.training.rollout import generate_rollouts
 
 
-def reinforce_pp_step(
-    model, tokenizer, prompts: list[str], answers: list[str], reward_fn,
-    params: list[torch.nn.Parameter], cfg,
-) -> tuple[list[torch.Tensor], dict]:
+def collect_reinforce_pp_batch(model, tokenizer, prompts, answers, reward_fn, cfg) -> CollectedBatch:
     model.eval()
-    sys_prompt = cfg.model.system_prompt
-    n = len(prompts)
-
     rollouts = generate_rollouts(
-        model, tokenizer, prompts, sys_prompt,
+        model,
+        tokenizer,
+        prompts,
+        cfg.model.system_prompt,
         max_prompt_length=cfg.rollout.max_prompt_length,
         max_new_tokens=cfg.rollout.max_new_tokens,
-        temperature=cfg.rollout.temperature, top_p=cfg.rollout.top_p,
-        do_sample=cfg.rollout.do_sample, num_return_sequences=1,
+        temperature=cfg.rollout.temperature,
+        top_p=cfg.rollout.top_p,
+        do_sample=cfg.rollout.do_sample,
+        num_return_sequences=1,
+    )
+    rewards = torch.tensor(
+        [reward_fn(r.response_text, a) for r, a in zip(rollouts, answers)],
+        dtype=torch.float32,
+    )
+    batch = CollectedBatch(
+        list(prompts),
+        list(answers),
+        rollouts,
+        rewards,
+        metadata={
+            "generations": [
+                {"prompt": p, "response_text": r.response_text, "reward": float(reward)}
+                for p, r, reward in zip(prompts, rollouts, rewards.tolist())
+            ]
+        },
     )
-    rewards = torch.tensor([reward_fn(r.response_text, a) for r, a in zip(rollouts, answers)], dtype=torch.float32)
+    batch.validate()
+    return batch
+
+
+def score_reinforce_pp_batch(model, tokenizer, collected, advantages, params, cfg):
+    valid = [i for i, r in enumerate(collected.rollouts) if len(r.response_ids) > 0]
+    if not valid:
+        raise RuntimeError("Every REINFORCE++ rollout was empty.")
+    rollouts = [collected.rollouts[i] for i in valid]
+    rewards = collected.rewards[valid]
+    advantages = advantages[valid].to(model.device)
 
     seqs = [
-        ScoredSequence(ids=r.prompt_ids + r.response_ids, response_start=len(r.prompt_ids), response_len=len(r.response_ids))
+        ScoredSequence(
+            ids=r.prompt_ids + r.response_ids,
+            response_start=len(r.prompt_ids),
+            response_len=len(r.response_ids),
+        )
         for r in rollouts
     ]
-    pad_id = tokenizer.pad_token_id
-    input_ids, attention_mask = build_scoring_batch(seqs, pad_id)
-    input_ids = input_ids.to(model.device)
-    attention_mask = attention_mask.to(model.device)
-    token_log_probs, _ = sequence_log_prob(model, input_ids, attention_mask, seqs)
-
-    adv_mean, adv_std = rewards.mean(), rewards.std(unbiased=False)
-    advantages = ((rewards - adv_mean) / (adv_std + cfg.training.adv_eps)).to(model.device)
-
-    eps_clip = cfg.training.clip_epsilon
-    losses = []
-    for i in range(n):
-        new_lp = token_log_probs[i].mean()
-        old_lp = new_lp.detach()
-        ratio = torch.exp(new_lp - old_lp)
-        unclipped = ratio * advantages[i]
-        clipped = torch.clamp(ratio, 1 - eps_clip, 1 + eps_clip) * advantages[i]
-        losses.append(-torch.min(unclipped, clipped))
-    loss = torch.stack(losses).mean()
+    input_ids, attention_mask = build_scoring_batch(seqs, tokenizer.pad_token_id)
+    token_log_probs, _ = sequence_log_prob(
+        model,
+        input_ids.to(model.device),
+        attention_mask.to(model.device),
+        seqs,
+    )
 
+    losses = [
+        -advantage * logps.mean()
+        for advantage, logps in zip(advantages, token_log_probs)
+        if logps.numel() > 0
+    ]
+    if not losses:
+        raise RuntimeError("No non-empty REINFORCE++ response remained after validation.")
+    loss = torch.stack(losses).mean()
     grads = compute_grads(loss, params)
-    metrics = {
-        "reward_mean": rewards.mean().item(),
-        "reward_std": rewards.std(unbiased=False).item(),
-        "loss_rl": loss.item(),
-        "response_len_mean": sum(len(r.response_ids) for r in rollouts) / n,
+    return grads, {
+        "reward_mean": float(rewards.mean().item()),
+        "reward_std": float(rewards.std(unbiased=False).item()),
+        "loss_rl": float(loss.item()),
+        "rl_grad_norm": float(torch.cat([g.reshape(-1) for g in grads]).norm().item()),
+        "response_len_mean": sum(len(r.response_ids) for r in rollouts) / len(rollouts),
+        "empty_response_fraction": 1.0 - len(valid) / max(len(collected.rollouts), 1),
+        "advantage_abs_mean": float(advantages.abs().mean().item()),
     }
-    return grads, metrics
+
+
+def reinforce_pp_step(model, tokenizer, prompts, answers, reward_fn, params, cfg):
+    """Backward-compatible microbatch wrapper."""
+    collected = collect_reinforce_pp_batch(model, tokenizer, prompts, answers, reward_fn, cfg)
+    rewards = collected.rewards
+    advantages = (rewards - rewards.mean()) / (rewards.std(unbiased=False) + cfg.training.adv_eps)
+    return score_reinforce_pp_batch(model, tokenizer, collected, advantages, params, cfg)
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/data/build_dataset.py /mnt/data/crisp_work/CRISP-main/crisp/data/build_dataset.py
--- /mnt/data/crisp_original/CRISP-main/crisp/data/build_dataset.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/data/build_dataset.py	2026-08-02 12:48:35.231713535 +0000
@@ -162,6 +162,37 @@
                            answer_transform=extract_last_boxed)
 
 
+
+def load_numina_sft(split: str = "train") -> list[dict]:
+    """Load full NuminaMath solutions for supervised fine-tuning."""
+    from datasets import load_dataset
+
+    ds = load_dataset("AI-MO/NuminaMath-CoT", split=split)
+    out = []
+    for row in ds:
+        problem = row.get("problem")
+        solution = row.get("solution")
+        if problem is None or solution is None:
+            continue
+        if not str(problem).strip() or not str(solution).strip():
+            continue
+        out.append({"prompt": str(problem), "completion": str(solution)})
+    if not out:
+        raise ValueError("numina_math_cot SFT normalization produced zero examples.")
+    return out
+
+
+def load_hmmt_2025(split: str = "train") -> list[dict]:
+    """Load the MathArena HMMT February 2025 evaluation set."""
+    dataset_id, ds = _try_load(["MathArena/hmmt_feb_2025"])
+    chosen_split = _pick_split(ds.keys(), [split, "train", "test"])
+    return normalize_rows(
+        list(ds[chosen_split]),
+        ["problem", "Problem", "question"],
+        ["answer", "Answer", "final_answer"],
+        f"hmmt2025 ({dataset_id})",
+    )
+
 def load_aime_2024(split: str = "train") -> list[dict]:
     dataset_id, ds = _try_load(["Maxwell-Jia/AIME_2024", "HuggingFaceH4/aime_2024"])
     chosen_split = _pick_split(ds.keys(), [split, "train", "test"])
@@ -213,6 +244,7 @@
     "math500": lambda: load_math500("test"),
     "aime2024": load_aime_2024,
     "aime2025": load_aime_2025,
+    "hmmt2025": load_hmmt_2025,
     "humaneval": lambda: load_humaneval("test"),
 }
 
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/eval/metrics.py /mnt/data/crisp_work/CRISP-main/crisp/eval/metrics.py
--- /mnt/data/crisp_original/CRISP-main/crisp/eval/metrics.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/eval/metrics.py	2026-08-02 12:49:55.259715629 +0000
@@ -42,11 +42,11 @@
     return total_tokens / num_correct
 
 
-def summarize(results: list[dict]) -> dict:
+def summarize(results: list[dict], n_bootstrap: int = 1000, bootstrap_seed: int = 0) -> dict:
     """`results`: list of `{"correct": bool, "response_tokens": int}`, one per problem."""
     if not results:
         return {
-            "pass@1_mean": 0.0, "pass@1_std": 0.0, "pass@1_ci_lo": 0.0, "pass@1_ci_hi": 0.0,
+            "pass@1_mean": 0.0, "item_correctness_std": 0.0, "pass@1_std": 0.0, "pass@1_ci_lo": 0.0, "pass@1_ci_hi": 0.0,
             "total_tokens": 0, "tokens_per_correct": float("inf"), "n": 0,
         }
     correct_flags = [r["correct"] for r in results]
@@ -54,9 +54,11 @@
     num_correct = sum(correct_flags)
     mean = pass_at_1(correct_flags)
     variance = sum((float(f) - mean) ** 2 for f in correct_flags) / len(correct_flags)
-    lo, hi = bootstrap_ci(correct_flags)
+    lo, hi = bootstrap_ci(correct_flags, n_bootstrap=n_bootstrap, seed=bootstrap_seed)
     return {
         "pass@1_mean": mean,
+        "item_correctness_std": variance ** 0.5,
+        # Deprecated compatibility alias; this is NOT cross-seed std.
         "pass@1_std": variance ** 0.5,
         "pass@1_ci_lo": lo,
         "pass@1_ci_hi": hi,
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/eval/run_eval.py /mnt/data/crisp_work/CRISP-main/crisp/eval/run_eval.py
--- /mnt/data/crisp_original/CRISP-main/crisp/eval/run_eval.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/eval/run_eval.py	2026-08-02 12:49:55.259715629 +0000
@@ -60,7 +60,11 @@
             else:
                 reward = reward_fn(rollout.response_text, ex["answer"])
             results.append({"correct": reward >= 1.0, "response_tokens": len(rollout.response_ids)})
-    return summarize(results)
+    return summarize(
+        results,
+        n_bootstrap=int(cfg.evaluation.n_bootstrap),
+        bootstrap_seed=int(cfg.evaluation.get("bootstrap_seed", cfg.seed)),
+    )
 
 
 def run_eval(cfg, model=None, tokenizer=None) -> dict:
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/model/load.py /mnt/data/crisp_work/CRISP-main/crisp/model/load.py
--- /mnt/data/crisp_original/CRISP-main/crisp/model/load.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/model/load.py	2026-08-02 12:54:59.363723587 +0000
@@ -136,7 +136,15 @@
     )
     if quant_config is not None:
         load_kwargs["quantization_config"] = quant_config
-        load_kwargs["device_map"] = "auto"
+        # In one-process-per-GPU distributed launches, device_map="auto"
+        # would let every process see and potentially occupy every GPU. Pin
+        # each quantized replica to its LOCAL_RANK instead.
+        import os
+        if int(os.environ.get("WORLD_SIZE", "1")) > 1:
+            local_rank = int(os.environ.get("LOCAL_RANK", "0"))
+            load_kwargs["device_map"] = {"": local_rank}
+        else:
+            load_kwargs["device_map"] = "auto"
     # else: placed explicitly on a single device below, so every tensor this
     # codebase builds by hand has one unambiguous target device (see
     # crisp/utils/device.py). We deliberately avoid device_map="auto" for the
@@ -202,3 +210,24 @@
     )
     model = get_peft_model(model, peft_config)
     return model, True
+
+
+def attach_or_load_lora(model, lora_cfg, initial_adapter: str | None = None):
+    """Attach a fresh LoRA adapter or resume/initialize from an existing one.
+
+    ``initial_adapter`` may point to an SFT adapter or a CRISP checkpoint.
+    The adapter is loaded as trainable, avoiding accidental stacking of a
+    second fresh adapter on top of the first.
+    """
+    if initial_adapter:
+        from peft import PeftModel, prepare_model_for_kbit_training
+
+        is_quantized = getattr(model, "is_loaded_in_4bit", False) or getattr(model, "is_loaded_in_8bit", False)
+        if is_quantized:
+            model = prepare_model_for_kbit_training(model, use_gradient_checkpointing=True)
+        else:
+            model.gradient_checkpointing_enable()
+            model.enable_input_require_grads()
+        model = PeftModel.from_pretrained(model, initial_adapter, is_trainable=True)
+        return model, True
+    return attach_lora(model, lora_cfg)
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/batch_types.py /mnt/data/crisp_work/CRISP-main/crisp/training/batch_types.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/batch_types.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/batch_types.py	2026-08-02 12:41:56.123703090 +0000
@@ -0,0 +1,44 @@
+"""Shared rollout-batch records for two-stage RL training.
+
+Stage A collects on-policy rollouts without retaining autograd graphs. Stage B
+scores those stored rollouts after reward statistics have been computed over
+the entire effective batch (all gradient-accumulation micro-batches and, when
+distributed training is enabled, all ranks).
+"""
+from __future__ import annotations
+
+from dataclasses import dataclass, field
+from typing import Any
+
+import torch
+
+
+@dataclass
+class CollectedBatch:
+    prompts: list[str]
+    answers: list[str]
+    rollouts: list[Any]
+    rewards: torch.Tensor
+    metadata: dict[str, Any] = field(default_factory=dict)
+
+    def validate(self) -> None:
+        if self.rewards.dim() != 1:
+            raise ValueError(f"rewards must be 1-D, got {tuple(self.rewards.shape)}")
+        if len(self.rollouts) != int(self.rewards.numel()):
+            raise ValueError(
+                f"rollout/reward count mismatch: {len(self.rollouts)} vs {self.rewards.numel()}"
+            )
+        if not torch.isfinite(self.rewards).all():
+            raise ValueError("Collected rewards contain non-finite values.")
+
+
+@dataclass(frozen=True)
+class AdvantageStats:
+    mean: float
+    std: float
+    count: int
+
+    def normalize(self, rewards: torch.Tensor, eps: float) -> torch.Tensor:
+        if not torch.isfinite(rewards).all():
+            raise ValueError("Cannot normalize non-finite rewards.")
+        return (rewards - float(self.mean)) / (float(self.std) + float(eps))
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/beta_controller.py /mnt/data/crisp_work/CRISP-main/crisp/training/beta_controller.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/beta_controller.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/beta_controller.py	2026-08-02 12:54:59.363723587 +0000
@@ -16,14 +16,13 @@
     small bounded multiplicative step based only on the SIGN of
     (ratio - 1):
 
-        baselined_kl   = kl_ema - reference_kl
-        ratio          = delta_reward_ema / (delta_baselined_kl + eps)
-        beta           = clip(beta * (1 + step_size * sign(ratio - 1)),
-                               beta_min, beta_max)
-
-    i.e. "is reward improving faster or slower than leakage is
-    changing, relative to a 1:1 trade-off" -- doc Section 7's own
-    framing. Reward is not optional here: a controller that reacts to
+        baselined_kl = kl_ema - reference_kl
+        signal        = delta_baselined_kl - delta_reward_ema
+        beta          = clip(beta * (1 + step_size * sign(signal)),
+                              beta_min, beta_max)
+
+    This increases the penalty when leakage grows faster than reward and
+    decreases it when reward improves faster than leakage. Reward is not optional here: a controller that reacts to
     KL alone doesn't implement this rule, it implements a different
     (pure KL-trend) one.
 
@@ -107,8 +106,11 @@
         self.prev_baselined_kl: Optional[float] = None
 
     def observe(self, reward: float, kl_value: float) -> None:
-        """Record one observation. Call BEFORE step() every micro-batch
-        (or at least once per optimizer step) to feed both EMAs.
+        """Record one effective-batch observation before ``step()``.
+
+        The CIBO trainer calls this exactly once per optimizer update after
+        aggregating micro-batch metrics, so controller dynamics do not depend
+        on ``grad_accum_steps``.
         """
         self.last_kl = float(kl_value)
         self.kl_ema = (
@@ -148,9 +150,15 @@
 
         delta_reward = self.reward_ema - self.prev_reward_ema
         delta_baselined_kl = baselined_kl - self.prev_baselined_kl
-        ratio = delta_reward / (delta_baselined_kl + self.beta_denominator_epsilon)
-        diff = ratio - 1.0
-        sign = 1.0 if diff > 0.0 else (-1.0 if diff < 0.0 else 0.0)
+
+        # Constraint-controller interpretation: increase beta when leakage
+        # grows faster than reward, decrease it when reward improves faster
+        # than leakage, and hold it when neither changes materially. This is
+        # sign-only and bounded, avoiding the unstable near-zero ratio used
+        # by the earlier implementation.
+        signal = delta_baselined_kl - delta_reward
+        tol = self.beta_denominator_epsilon
+        sign = 1.0 if signal > tol else (-1.0 if signal < -tol else 0.0)
 
         new_beta = self.beta * (1.0 + sign * self.beta_step_size)
         new_beta = max(self.beta_min, min(self.beta_max, new_beta))
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/cibo_v2_step.py /mnt/data/crisp_work/CRISP-main/crisp/training/cibo_v2_step.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/cibo_v2_step.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/cibo_v2_step.py	2026-08-02 12:46:44.027710625 +0000
@@ -1,43 +1,4 @@
-"""CIBO-CRISP v2 single-objective step.
-
-CIBO v2's defining property (vs CRISP and VACS) is the SINGLE composite
-loss:
-
-    loss_total = loss_rl + beta * loss_ib + alpha * loss_anchor
-
-with no PC-Grad, no symmetric projection, no RL/SD deconfliction.
-That single-loss structure is exactly what the CIBO v2 design doc
-prescribes: every parameter is updated by one gradient that already
-contains the IB-leakage penalty and the EMA-anchored regularization,
-with the two auxiliary weights (`beta`, `alpha`) treated as
-coefficients of one scalar.
-
-This module is the per-micro-batch equivalent of `crisp_step.py` /
-`vacs_step.py`. It does NOT call `optimizer.step` and does NOT mutate
-`param.grad`; the common training loop owns that. What it DOES:
-
-  1. Run one batched teacher+student forward over the same response
-     tokens.
-  2. Compute the CIBO token-level advantages via the tanh-credit
-     primitive (teacher side detached).
-  3. Compute the soft-gated forward-KL IB term.
-  4. If the EMA anchor is enabled, run a forward under the EMA
-     parameters (inside `ema_target_forward`, which restores the live
-     parameters in `finally`) and build the anchor cross-entropy.
-  5. Form `loss_total` and return it, plus `CiboComponents` for the
-     caller's metrics.
-
-The EMA anchor target has no autograd through the live parameters --
-that's what `ema_target_forward`'s `torch.no_grad()` block guarantees.
-The IB target's teacher side is detached by `cibo_token_advantages`,
-which is structurally the same isolation VACS uses.
-
-For the beta controller's timing invariant: this module does NOT call
-`beta_controller.step()`. The post_optimizer_step lifecycle hook in
-`crisp_v2_train` is what calls it -- once per optimizer step, AFTER
-the optimizer has already applied `loss_total`'s gradient. That
-mirrors VACS's "observe-then-commit" pattern.
-"""
+"""CIBO-CRISP v2 single-objective scoring."""
 from __future__ import annotations
 
 from dataclasses import dataclass, field
@@ -59,17 +20,6 @@
 
 @dataclass
 class CiboComponents:
-    """Intermediate state from one CIBO micro-batch.
-
-    `loss_total` is the single scalar loss fed to `compute_grads`
-    exactly once. `loss_rl`, `loss_ib`, `loss_anchor` are the named
-    pieces so the metrics block can report them separately.
-
-    `token_adv` is the CIBO token-weighted advantage (with tanh credit).
-    `beta` is the current beta value read from the controller at the
-    moment of this step (NOT updated here; that's the hook's job).
-    """
-
     loss_total: torch.Tensor
     loss_rl: torch.Tensor
     loss_ib: torch.Tensor
@@ -80,12 +30,18 @@
     metrics: dict = field(default_factory=dict)
 
 
-def _stack_per_row(rows):
-    """Stack per-row 1-D tensors into a [N, max_len] tensor, right-padded with zeros."""
-    max_len = max(r.shape[0] for r in rows)
-    out = torch.zeros(len(rows), max_len, device=rows[0].device)
-    for i, r in enumerate(rows):
-        out[i, :r.shape[0]] = r
+def _stack_per_row(rows: list[torch.Tensor]) -> torch.Tensor:
+    if not rows:
+        raise ValueError("Cannot stack an empty row list.")
+    max_len = max(int(row.shape[0]) for row in rows)
+    out = torch.zeros(
+        len(rows),
+        max_len,
+        device=rows[0].device,
+        dtype=rows[0].dtype,
+    )
+    for i, row in enumerate(rows):
+        out[i, : row.shape[0]] = row
     return out
 
 
@@ -104,115 +60,111 @@
     reward_threshold: float,
     anchor_alpha: float,
     use_anchor: bool,
+    advantages: torch.Tensor | None = None,
+    ib_reduction: str = "sequence_sum",
+    use_soft_gate: bool = True,
 ) -> CiboComponents:
-    """Compute the CIBO v2 single scalar loss + component block for one micro-batch.
-
-    Returns `CiboComponents` so the caller (common loop) can compute
-    ONE gradient (`compute_grads(comp.loss_total, params)`) and emit
-    the per-component metrics without re-running anything.
-
-    No `optimizer.step`, no `.grad` mutation, no `beta_controller.step()`
-    -- the post_optimizer_step hook owns those.
-    """
-    # ---- 1. EMA-anchored target forward (if enabled).
-    # Run BEFORE the live forward so the EMA context manager's restore
-    # in `finally` cannot race with the live forward's autograd graph.
+    """Build one scalar CIBO objective and differentiate it exactly once."""
     if use_anchor and anchor_alpha > 0.0:
         with ema_target_forward(ema, params):
-            anchor_scored = score_teacher_student(
+            ema_scored = score_teacher_student(
                 model=model,
                 teacher_sequences=teacher_sequences,
                 student_sequences=student_sequences,
                 pad_id=pad_id,
             )
-        # The EMA context has restored live params. Use the EMA scored
-        # output for the anchor target.
-        anchor_token_logps = anchor_scored.student_token_logps
+        ema_student_row_log_probs = [
+            logp.detach() for logp in ema_scored.student_row_log_probs
+        ]
     else:
-        anchor_token_logps = None
+        ema_student_row_log_probs = None
 
-    # ---- 2. Live teacher+student forward (one batched call).
     scored = score_teacher_student(
         model=model,
         teacher_sequences=teacher_sequences,
         student_sequences=student_sequences,
         pad_id=pad_id,
     )
-    teacher_token_logps_2d = _stack_per_row(scored.teacher_token_logps)
-    student_token_logps_2d = _stack_per_row(scored.student_token_logps)
+    teacher_token_logps = _stack_per_row(scored.teacher_token_logps)
+    student_token_logps = _stack_per_row(scored.student_token_logps)
 
-    # ---- 3. RL term: CIBO token-level advantages + plain per-token REINFORCE.
-    advantages = normalize_advantages(rewards, eps=1e-8).to(model.device)
+    if advantages is None:
+        advantages = normalize_advantages(rewards, eps=1e-8)
+    advantages = advantages.to(model.device)
     token_adv = cibo_token_advantages(
         advantages,
-        teacher_token_logps_2d,
-        student_token_logps_2d,
+        teacher_token_logps,
+        student_token_logps,
         credit_lambda=credit_lambda,
     )
-    # `_stack_per_row` right-pads variable-length rows to a common width
-    # with zeros; mask them out before the per-sequence 1/T mean, or
-    # shorter rollouts get diluted by the padding in the denominator.
-    response_lens = [r.shape[0] for r in scored.student_token_logps]
-    max_len = student_token_logps_2d.shape[1]
-    valid_mask = torch.zeros(len(response_lens), max_len, device=student_token_logps_2d.device)
+
+    response_lens = [int(row.shape[0]) for row in scored.student_token_logps]
+    max_len = student_token_logps.shape[1]
+    valid_mask = torch.zeros(
+        len(response_lens), max_len, device=student_token_logps.device
+    )
     for i, length in enumerate(response_lens):
         valid_mask[i, :length] = 1.0
-    # doc Section 5.1.1's boxed RL loss: -(1/T) sum_t a_tilde_t * log
-    # pi_theta(y_t|...), batch-averaged -- plain REINFORCE, credit
-    # already embedded in token_adv, no PPO ratio/clip in the formula.
-    per_token_rl = token_adv * student_token_logps_2d * valid_mask
-    per_example_rl_loss = -(per_token_rl.sum(dim=1) / valid_mask.sum(dim=1).clamp(min=1.0))
-    loss_rl = per_example_rl_loss.mean()
+    if not bool(valid_mask.any().item()):
+        raise RuntimeError("CIBO received no response tokens to score.")
 
-    # ---- 4. IB term: soft-gated forward-KL with sequence_sum reduction.
-    gate = soft_reward_gate(rewards, slope=soft_gate_slope, threshold=reward_threshold)
+    per_token_rl = token_adv * student_token_logps * valid_mask
+    per_example_rl = -(
+        per_token_rl.sum(dim=1) / valid_mask.sum(dim=1).clamp(min=1.0)
+    )
+    loss_rl = per_example_rl.mean()
+
+    if use_soft_gate:
+        gate = soft_reward_gate(
+            rewards,
+            slope=soft_gate_slope,
+            threshold=reward_threshold,
+        )
+    else:
+        gate = torch.ones_like(rewards)
     loss_ib = reduce_gated_kl(
-        scored.teacher_per_token_kl, gate, reduction="sequence_sum"
+        scored.teacher_per_token_kl,
+        gate,
+        reduction=ib_reduction,
     )
 
-    # ---- 5. Anchor term: cross-entropy between live student and
-    # EMA-anchored student. Computed only if both use_anchor is True
-    # AND anchor_alpha is positive (the beta_controller-style flag
-    # toggle).
-    if use_anchor and anchor_alpha > 0.0 and anchor_token_logps is not None:
-        # Anchor cross-entropy: mean over response tokens of
-        # -EMA_target_logp(student_response_tokens). The EMA target
-        # logps come from inside the EMA context (no grad through live
-        # params). The student_response_ids are read from the
-        # student_sequences.
-        anchor_ce_terms = []
-        for i, seq in enumerate(student_sequences):
-            ids = torch.tensor(
-                seq.ids[seq.response_start: seq.response_start + seq.response_len],
-                device=model.device,
-            )
-            target_logp = anchor_token_logps[i].to(model.device)
-            if ids.numel() == 0:
+    # Full-distribution cross entropy H(p_EMA, p_live). The EMA side is a
+    # detached target; the live student side remains differentiable.
+    anchor_terms: list[torch.Tensor] = []
+    if ema_student_row_log_probs is not None:
+        for ema_logp, live_logp in zip(
+            ema_student_row_log_probs,
+            scored.student_row_log_probs,
+        ):
+            if ema_logp.shape != live_logp.shape:
+                raise ValueError(
+                    f"EMA/live response distributions differ: {ema_logp.shape} vs {live_logp.shape}"
+                )
+            if ema_logp.shape[0] == 0:
                 continue
-            # anchor_token_logps[i] is already the per-row tensor of gathered
-            # log-probs of the actual response token ids (computed by
-            # score_teacher_student -> gather_token_log_probs). So the
-            # anchor NLL for this rollout is just -mean(target_logp) --
-            # we do NOT gather again (target_logp is not a full vocab
-            # distribution, it is the gathered response-token log-prob).
-            nll = -target_logp.mean()
-            anchor_ce_terms.append(nll)
-        if anchor_ce_terms:
-            loss_anchor = torch.stack(anchor_ce_terms).mean()
-        else:
-            loss_anchor = torch.zeros((), device=model.device)
-    else:
-        loss_anchor = torch.zeros((), device=model.device)
+            ema_probs = ema_logp.exp()
+            anchor_terms.append(-(ema_probs * live_logp).sum(dim=-1).mean())
+    loss_anchor = (
+        torch.stack(anchor_terms).mean()
+        if anchor_terms
+        else torch.zeros((), device=model.device)
+    )
 
-    # ---- 6. Single composite loss.
     beta = float(beta_controller.beta)
     loss_total = loss_rl + beta * loss_ib + float(anchor_alpha) * loss_anchor
-
-    # ---- 7. Compute the single gradient ONCE. Caller (the common
-    # loop) is responsible for assigning this to `param.grad`.
     gradients = compute_grads(loss_total, params, retain_graph=False)
 
-    # ---- 8. Metrics. Names follow the plan exactly.
+    nonempty_kl = [row.mean() for row in scored.teacher_per_token_kl if row.numel() > 0]
+    kl_mean = (
+        float(torch.stack(nonempty_kl).mean().item()) if nonempty_kl else 0.0
+    )
+    credit_multiplier = (
+        1.0
+        + credit_lambda
+        * torch.tanh(
+            teacher_token_logps.detach() - student_token_logps.detach()
+        )
+    )
     metrics = {
         "cibo/loss_total": float(loss_total.item()),
         "cibo/loss_rl": float(loss_rl.item()),
@@ -227,16 +179,13 @@
             else 0.0
         ),
         "cibo/credit_multiplier_mean": float(
-            (1.0 + credit_lambda
-             * torch.tanh(teacher_token_logps_2d.detach()
-                          - student_token_logps_2d.detach()))[valid_mask.bool()].mean().item()
+            credit_multiplier[valid_mask.bool()].mean().item()
         ),
         "cibo/ib_gate_mean": float(gate.mean().item()),
-        "cibo/kl_per_token_mean": float(
-            torch.stack([k.mean() for k in scored.teacher_per_token_kl]).mean().item()
-        ) if scored.teacher_per_token_kl else 0.0,
+        "cibo/kl_per_token_mean": kl_mean,
         "cibo/anchor_alpha": float(anchor_alpha),
         "cibo/ema_decay": float(ema.decay),
+        "cibo/ib_reduction_token_mean": float(ib_reduction == "token_mean"),
     }
 
     return CiboComponents(
@@ -266,8 +215,10 @@
     reward_threshold: float,
     anchor_alpha: float,
     use_anchor: bool,
+    advantages: torch.Tensor | None = None,
+    ib_reduction: str = "sequence_sum",
+    use_soft_gate: bool = True,
 ):
-    """End-to-end CIBO v2 micro-batch: returns (gradients, metrics)."""
     comp = build_cibo_components(
         model=model,
         teacher_sequences=teacher_sequences,
@@ -282,5 +233,8 @@
         reward_threshold=reward_threshold,
         anchor_alpha=anchor_alpha,
         use_anchor=use_anchor,
+        advantages=advantages,
+        ib_reduction=ib_reduction,
+        use_soft_gate=use_soft_gate,
     )
-    return comp.gradients, comp.metrics
\ No newline at end of file
+    return comp.gradients, comp.metrics
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/cibo_v2_train.py /mnt/data/crisp_work/CRISP-main/crisp/training/cibo_v2_train.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/cibo_v2_train.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/cibo_v2_train.py	2026-08-02 12:46:44.027710625 +0000
@@ -1,24 +1,4 @@
-"""CIBO-CRISP v2 training entry point.
-
-`python train_launcher.py cibo-v2 --config config/cibo_v2_7b.yaml`
-
-Same pattern as `vacs_train.py`: reuse the shared common_loop.py,
-delegate per-micro-batch math to `cibo_v2_step.py`, and use the
-post_optimizer_step lifecycle hook to advance the beta controller
-+ EMA adapter ONCE per optimizer step (not per micro-batch).
-
-The plan calls out three scheduler/post-step facts the hook is
-responsible for:
-
-  - Beta observations must be staged DURING the step, but the
-    controller's step() must NOT run during the step itself -- it
-    runs in the post-step hook, which is when the just-applied
-    gradient signal is committed to the controller's reference EMA.
-  - EMA adapter `update()` must run in the post-step hook, after
-    `optimizer.step()`, so the EMA tracks the just-stepped parameters.
-  - Save/load via `checkpoint_state()` must round-trip both the
-    EMA adapter state and the beta controller state.
-"""
+"""CIBO-CRISP v2 training entry point."""
 from __future__ import annotations
 
 import argparse
@@ -29,118 +9,122 @@
 from crisp.data.build_dataset import load_train_dataset
 from crisp.data.reward import build_reward_fn
 from crisp.eval.run_eval import make_eval_fn
-from crisp.model.load import attach_lora, load_model_and_tokenizer
+from crisp.model.load import attach_or_load_lora, load_model_and_tokenizer
+from crisp.training.batch_types import CollectedBatch
 from crisp.training.beta_controller import BetaController
 from crisp.training.cibo_v2_step import cibo_v2_step_from_arrays
 from crisp.training.common_loop import run_training_loop
-from crisp.training.crisp_step import build_teacher_student_sequences
 from crisp.training.ema_adapter import EmaAdapterState
 from crisp.training.rollout import generate_rollouts
+from crisp.training.teacher_student import build_teacher_student_sequences
 from crisp.utils.config import load_config
+from crisp.utils.distributed import all_reduce_scalar, init_distributed
 from crisp.utils.seeding import set_seed
 
 
-def cibo_v2_step_fn_factory(model, tokenizer, params, cfg, ema, ctrl):
-    """Build a `step_fn` for the CIBO v2 method.
-
-    Closure-captures model, tokenizer, params, config, EMA adapter
-    state, and beta controller so each call from the common loop can
-    produce rollouts + scores + the single scalar loss without
-    re-reading them.
-    """
-    sys_prompt = cfg.model.system_prompt
-
-    def step_fn(model_, tokenizer_, prompts, answers, global_step):
-        model_.eval()
-        n = len(prompts)
-
-        # 1. On-policy rollouts from the STUDENT context.
-        rollouts = generate_rollouts(
-            model_, tokenizer_, prompts, sys_prompt,
-            max_prompt_length=cfg.rollout.max_prompt_length,
-            max_new_tokens=cfg.rollout.max_new_tokens,
-            temperature=cfg.rollout.temperature, top_p=cfg.rollout.top_p,
-            do_sample=cfg.rollout.do_sample, num_return_sequences=1,
-        )
+def collect_cibo_batch(model, tokenizer, prompts, answers, reward_fn, cfg) -> CollectedBatch:
+    model.eval()
+    rollouts = generate_rollouts(
+        model,
+        tokenizer,
+        prompts,
+        cfg.model.system_prompt,
+        max_prompt_length=cfg.rollout.max_prompt_length,
+        max_new_tokens=cfg.rollout.max_new_tokens,
+        temperature=cfg.rollout.temperature,
+        top_p=cfg.rollout.top_p,
+        do_sample=cfg.rollout.do_sample,
+        num_return_sequences=1,
+    )
+    rewards = torch.tensor(
+        [reward_fn(r.response_text, answer) for r, answer in zip(rollouts, answers)],
+        dtype=torch.float32,
+    )
+    batch = CollectedBatch(
+        list(prompts),
+        list(answers),
+        rollouts,
+        rewards,
+        metadata={
+            "generations": [
+                {"prompt": p, "response_text": r.response_text, "reward": float(reward)}
+                for p, r, reward in zip(prompts, rollouts, rewards.tolist())
+            ]
+        },
+    )
+    batch.validate()
+    return batch
 
-        # 2. Verifiable reward per rollout.
-        reward_fn = build_reward_fn(cfg.reward)
-        rewards_tensor = torch.tensor(
-            [reward_fn(r.response_text, a) for r, a in zip(rollouts, answers)],
-            dtype=torch.float32,
-        )
 
-        # 3. Build paired teacher+student sequences.
+def cibo_score_fn_factory(tokenizer, params, cfg, ema, controller):
+    def score_fn(model, tokenizer_, collected, advantages, global_step):
+        valid = [i for i, rollout in enumerate(collected.rollouts) if len(rollout.response_ids) > 0]
+        if not valid:
+            raise RuntimeError("Every CIBO rollout was empty.")
+        prompts = [collected.prompts[i] for i in valid]
+        answers = [collected.answers[i] for i in valid]
+        rollouts = [collected.rollouts[i] for i in valid]
+        rewards = collected.rewards[valid]
+        advantages_valid = advantages[valid]
         teacher_seqs, student_seqs = build_teacher_student_sequences(
-            prompts, answers, rollouts, tokenizer=tokenizer_,
-            sys_prompt=sys_prompt, hint_template=cfg.data.teacher_hint_template,
+            prompts,
+            answers,
+            rollouts,
+            tokenizer=tokenizer_,
+            sys_prompt=cfg.model.system_prompt,
+            hint_template=cfg.data.teacher_hint_template,
         )
-
-        # 4. Run the CIBO step end-to-end.
         gradients, metrics = cibo_v2_step_from_arrays(
-            model=model_,
+            model=model,
             teacher_sequences=teacher_seqs,
             student_sequences=student_seqs,
             pad_id=tokenizer_.pad_token_id,
             params=params,
-            rewards=rewards_tensor,
+            rewards=rewards,
             ema=ema,
-            beta_controller=ctrl,
+            beta_controller=controller,
             credit_lambda=cfg.cibo_v2.credit_lambda,
             soft_gate_slope=cfg.cibo_v2.soft_gate_slope,
             reward_threshold=cfg.cibo_v2.reward_threshold,
             anchor_alpha=cfg.cibo_v2.anchor_alpha,
             use_anchor=cfg.cibo_v2.anchor_alpha > 0.0,
-        )
-        # 5. Stage beta observation for the post-step controller. We use
-        # the IB loss value as a KL scalar proxy for the adaptive
-        # controller's KL-side EMA, and the batch's mean reward for its
-        # reward-side EMA (see beta_controller.py -- the adaptive rule
-        # is a ratio of reward-improvement to leakage-change, not a
-        # KL-only signal, so a real reward is required here).
-        ib_value = float(metrics["cibo/loss_ib"])
-        ctrl.observe(reward=float(rewards_tensor.mean().item()), kl_value=ib_value)
-        # 6. Step-level metrics include reward and response-length stats.
-        step_metrics = dict(metrics)
-        step_metrics["reward_mean"] = float(rewards_tensor.mean().item())
-        step_metrics["reward_std"] = float(rewards_tensor.std(unbiased=False).item())
-        step_metrics["response_len_mean"] = (
-            sum(len(r.response_ids) for r in rollouts) / max(n, 1)
-        )
-        step_metrics["_generations"] = [
-            {"prompt": p, "response_text": r.response_text, "reward": float(rw)}
-            for p, r, rw in zip(prompts, rollouts, rewards_tensor.tolist())
-        ]
-        return gradients, step_metrics
+            advantages=advantages_valid,
+            ib_reduction=cfg.cibo_v2.ib_reduction,
+            use_soft_gate=bool(cfg.cibo_v2.get("use_soft_gate", True)),
+        )
+        metrics = dict(metrics)
+        metrics.update({
+            "reward_mean": float(rewards.mean().item()),
+            "reward_std": float(rewards.std(unbiased=False).item()),
+            "response_len_mean": sum(len(r.response_ids) for r in rollouts) / len(rollouts),
+            "empty_response_fraction": 1.0 - len(valid) / max(len(collected.rollouts), 1),
+            "advantage_abs_mean": float(advantages_valid.abs().mean().item()),
+        })
+        return gradients, metrics
 
-    return step_fn
+    return score_fn
 
 
 def main(config_path: str, overrides: Optional[dict] = None) -> None:
     cfg = load_config(config_path, overrides)
+    init_distributed()
     set_seed(cfg.seed)
 
     model, tokenizer = load_model_and_tokenizer(cfg.model)
-    model, _is_peft = attach_lora(model, cfg.lora)
+    initial_adapter = cfg.model.get("initial_adapter") or cfg.training.get("resume_from")
+    model, _ = attach_or_load_lora(model, cfg.lora, initial_adapter=initial_adapter)
     params = [p for p in model.parameters() if p.requires_grad]
     if not params:
-        raise RuntimeError(
-            "No trainable parameters found. If lora.enabled is false this does a full "
-            "finetune and every parameter should already require grad; if lora.enabled is "
-            "true, check that attach_lora actually ran (see crisp/model/load.py)."
-        )
+        raise RuntimeError("No trainable parameters found.")
 
     train_dataset = load_train_dataset(
         cfg.data.train_dataset,
         split=cfg.data.train_split,
         train_subset_size=cfg.data.train_subset_size,
     )
-    eval_fn = make_eval_fn(cfg) if cfg.data.eval_datasets else None
-
-    # EMA adapter anchor (Task 7).
+    reward_fn = build_reward_fn(cfg.reward)
     ema = EmaAdapterState(params, decay=cfg.cibo_v2.anchor_ema_decay)
-    # Beta controller (Task 7).
-    ctrl = BetaController(
+    controller = BetaController(
         beta=cfg.cibo_v2.beta,
         beta_mode=cfg.cibo_v2.beta_mode,
         beta_min=cfg.cibo_v2.beta_min,
@@ -150,43 +134,56 @@
         beta_reference_warmup_steps=cfg.cibo_v2.beta_reference_warmup_steps,
         beta_denominator_epsilon=cfg.cibo_v2.beta_denominator_epsilon,
     )
-    step_fn = cibo_v2_step_fn_factory(model, tokenizer, params, cfg, ema, ctrl)
 
-    def post_optimizer_step(model_, aggregated_step_metrics):
-        # The CIBO step's step_fn already called ctrl.observe().
-        # Here we commit the controller and advance the EMA adapter --
-        # both happen ONCE per optimizer step, NEVER per micro-batch.
-        ctrl.step()
+    def collect_fn(model_, tokenizer_, prompts, answers, global_step):
+        return collect_cibo_batch(model_, tokenizer_, prompts, answers, reward_fn, cfg)
+
+    score_fn = cibo_score_fn_factory(tokenizer, params, cfg, ema, controller)
+
+    def post_optimizer_step(model_, aggregated):
+        # One globally averaged observation per optimizer update; controller
+        # behavior no longer changes when grad_accum_steps changes.
+        reward = all_reduce_scalar(aggregated.get("reward_mean", 0.0), model_.device)
+        kl_value = all_reduce_scalar(aggregated.get("cibo/loss_ib", 0.0), model_.device)
+        controller.observe(reward=reward, kl_value=kl_value)
+        controller.step()
         ema.update()
 
     def checkpoint_state() -> dict:
         return {
             "cibo_v2_ema": ema.state_dict(),
-            "cibo_v2_beta_controller": ctrl.state_dict(),
+            "cibo_v2_beta_controller": controller.state_dict(),
         }
 
+    def restore_method_state(payload: dict) -> None:
+        if not payload:
+            return
+        if "cibo_v2_ema" in payload:
+            ema.load_state_dict(payload["cibo_v2_ema"])
+        if "cibo_v2_beta_controller" in payload:
+            controller.load_state_dict(payload["cibo_v2_beta_controller"])
+
     run_training_loop(
         model=model,
         tokenizer=tokenizer,
         params=params,
         train_dataset=train_dataset,
-        step_fn=step_fn,
+        step_fn=None,
+        collect_fn=collect_fn,
+        score_fn=score_fn,
         cfg=cfg,
         run_name=cfg.logging.run_name or "cibo-v2",
-        eval_fn=eval_fn,
+        eval_fn=make_eval_fn(cfg) if cfg.data.eval_datasets else None,
         post_optimizer_step=post_optimizer_step,
         checkpoint_state=checkpoint_state,
+        restore_method_state=restore_method_state,
     )
 
 
 if __name__ == "__main__":
     parser = argparse.ArgumentParser()
     parser.add_argument("--config", required=True)
-    parser.add_argument(
-        "--override", action="append", default=[],
-        metavar="key.path=value",
-        help="override a config value; repeatable",
-    )
+    parser.add_argument("--override", action="append", default=[])
     args = parser.parse_args()
     from train_launcher import parse_overrides
     main(args.config, overrides=parse_overrides(args.override))
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/common_loop.py /mnt/data/crisp_work/CRISP-main/crisp/training/common_loop.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/common_loop.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/common_loop.py	2026-08-02 12:54:59.363723587 +0000
@@ -1,34 +1,91 @@
-"""
-Generic outer training loop shared by CRISP and every baseline.
+"""Shared training loop for CRISP, baselines, VACS, and CIBO.
+
+Two execution contracts are supported:
 
-Each method supplies a `step_fn(model, tokenizer, prompts, answers,
-global_step) -> (grads, metrics)` closure, where `grads` is a `list[Tensor]`
-already aligned with the fixed `params` list passed into `run_training_loop`
-(see training/grad_utils.py and training/pcgrad.py -- both CRISP and the
-baselines compute their gradients via `torch.autograd.grad` rather than
-`.backward()`, precisely so this loop has one uniform interface instead of
-branching on "is this a PC-Grad method or a plain-loss method").
-
-This loop owns everything that's identical across methods: batching from the
-dataset, gradient accumulation, optimizer/scheduler stepping, grad-norm
-clipping, periodic eval, checkpointing, and logging. Keeping this in one
-place means a fix here (e.g. to how checkpoints are written, or how grad
-accumulation is averaged) applies to CRISP and all three baselines at once,
-instead of being copy-pasted into four separate loops and inevitably
-drifting out of sync.
+1. Legacy ``step_fn``: one micro-batch directly returns gradients + metrics.
+2. Two-stage ``collect_fn``/``score_fn``: collect all rollouts for the
+   effective batch first, compute one reward mean/std over all accumulated
+   micro-batches (and all distributed ranks), then score each stored
+   micro-batch with those shared statistics.
+
+The second contract is the paper-correct path for REINFORCE++-style global
+advantage normalization. Collection never retains autograd graphs, so it does
+not increase scoring-graph memory with ``grad_accum_steps``.
 """
 from __future__ import annotations
 
 import json
 import random
 from pathlib import Path
-from typing import Callable, Iterable, Optional
+from typing import Callable, Optional
 
 import torch
 
+from crisp.training.batch_types import AdvantageStats, CollectedBatch
+from crisp.utils.distributed import (
+    all_reduce_gradients,
+    barrier,
+    gather_1d_equal,
+    is_main_process,
+    rank,
+    world_size,
+)
 from crisp.utils.logging_utils import RunLogger
 
 
+class StatefulBatchStream:
+    """Deterministic, resumable stream of shuffled fixed-size batches.
+
+    In distributed mode each rank owns a disjoint strided shard. All ranks
+    must use the same batch size so reward all-gather lengths match.
+    """
+
+    def __init__(self, dataset: list[dict], batch_size: int, seed: int):
+        self.batch_size = int(batch_size)
+        self.rng = random.Random(int(seed))
+        self.indices = list(range(len(dataset)))[rank()::world_size()]
+        self.dataset = dataset
+        self.position = len(self.indices)  # force a shuffle before first use
+        if len(self.indices) < self.batch_size:
+            raise ValueError(
+                f"Rank {rank()} has only {len(self.indices)} examples after distributed "
+                f"sharding, smaller than micro_batch_size={self.batch_size}."
+            )
+
+    def _reshuffle(self) -> None:
+        self.rng.shuffle(self.indices)
+        self.position = 0
+
+    def next_batch(self) -> list[dict]:
+        if self.position + self.batch_size > len(self.indices):
+            self._reshuffle()
+        selected = self.indices[self.position:self.position + self.batch_size]
+        self.position += self.batch_size
+        return [self.dataset[i] for i in selected]
+
+    def state_dict(self) -> dict:
+        return {
+            "indices": list(self.indices),
+            "position": int(self.position),
+            "rng_state": self.rng.getstate(),
+            "batch_size": self.batch_size,
+        }
+
+    def load_state_dict(self, state: dict) -> None:
+        if int(state.get("batch_size", self.batch_size)) != self.batch_size:
+            raise ValueError("Cannot resume with a different micro_batch_size.")
+        self.indices = list(state["indices"])
+        self.position = int(state["position"])
+        self.rng.setstate(state["rng_state"])
+
+
+def batches(dataset: list[dict], batch_size: int, seed: int):
+    """Backward-compatible generator used by older external callers."""
+    stream = StatefulBatchStream(dataset, batch_size, seed)
+    while True:
+        yield stream.next_batch()
+
+
 def build_optimizer_and_scheduler(params, cfg):
     optimizer = torch.optim.AdamW(
         params,
@@ -37,40 +94,127 @@
         eps=cfg.optimizer.eps,
         weight_decay=cfg.optimizer.weight_decay,
     )
-    warmup_steps = max(1, cfg.optimizer.warmup_steps)
+    warmup_steps = max(1, int(cfg.optimizer.warmup_steps))
 
     def lr_lambda(step: int) -> float:
         if step < warmup_steps:
             return (step + 1) / warmup_steps
-        return 1.0  # constant-after-warmup; swap in a decay schedule here if you want one
+        return 1.0
 
     scheduler = torch.optim.lr_scheduler.LambdaLR(optimizer, lr_lambda)
     return optimizer, scheduler
 
 
-def batches(dataset: list[dict], batch_size: int, seed: int) -> Iterable[list[dict]]:
-    """Infinite stream of shuffled batches (reshuffles at the start of every pass)."""
-    if len(dataset) < batch_size:
-        raise ValueError(
-            f"micro_batch_size ({batch_size}) is larger than the dataset ({len(dataset)} examples)."
-        )
-    rng = random.Random(seed)
-    indices = list(range(len(dataset)))
-    while True:
-        rng.shuffle(indices)
-        for start in range(0, len(indices) - batch_size + 1, batch_size):
-            batch_idx = indices[start:start + batch_size]
-            yield [dataset[j] for j in batch_idx]
+def _save_model(model, path: Path, cfg) -> None:
+    if cfg.checkpointing.get("save_adapter_only", True) and hasattr(model, "save_pretrained"):
+        model.save_pretrained(str(path))
+    else:
+        torch.save(model.state_dict(), path / "model_state_dict.pt")
 
 
-def save_checkpoint(model, path: Path, cfg, extra_state: Optional[dict] = None) -> None:
+def save_checkpoint(
+    model,
+    path: Path,
+    cfg,
+    *,
+    optimizer=None,
+    scheduler=None,
+    batch_stream: Optional[StatefulBatchStream] = None,
+    global_step: int = 0,
+    method_state: Optional[dict] = None,
+) -> None:
+    """Save adapter/model and complete per-rank resumable training state.
+
+    The adapter itself is identical on every rank after gradient all-reduce, so
+    only rank 0 writes model files. RNG, data-stream position, and method state
+    can differ by rank and are therefore written to rank-specific state files.
+    """
     path.mkdir(parents=True, exist_ok=True)
-    if cfg.checkpointing.get("save_adapter_only", True) and hasattr(model, "save_pretrained"):
-        model.save_pretrained(str(path))  # PEFT models: this saves only the (small) adapter
+    if is_main_process():
+        _save_model(model, path, cfg)
+
+    state = {
+        "global_step": int(global_step),
+        "rank": int(rank()),
+        "world_size": int(world_size()),
+        "optimizer": optimizer.state_dict() if optimizer is not None else None,
+        "scheduler": scheduler.state_dict() if scheduler is not None else None,
+        "batch_stream": batch_stream.state_dict() if batch_stream is not None else None,
+        "python_rng": random.getstate(),
+        "torch_rng": torch.get_rng_state(),
+        "cuda_rng": torch.cuda.get_rng_state_all() if torch.cuda.is_available() else None,
+        "method_state": method_state,
+    }
+
+    if world_size() > 1:
+        state_path = path / f"training_state_rank_{rank()}.pt"
     else:
-        torch.save(model.state_dict(), path / "model_state_dict.pt")
-    if extra_state:
-        torch.save(extra_state, path / "method_state.pt")
+        state_path = path / "training_state.pt"
+    torch.save(state, state_path)
+
+    # Rank-zero aliases keep older tooling usable without sacrificing exact
+    # per-rank resume data for distributed runs.
+    if is_main_process() and world_size() > 1:
+        torch.save(state, path / "training_state.pt")
+    if is_main_process() and method_state:
+        torch.save(method_state, path / "method_state.pt")
+
+
+def _restore_training_state(
+    resume_dir: Path,
+    *,
+    optimizer,
+    scheduler,
+    batch_stream: StatefulBatchStream,
+    restore_method_state: Optional[Callable],
+) -> int:
+    rank_state = resume_dir / f"training_state_rank_{rank()}.pt"
+    common_state = resume_dir / "training_state.pt"
+    state_path = rank_state if rank_state.exists() else common_state
+    if not state_path.exists():
+        raise FileNotFoundError(
+            f"Resume directory {resume_dir} has no training state for rank {rank()}. "
+            "It may be an old adapter-only checkpoint and cannot exactly resume."
+        )
+    state = torch.load(state_path, map_location="cpu", weights_only=False)
+    saved_world = int(state.get("world_size", 1))
+    if saved_world != world_size():
+        raise ValueError(
+            f"Cannot exactly resume a checkpoint saved with world_size={saved_world} "
+            f"using world_size={world_size()}."
+        )
+    if state.get("optimizer") is not None:
+        optimizer.load_state_dict(state["optimizer"])
+    if state.get("scheduler") is not None:
+        scheduler.load_state_dict(state["scheduler"])
+    if state.get("batch_stream") is not None:
+        batch_stream.load_state_dict(state["batch_stream"])
+    if state.get("python_rng") is not None:
+        random.setstate(state["python_rng"])
+    if state.get("torch_rng") is not None:
+        torch.set_rng_state(state["torch_rng"])
+    if torch.cuda.is_available() and state.get("cuda_rng") is not None:
+        torch.cuda.set_rng_state_all(state["cuda_rng"])
+    if restore_method_state is not None and state.get("method_state") is not None:
+        restore_method_state(state["method_state"])
+    return int(state.get("global_step", 0))
+
+
+def _numeric_average(metrics: list[dict]) -> dict:
+    if not metrics:
+        return {}
+    keys = set.intersection(*[
+        {k for k, v in m.items() if isinstance(v, (int, float))}
+        for m in metrics
+    ])
+    return {k: sum(float(m[k]) for m in metrics) / len(metrics) for k in sorted(keys)}
+
+
+def _write_generations(handle, records) -> None:
+    if not records or handle is None:
+        return
+    for rec in records:
+        handle.write(json.dumps(rec) + "\n")
 
 
 def run_training_loop(
@@ -78,105 +222,206 @@
     tokenizer,
     params: list[torch.nn.Parameter],
     train_dataset: list[dict],
-    step_fn: Callable,
+    step_fn: Optional[Callable],
     cfg,
     run_name: str,
     eval_fn: Optional[Callable] = None,
     post_optimizer_step: Optional[Callable] = None,
     checkpoint_state: Optional[Callable] = None,
+    restore_method_state: Optional[Callable] = None,
+    collect_fn: Optional[Callable] = None,
+    score_fn: Optional[Callable] = None,
 ) -> None:
+    if (collect_fn is None) != (score_fn is None):
+        raise ValueError("collect_fn and score_fn must either both be provided or both omitted.")
+    if collect_fn is None and step_fn is None:
+        raise ValueError("Provide either step_fn or collect_fn+score_fn.")
+
     output_dir = Path(cfg.training.output_dir)
-    output_dir.mkdir(parents=True, exist_ok=True)
-    logger = RunLogger(str(output_dir), run_name, dict(cfg), backend=cfg.logging.backend)
-    # step_fn may attach a "_generations" key (list[dict], e.g.
-    # {"prompt":..., "response_text":..., "reward":...}) to its metrics
-    # dict to have raw rollout text persisted alongside the numeric
-    # metrics -- used by scripts/kaggle_smoke.py to verify the rollout
-    # pipeline actually produces non-empty text, not just that losses
-    # are finite. Kept as a separate file (not metrics.jsonl) since it's
-    # not numeric and would otherwise need special-casing everywhere
-    # metrics get aggregated/averaged.
-    generations_path = output_dir / "generations.jsonl"
-    generations_fh = open(generations_path, "a", buffering=1)
+    if is_main_process():
+        output_dir.mkdir(parents=True, exist_ok=True)
+    barrier()
+
+    logger = RunLogger(str(output_dir), run_name, dict(cfg), backend=(cfg.logging.backend if is_main_process() else "none"))
+    generations_fh = None
+    if is_main_process():
+        generations_fh = open(output_dir / "generations.jsonl", "a", buffering=1)
 
     optimizer, scheduler = build_optimizer_and_scheduler(params, cfg)
-    batch_size = cfg.training.micro_batch_size
-    accum_steps = cfg.training.grad_accum_steps
-    batch_iter = batches(train_dataset, batch_size, seed=cfg.seed)
-
-    # Model stays in eval() mode for the ENTIRE RL loop -- rollout sampling
-    # AND the scoring/gradient forward pass. If dropout were active during
-    # one but not the other, the "on-policy" ratio in the RL loss would be
-    # noisy purely from different dropout masks between the two passes, an
-    # entirely avoidable source of training instability. There is no
-    # BatchNorm anywhere in this stack, so eval() mode costs nothing else.
+    accum_steps = int(cfg.training.grad_accum_steps)
+    stream = StatefulBatchStream(
+        train_dataset,
+        int(cfg.training.micro_batch_size),
+        seed=int(cfg.seed),
+    )
     model.eval()
 
+    resume_from = cfg.training.get("resume_from", None)
     global_step = 0
+    if resume_from:
+        global_step = _restore_training_state(
+            Path(resume_from),
+            optimizer=optimizer,
+            scheduler=scheduler,
+            batch_stream=stream,
+            restore_method_state=restore_method_state,
+        )
+        if is_main_process():
+            print(f"[{run_name}] resumed at optimizer step {global_step} from {resume_from}")
+
+    completed = False
     try:
-        while global_step < cfg.training.total_steps:
+        while global_step < int(cfg.training.total_steps):
             optimizer.zero_grad(set_to_none=True)
             accumulated: Optional[list[torch.Tensor]] = None
             accum_metrics: list[dict] = []
 
-            for _ in range(accum_steps):
-                batch = next(batch_iter)
-                prompts = [ex["prompt"] for ex in batch]
-                answers = [ex["answer"] for ex in batch]
-                grads, metrics = step_fn(model, tokenizer, prompts, answers, global_step)
-                generations = metrics.pop("_generations", None)
-                if generations:
-                    for rec in generations:
-                        generations_fh.write(json.dumps(rec) + "\n")
-                accum_metrics.append(metrics)
-
-                if accumulated is None:
-                    accumulated = [g.detach().clone() for g in grads]
-                else:
-                    for acc, g in zip(accumulated, grads):
-                        acc.add_(g)
+            if collect_fn is not None:
+                collected_batches: list[CollectedBatch] = []
+                for _ in range(accum_steps):
+                    batch = stream.next_batch()
+                    prompts = [ex["prompt"] for ex in batch]
+                    answers = [ex["answer"] for ex in batch]
+                    collected = collect_fn(model, tokenizer, prompts, answers, global_step)
+                    collected.validate()
+                    collected_batches.append(collected)
+                    _write_generations(generations_fh, collected.metadata.get("generations"))
+
+                local_rewards = torch.cat([c.rewards.float() for c in collected_batches], dim=0)
+                stats_device = params[0].device if params else model.device
+                global_rewards = gather_1d_equal(local_rewards.to(stats_device))
+                adv_stats = AdvantageStats(
+                    mean=float(global_rewards.mean().item()),
+                    std=float(global_rewards.std(unbiased=False).item()),
+                    count=int(global_rewards.numel()),
+                )
+
+                for collected in collected_batches:
+                    local_advantages = adv_stats.normalize(
+                        collected.rewards.float(),
+                        eps=float(cfg.training.adv_eps),
+                    )
+                    grads, metrics = score_fn(
+                        model,
+                        tokenizer,
+                        collected,
+                        local_advantages,
+                        global_step,
+                    )
+                    metrics = dict(metrics)
+                    metrics.setdefault("reward_mean", float(collected.rewards.mean().item()))
+                    metrics.setdefault("reward_std", float(collected.rewards.std(unbiased=False).item()))
+                    metrics["effective_reward_mean"] = adv_stats.mean
+                    metrics["effective_reward_std"] = adv_stats.std
+                    metrics["effective_reward_count"] = float(adv_stats.count)
+                    accum_metrics.append(metrics)
+                    if accumulated is None:
+                        accumulated = [g.detach().clone() for g in grads]
+                    else:
+                        for acc, grad in zip(accumulated, grads):
+                            acc.add_(grad)
+            else:
+                for _ in range(accum_steps):
+                    batch = stream.next_batch()
+                    prompts = [ex["prompt"] for ex in batch]
+                    answers = [ex["answer"] for ex in batch]
+                    grads, metrics = step_fn(model, tokenizer, prompts, answers, global_step)
+                    metrics = dict(metrics)
+                    _write_generations(generations_fh, metrics.pop("_generations", None))
+                    accum_metrics.append(metrics)
+                    if accumulated is None:
+                        accumulated = [g.detach().clone() for g in grads]
+                    else:
+                        for acc, grad in zip(accumulated, grads):
+                            acc.add_(grad)
+
+            if accumulated is None:
+                raise RuntimeError("No gradients were produced for this optimizer step.")
+            accumulated = [g / accum_steps for g in accumulated]
+            # Manual-gradient methods need explicit distributed synchronization.
+            accumulated = all_reduce_gradients(accumulated)
 
-            for p, g in zip(params, accumulated):
-                p.grad = g / accum_steps
+            for parameter, gradient in zip(params, accumulated):
+                parameter.grad = gradient
 
             grad_norm = torch.nn.utils.clip_grad_norm_(params, cfg.optimizer.max_grad_norm)
             optimizer.step()
             scheduler.step()
 
-            if cfg.training.log_every and global_step % cfg.training.log_every == 0:
-                numeric_keys = [k for k in accum_metrics[0] if isinstance(accum_metrics[0][k], (int, float))]
-                agg = {k: sum(m[k] for m in accum_metrics) / len(accum_metrics) for k in numeric_keys}
-                agg["grad_norm"] = grad_norm.item()
-                agg["lr"] = scheduler.get_last_lr()[0]
-                logger.log(agg, global_step)
-                print(f"[{run_name}] step {global_step}: " + ", ".join(f"{k}={v:.4f}" for k, v in agg.items()))
-
-            # Lifecycle hook: called ONCE per optimizer step (NOT per
-            # micro-batch), AFTER optimizer.step(). This is the right
-            # hook point for VACS-style adaptive state that must observe
-            # the just-stepped gradient norms.
+            aggregated = _numeric_average(accum_metrics)
+            aggregated["grad_norm"] = float(grad_norm.item())
+            aggregated["lr"] = float(scheduler.get_last_lr()[0])
+
             if post_optimizer_step is not None:
-                numeric_keys = [k for k in accum_metrics[0] if isinstance(accum_metrics[0][k], (int, float))]
-                agg = {k: sum(m[k] for m in accum_metrics) / len(accum_metrics) for k in numeric_keys}
-                post_optimizer_step(model, agg)
+                post_optimizer_step(model, aggregated)
+
+            if cfg.training.log_every and global_step % int(cfg.training.log_every) == 0 and is_main_process():
+                logger.log(aggregated, global_step)
+                print(
+                    f"[{run_name}] step {global_step}: "
+                    + ", ".join(f"{k}={v:.8g}" for k, v in aggregated.items())
+                )
 
             if (
                 eval_fn is not None
                 and cfg.training.eval_every
-                and global_step % cfg.training.eval_every == 0
+                and global_step % int(cfg.training.eval_every) == 0
                 and global_step > 0
+                and is_main_process()
             ):
                 eval_metrics = eval_fn(model, tokenizer, global_step)
                 logger.log({f"eval/{k}": v for k, v in eval_metrics.items()}, global_step)
-                model.eval()  # eval_fn shouldn't change the mode, but cheap insurance
-
-            if cfg.training.save_every and global_step % cfg.training.save_every == 0 and global_step > 0:
-                extra = checkpoint_state() if checkpoint_state is not None else None
-                save_checkpoint(model, output_dir / f"step_{global_step}", cfg, extra_state=extra)
+                model.eval()
 
             global_step += 1
+
+            if cfg.training.save_every and global_step % int(cfg.training.save_every) == 0:
+                method_state = checkpoint_state() if checkpoint_state is not None else None
+                save_checkpoint(
+                    model,
+                    output_dir / f"step_{global_step}",
+                    cfg,
+                    optimizer=optimizer,
+                    scheduler=scheduler,
+                    batch_stream=stream,
+                    global_step=global_step,
+                    method_state=method_state,
+                )
+                barrier()
+
+        completed = True
+
+    except BaseException:
+        method_state = checkpoint_state() if checkpoint_state is not None else None
+        save_checkpoint(
+            model,
+            output_dir / "interrupted",
+            cfg,
+            optimizer=optimizer,
+            scheduler=scheduler,
+            batch_stream=stream,
+            global_step=global_step,
+            method_state=method_state,
+        )
+        barrier()
+        raise
     finally:
-        extra = checkpoint_state() if checkpoint_state is not None else None
-        save_checkpoint(model, output_dir / "final", cfg, extra_state=extra)
         logger.close()
-        generations_fh.close()
+        if generations_fh is not None:
+            generations_fh.close()
+
+    if completed:
+        method_state = checkpoint_state() if checkpoint_state is not None else None
+        save_checkpoint(
+            model,
+            output_dir / "final",
+            cfg,
+            optimizer=optimizer,
+            scheduler=scheduler,
+            batch_stream=stream,
+            global_step=global_step,
+            method_state=method_state,
+        )
+        if is_main_process():
+            (output_dir / "_SUCCESS").write_text("completed\n")
+        barrier()
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/crisp_step.py /mnt/data/crisp_work/CRISP-main/crisp/training/crisp_step.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/crisp_step.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/crisp_step.py	2026-08-02 12:43:26.995705468 +0000
@@ -1,132 +1,206 @@
-"""
-CRISP per-step loss (README Section 2): unifies the RL (REINFORCE++-style,
-PPO-clipped) term and the on-policy self-distillation term into gradients
-combined via PC-Grad (training/pcgrad.py), with the correctness gate and
-cosine-annealed lambda schedule.
-
-One call to `crisp_step` = one micro-batch's worth of: rollout generation,
-reward computation, one batched teacher+student forward pass, both losses,
-and a PC-Grad-combined gradient (NOT yet applied to `.grad` / stepped -- see
-training/common_loop.py for the outer loop that accumulates and applies
-these across micro-batches and calls optimizer.step()).
-
-Explicit design decisions worth flagging (the README specifies the loss
-*shapes* but leaves some implementation choices open):
-
-- The teacher's "privileged context" is injected as an addendum to the
-  system message (`data.teacher_hint_template`), not a fabricated extra chat
-  turn -- this keeps the chat template structurally valid across model
-  families without inventing a new role.
-- The RL term is implemented as a proper PPO-clipped surrogate (matching the
-  README's prose, "clipped ratio epsilon=0.2") even though the displayed
-  formula is plain REINFORCE; with a single on-policy pass the ratio is 1 at
-  computation time by construction, so the clip is a no-op until/unless
-  `ppo_epochs`-style rollout reuse is added on top of this loop.
-- log-probs are averaged over response tokens (length-normalized), not
-  summed, so reward/advantage signal isn't confounded with response length.
+"""CRISP rollout collection and scoring.
+
+The paper-correct path is two-stage: ``collect_crisp_batch`` samples and
+scores rewards without autograd; ``score_crisp_batch`` receives advantages
+normalized over the full effective batch from ``common_loop``.
 """
 from __future__ import annotations
 
 import torch
 
-from crisp.training.logprobs import (
-    build_scoring_batch,
-    forward_kl_teacher_student,
-    sequence_log_prob,
-)
-from crisp.training.pcgrad import pc_grad_combine
+from crisp.training.batch_types import CollectedBatch
+from crisp.training.grad_utils import compute_grads
+from crisp.training.gradient_surgery import flatten_grads
+from crisp.training.logprobs import forward_kl_teacher_student
+from crisp.training.pcgrad import PCGradResult, pc_grad_combine
 from crisp.training.rollout import generate_rollouts
 from crisp.training.schedules import lambda_schedule
-from crisp.training.teacher_student import build_teacher_student_sequences
+from crisp.training.teacher_student import (
+    build_teacher_student_sequences,
+    score_teacher_student,
+)
 
 
-def crisp_step(
-    model, tokenizer, prompts: list[str], answers: list[str], reward_fn,
-    params: list[torch.nn.Parameter], global_step: int, cfg,
-) -> tuple[list[torch.Tensor], dict]:
-    model.eval()  # dropout off for the whole RL step -- see common_loop.py
-    sys_prompt = cfg.model.system_prompt
-    n = len(prompts)
+def _valid_rollout_indices(rollouts) -> list[int]:
+    return [i for i, rollout in enumerate(rollouts) if len(rollout.response_ids) > 0]
+
 
-    # 1. Sample on-policy rollouts from the STUDENT (unconditioned) context.
+def collect_crisp_batch(
+    model,
+    tokenizer,
+    prompts: list[str],
+    answers: list[str],
+    reward_fn,
+    cfg,
+) -> CollectedBatch:
+    model.eval()
     rollouts = generate_rollouts(
-        model, tokenizer, prompts, sys_prompt,
+        model,
+        tokenizer,
+        prompts,
+        cfg.model.system_prompt,
         max_prompt_length=cfg.rollout.max_prompt_length,
         max_new_tokens=cfg.rollout.max_new_tokens,
-        temperature=cfg.rollout.temperature, top_p=cfg.rollout.top_p,
-        do_sample=cfg.rollout.do_sample, num_return_sequences=1,
+        temperature=cfg.rollout.temperature,
+        top_p=cfg.rollout.top_p,
+        do_sample=cfg.rollout.do_sample,
+        num_return_sequences=1,
     )
+    rewards = torch.tensor(
+        [reward_fn(r.response_text, answer) for r, answer in zip(rollouts, answers)],
+        dtype=torch.float32,
+    )
+    collected = CollectedBatch(
+        prompts=list(prompts),
+        answers=list(answers),
+        rollouts=rollouts,
+        rewards=rewards,
+        metadata={
+            "generations": [
+                {"prompt": p, "response_text": r.response_text, "reward": float(reward)}
+                for p, r, reward in zip(prompts, rollouts, rewards.tolist())
+            ]
+        },
+    )
+    collected.validate()
+    return collected
+
+
+def _naive_combine(loss_rl, loss_sd, params, lam: float, eps: float) -> PCGradResult:
+    needs_sd = loss_sd is not None and loss_sd.requires_grad
+    g_rl = compute_grads(loss_rl, params, retain_graph=needs_sd)
+    g_sd = (
+        compute_grads(loss_sd, params, retain_graph=False)
+        if needs_sd
+        else [torch.zeros_like(p) for p in params]
+    )
+    flat_rl = flatten_grads(g_rl)
+    flat_sd = flatten_grads(g_sd)
+    rl_norm = flat_rl.norm()
+    sd_norm = flat_sd.norm()
+    dot = torch.dot(flat_rl, flat_sd)
+    cos = 0.0
+    if float(rl_norm.item()) > 0.0 and float(sd_norm.item()) > 0.0:
+        cos = float((dot / (rl_norm * sd_norm + eps)).item())
+    return PCGradResult(
+        combined=[a + lam * b for a, b in zip(g_rl, g_sd)],
+        cos_sim=cos,
+        conflicted=bool((dot < 0).item()),
+        rl_grad_norm=float(rl_norm.item()),
+        sd_grad_norm=float(sd_norm.item()),
+    )
+
 
-    # 2. Verifiable reward per rollout.
-    rewards = torch.tensor([reward_fn(r.response_text, a) for r, a in zip(rollouts, answers)], dtype=torch.float32)
+def score_crisp_batch(
+    model,
+    tokenizer,
+    collected: CollectedBatch,
+    advantages: torch.Tensor,
+    params: list[torch.nn.Parameter],
+    global_step: int,
+    cfg,
+) -> tuple[list[torch.Tensor], dict]:
+    valid = _valid_rollout_indices(collected.rollouts)
+    if not valid:
+        raise RuntimeError("Every CRISP rollout was empty; no response tokens can be scored.")
+
+    prompts = [collected.prompts[i] for i in valid]
+    answers = [collected.answers[i] for i in valid]
+    rollouts = [collected.rollouts[i] for i in valid]
+    rewards = collected.rewards[valid]
+    advantages = advantages[valid].to(model.device)
 
-    # 3. Build the combined teacher+student scoring batch: 2N rows, teacher
-    #    rows first, in a fixed order remembered via list slicing below --
-    #    a single forward pass, never two model copies (README Section 2.1).
-    #    Sequence construction now lives in build_teacher_student_sequences
-    #    so CRISP, OPSD, and the new methods all share one source of truth
-    #    for the "teacher and student score the same response tokens"
-    #    invariant.
-    hint_template = cfg.data.teacher_hint_template
     teacher_seqs, student_seqs = build_teacher_student_sequences(
-        prompts, answers, rollouts, tokenizer=tokenizer,
-        sys_prompt=sys_prompt, hint_template=hint_template,
+        prompts,
+        answers,
+        rollouts,
+        tokenizer=tokenizer,
+        sys_prompt=cfg.model.system_prompt,
+        hint_template=cfg.data.teacher_hint_template,
+    )
+    scored = score_teacher_student(
+        model=model,
+        teacher_sequences=teacher_seqs,
+        student_sequences=student_seqs,
+        pad_id=tokenizer.pad_token_id,
+    )
+
+    # Plain single-pass policy gradient. A PPO ratio built from
+    # ``new_lp - new_lp.detach()`` is numerically one and cannot clip.
+    per_example = []
+    for advantage, token_logps in zip(advantages, scored.student_token_logps):
+        if token_logps.numel() == 0:
+            continue
+        per_example.append(-advantage * token_logps.mean())
+    if not per_example:
+        raise RuntimeError("No non-empty CRISP response remained after validation.")
+    loss_rl = torch.stack(per_example).mean()
+
+    kl_per_example = forward_kl_teacher_student(
+        scored.teacher_row_log_probs,
+        scored.student_row_log_probs,
     )
-    all_seqs = teacher_seqs + student_seqs
-    pad_id = tokenizer.pad_token_id
-    input_ids, attention_mask = build_scoring_batch(all_seqs, pad_id)
-    input_ids = input_ids.to(model.device)
-    attention_mask = attention_mask.to(model.device)
-
-    token_log_probs, row_log_probs = sequence_log_prob(model, input_ids, attention_mask, all_seqs)
-    teacher_row_log_probs, student_row_log_probs = row_log_probs[:n], row_log_probs[n:]
-    student_token_log_probs = token_log_probs[n:]
-
-    # 4. L_RL: REINFORCE++-style global-batch-normalized advantage,
-    #    PPO-clipped (see module docstring re: the clip being a no-op under
-    #    strict single-pass on-policy training).
-    reward_device = rewards  # kept on CPU for the mean/std reduction; moved below
-    adv_mean, adv_std = reward_device.mean(), reward_device.std(unbiased=False)
-    advantages = ((rewards - adv_mean) / (adv_std + cfg.training.adv_eps)).to(model.device)
-
-    eps_clip = cfg.training.clip_epsilon
-    per_example_rl_loss = []
-    for i in range(n):
-        new_lp = student_token_log_probs[i].mean()  # length-normalized sequence log-prob
-        old_lp = new_lp.detach()                     # on-policy: same value, detached
-        ratio = torch.exp(new_lp - old_lp)
-        unclipped = ratio * advantages[i]
-        clipped = torch.clamp(ratio, 1 - eps_clip, 1 + eps_clip) * advantages[i]
-        per_example_rl_loss.append(-torch.min(unclipped, clipped))
-    loss_rl = torch.stack(per_example_rl_loss).mean()
-
-    # 5. L_SD: forward-KL(teacher || student), correctness-gated to 0 for
-    #    already-correct rollouts (README Section 2.1, "mitigates FM4").
-    kl_per_example = forward_kl_teacher_student(teacher_row_log_probs, student_row_log_probs)
-    gate = (rewards.to(model.device) < 1.0).float()
-    any_active = bool(gate.sum().item() > 0)
-    if any_active:
-        gated_kl = torch.stack([k * g for k, g in zip(kl_per_example, gate)])
-        loss_sd = gated_kl.sum() / gate.sum().clamp(min=1.0)
+    if cfg.training.get("correctness_gate", True):
+        gate = (rewards.to(model.device) < 1.0).float()
+    else:
+        gate = torch.ones_like(rewards, device=model.device)
+    if bool(gate.sum().item() > 0):
+        gated = torch.stack([kl * weight for kl, weight in zip(kl_per_example, gate)])
+        loss_sd = gated.sum() / gate.sum().clamp(min=1.0)
     else:
         loss_sd = None
 
-    lam = lambda_schedule(global_step, cfg.training.total_steps, cfg.training.lambda_max)
-
-    # 6. PC-Grad-deconflicted combined gradient (not yet applied).
-    pcgrad_result = pc_grad_combine(loss_rl, loss_sd, params, lam, eps=cfg.training.pcgrad_eps)
+    lam = lambda_schedule(
+        global_step,
+        cfg.training.total_steps,
+        cfg.training.lambda_max,
+        mode=cfg.training.get("lambda_schedule", "cosine"),
+    )
+    if cfg.training.get("use_pcgrad", True):
+        result = pc_grad_combine(
+            loss_rl,
+            loss_sd,
+            params,
+            lam,
+            eps=cfg.training.pcgrad_eps,
+        )
+    else:
+        result = _naive_combine(loss_rl, loss_sd, params, lam, cfg.training.pcgrad_eps)
 
     metrics = {
-        "reward_mean": rewards.mean().item(),
-        "reward_std": rewards.std(unbiased=False).item(),
-        "loss_rl": loss_rl.item(),
-        "loss_sd": (loss_sd.item() if loss_sd is not None else 0.0),
-        "lambda": lam,
-        "cos_g_rl_g_sd": pcgrad_result.cos_sim,
-        "grad_conflicted": float(pcgrad_result.conflicted),
-        "rl_grad_norm": pcgrad_result.rl_grad_norm,
-        "sd_grad_norm": pcgrad_result.sd_grad_norm,
-        "pct_gated_correct": 1.0 - gate.mean().item(),
-        "response_len_mean": sum(len(r.response_ids) for r in rollouts) / n,
+        "reward_mean": float(rewards.mean().item()),
+        "reward_std": float(rewards.std(unbiased=False).item()),
+        "loss_rl": float(loss_rl.item()),
+        "loss_sd": float(loss_sd.item()) if loss_sd is not None else 0.0,
+        "lambda": float(lam),
+        "cos_g_rl_g_sd": result.cos_sim,
+        "grad_conflicted": float(result.conflicted),
+        "rl_grad_norm": result.rl_grad_norm,
+        "sd_grad_norm": result.sd_grad_norm,
+        "pct_gated_correct": float(1.0 - gate.mean().item()),
+        "response_len_mean": sum(len(r.response_ids) for r in rollouts) / len(rollouts),
+        "empty_response_fraction": 1.0 - len(valid) / max(len(collected.rollouts), 1),
+        "advantage_abs_mean": float(advantages.abs().mean().item()),
     }
-    return pcgrad_result.combined, metrics
+    return result.combined, metrics
+
+
+def crisp_step(
+    model,
+    tokenizer,
+    prompts: list[str],
+    answers: list[str],
+    reward_fn,
+    params: list[torch.nn.Parameter],
+    global_step: int,
+    cfg,
+) -> tuple[list[torch.Tensor], dict]:
+    """Backward-compatible local-microbatch wrapper used by unit tests/tools."""
+    collected = collect_crisp_batch(model, tokenizer, prompts, answers, reward_fn, cfg)
+    rewards = collected.rewards
+    advantages = (rewards - rewards.mean()) / (
+        rewards.std(unbiased=False) + cfg.training.adv_eps
+    )
+    return score_crisp_batch(
+        model, tokenizer, collected, advantages, params, global_step, cfg
+    )
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/logprobs.py /mnt/data/crisp_work/CRISP-main/crisp/training/logprobs.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/logprobs.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/logprobs.py	2026-08-02 12:47:47.127712276 +0000
@@ -167,6 +167,9 @@
                 "just under different left-context -- check how teacher/student ScoredSequences "
                 "were built (both should append the identical response_ids)."
             )
+        if t_lp.shape[0] == 0:
+            out.append(torch.zeros((), dtype=s_lp.dtype, device=s_lp.device))
+            continue
         t_lp = t_lp.detach()
         t_p = t_lp.exp()
         per_token_kl = (t_p * (t_lp - s_lp)).sum(dim=-1)  # [response_len]
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/schedules.py /mnt/data/crisp_work/CRISP-main/crisp/training/schedules.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/schedules.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/schedules.py	2026-08-02 12:43:01.603704804 +0000
@@ -1,29 +1,32 @@
-"""
-Pure-math schedule functions used during training. Deliberately torch-free
-(only stdlib `math`) so they're unit-testable in any environment, including
-one without torch/transformers installed -- there's no reason a formula this
-simple should require pulling in the rest of the stack to verify.
-"""
+"""Pure-math training schedules."""
 from __future__ import annotations
 
 import math
 
 
-def lambda_schedule(step: int, total_steps: int, lambda_max: float) -> float:
-    """lambda(t) = lambda_max * cos(pi * t / (2T)) (README Section 2.3):
-    starts at `lambda_max` and anneals to 0 over `total_steps`, so the
-    self-distillation term dominates early (when rollouts are mostly wrong
-    and need correcting) and fades out as training progresses.
+def lambda_schedule(
+    step: int,
+    total_steps: int,
+    lambda_max: float,
+    mode: str = "cosine",
+) -> float:
+    """Return a bounded auxiliary-loss coefficient.
 
-    Clipped to [0, lambda_max]: without clipping, `t > total_steps` (e.g. a
-    resumed or accidentally-extended run) pushes cos(...) negative, which
-    would flip the distillation term adversarial instead of merely
-    switching it off. `step` is clamped into `[0, total_steps]` before the
-    cosine is evaluated for the same reason, and negative `step` values
-    (shouldn't happen, but cheap to guard) are treated as step 0.
+    Supported modes are ``cosine``, ``linear``, and ``constant``. The step is
+    clamped to ``[0, total_steps]`` so an extended/resumed run never flips the
+    coefficient negative.
     """
-    if total_steps <= 0:
+    if total_steps <= 0 or lambda_max <= 0.0:
         return 0.0
-    t = min(max(step, 0), total_steps)
-    value = lambda_max * math.cos(math.pi * t / (2 * total_steps))
-    return max(0.0, value)
+    t = min(max(int(step), 0), int(total_steps))
+    if mode == "cosine":
+        value = lambda_max * math.cos(math.pi * t / (2.0 * total_steps))
+    elif mode == "linear":
+        value = lambda_max * (1.0 - t / total_steps)
+    elif mode == "constant":
+        value = lambda_max
+    else:
+        raise ValueError(
+            f"Unknown lambda schedule {mode!r}; expected cosine, linear, or constant."
+        )
+    return max(0.0, min(float(lambda_max), float(value)))
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/sft_train.py /mnt/data/crisp_work/CRISP-main/crisp/training/sft_train.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/sft_train.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/sft_train.py	2026-08-02 12:49:10.227714451 +0000
@@ -0,0 +1,130 @@
+"""Completion-only supervised fine-tuning for SFT -> RL experiments."""
+from __future__ import annotations
+
+import argparse
+from pathlib import Path
+from typing import Optional
+
+import torch
+from datasets import Dataset
+from transformers import DataCollatorForSeq2Seq, Trainer, TrainingArguments
+
+from crisp.data.build_dataset import load_numina_sft
+from crisp.model.load import attach_or_load_lora, load_model_and_tokenizer
+from crisp.utils.config import load_config
+from crisp.utils.distributed import init_distributed, is_main_process
+from crisp.utils.seeding import set_seed
+
+
+def _tokenize_sft_example(example, tokenizer, cfg):
+    prompt_messages = [
+        {"role": "system", "content": cfg.model.system_prompt},
+        {"role": "user", "content": example["prompt"]},
+    ]
+    full_messages = prompt_messages + [
+        {"role": "assistant", "content": example["completion"]}
+    ]
+    prompt_text = tokenizer.apply_chat_template(
+        prompt_messages,
+        tokenize=False,
+        add_generation_prompt=True,
+    )
+    full_text = tokenizer.apply_chat_template(
+        full_messages,
+        tokenize=False,
+        add_generation_prompt=False,
+    )
+    max_length = int(cfg.sft.max_sequence_length)
+    encoded = tokenizer(
+        full_text,
+        truncation=True,
+        max_length=max_length,
+        add_special_tokens=False,
+    )
+    labels = list(encoded["input_ids"])
+    if cfg.sft.get("completion_only_loss", True):
+        prompt_ids = tokenizer(
+            prompt_text,
+            truncation=True,
+            max_length=max_length,
+            add_special_tokens=False,
+        )["input_ids"]
+        prompt_len = min(len(prompt_ids), len(labels))
+        labels[:prompt_len] = [-100] * prompt_len
+    encoded["labels"] = labels
+    return encoded
+
+
+def main(config_path: str, overrides: Optional[dict] = None) -> None:
+    cfg = load_config(config_path, overrides)
+    init_distributed()
+    set_seed(cfg.seed)
+
+    model, tokenizer = load_model_and_tokenizer(cfg.model)
+    model, _ = attach_or_load_lora(
+        model,
+        cfg.lora,
+        initial_adapter=cfg.model.get("initial_adapter"),
+    )
+    model.config.use_cache = False
+
+    rows = load_numina_sft(cfg.data.train_split)
+    if cfg.data.train_subset_size is not None:
+        rows = rows[: int(cfg.data.train_subset_size)]
+    dataset = Dataset.from_list(rows).map(
+        lambda row: _tokenize_sft_example(row, tokenizer, cfg),
+        remove_columns=["prompt", "completion"],
+    )
+
+    output_dir = Path(cfg.training.output_dir)
+    output_dir.mkdir(parents=True, exist_ok=True)
+    report_to = ["wandb"] if cfg.logging.backend == "wandb" else []
+    args = TrainingArguments(
+        output_dir=str(output_dir),
+        max_steps=int(cfg.training.total_steps),
+        num_train_epochs=float(cfg.sft.num_train_epochs),
+        per_device_train_batch_size=int(cfg.training.micro_batch_size),
+        gradient_accumulation_steps=int(cfg.training.grad_accum_steps),
+        learning_rate=float(cfg.optimizer.lr),
+        warmup_steps=int(cfg.optimizer.warmup_steps),
+        weight_decay=float(cfg.optimizer.weight_decay),
+        max_grad_norm=float(cfg.optimizer.max_grad_norm),
+        logging_steps=max(1, int(cfg.training.log_every)),
+        save_steps=max(1, int(cfg.training.save_every)) if cfg.training.save_every else 10**9,
+        save_total_limit=2,
+        bf16=torch.cuda.is_available() and cfg.model.torch_dtype == "bfloat16",
+        fp16=torch.cuda.is_available() and cfg.model.torch_dtype == "float16",
+        report_to=report_to,
+        run_name=cfg.logging.run_name or "sft",
+        remove_unused_columns=False,
+        ddp_find_unused_parameters=False,
+    )
+    collator = DataCollatorForSeq2Seq(
+        tokenizer=tokenizer,
+        model=model,
+        padding=True,
+        label_pad_token_id=-100,
+        return_tensors="pt",
+    )
+    trainer = Trainer(
+        model=model,
+        args=args,
+        train_dataset=dataset,
+        data_collator=collator,
+    )
+    resume = cfg.training.get("resume_from")
+    trainer.train(resume_from_checkpoint=resume if resume else None)
+    final_dir = output_dir / "final"
+    trainer.save_model(str(final_dir))
+    tokenizer.save_pretrained(str(final_dir))
+    if is_main_process():
+        (output_dir / "_SUCCESS").write_text("completed\n")
+
+
+if __name__ == "__main__":
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--config", required=True)
+    parser.add_argument("--override", action="append", default=[])
+    args = parser.parse_args()
+    from train_launcher import parse_overrides
+    main(args.config, overrides=parse_overrides(args.override))
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/train.py /mnt/data/crisp_work/CRISP-main/crisp/training/train.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/train.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/train.py	2026-08-02 12:43:44.847705936 +0000
@@ -1,4 +1,4 @@
-"""CRISP training entrypoint: `python train_launcher.py crisp --config ...`"""
+"""CRISP training entrypoint."""
 from __future__ import annotations
 
 import argparse
@@ -7,26 +7,25 @@
 from crisp.data.build_dataset import load_train_dataset
 from crisp.data.reward import build_reward_fn
 from crisp.eval.run_eval import make_eval_fn
-from crisp.model.load import attach_lora, load_model_and_tokenizer
+from crisp.model.load import attach_or_load_lora, load_model_and_tokenizer
 from crisp.training.common_loop import run_training_loop
-from crisp.training.crisp_step import crisp_step
+from crisp.training.crisp_step import collect_crisp_batch, score_crisp_batch
 from crisp.utils.config import load_config
+from crisp.utils.distributed import init_distributed
 from crisp.utils.seeding import set_seed
 
 
 def main(config_path: str, overrides: Optional[dict] = None) -> None:
     cfg = load_config(config_path, overrides)
+    init_distributed()
     set_seed(cfg.seed)
 
     model, tokenizer = load_model_and_tokenizer(cfg.model)
-    model, is_peft = attach_lora(model, cfg.lora)
+    initial_adapter = cfg.model.get("initial_adapter") or cfg.training.get("resume_from")
+    model, _ = attach_or_load_lora(model, cfg.lora, initial_adapter=initial_adapter)
     params = [p for p in model.parameters() if p.requires_grad]
     if not params:
-        raise RuntimeError(
-            "No trainable parameters found. If lora.enabled is false this does a full "
-            "finetune and every parameter should already require grad; if lora.enabled is "
-            "true, check that attach_lora actually ran (see crisp/model/load.py)."
-        )
+        raise RuntimeError("No trainable parameters found.")
 
     train_dataset = load_train_dataset(
         cfg.data.train_dataset,
@@ -35,18 +34,26 @@
     )
     reward_fn = build_reward_fn(cfg.reward)
 
-    def step_fn(model, tokenizer, prompts, answers, global_step):
-        # `global_step` is threaded through from the outer loop (rather than
-        # each method counting its own calls) specifically because it must
-        # advance once per OPTIMIZER step, not once per micro-batch -- with
-        # grad_accum_steps > 1, crisp_step is called multiple times per
-        # optimizer step, and the lambda(t) schedule needs the outer count.
-        return crisp_step(model, tokenizer, prompts, answers, reward_fn, params, global_step, cfg)
+    def collect_fn(model_, tokenizer_, prompts, answers, global_step):
+        return collect_crisp_batch(model_, tokenizer_, prompts, answers, reward_fn, cfg)
+
+    def score_fn(model_, tokenizer_, collected, advantages, global_step):
+        return score_crisp_batch(
+            model_, tokenizer_, collected, advantages, params, global_step, cfg
+        )
 
     eval_fn = make_eval_fn(cfg) if cfg.data.eval_datasets else None
     run_training_loop(
-        model, tokenizer, params, train_dataset, step_fn, cfg,
-        run_name=cfg.logging.run_name or "crisp", eval_fn=eval_fn,
+        model=model,
+        tokenizer=tokenizer,
+        params=params,
+        train_dataset=train_dataset,
+        step_fn=None,
+        collect_fn=collect_fn,
+        score_fn=score_fn,
+        cfg=cfg,
+        run_name=cfg.logging.run_name or "crisp",
+        eval_fn=eval_fn,
     )
 
 
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/vacs_state.py /mnt/data/crisp_work/CRISP-main/crisp/training/vacs_state.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/vacs_state.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/vacs_state.py	2026-08-02 12:45:06.647708076 +0000
@@ -1,23 +1,9 @@
-"""VACS adaptive mixing state and `lambda_eff` computation.
+"""Adaptive gradient-energy state for VACS.
 
-VACS uses an EMA of recent gradient norms and cosine values to scale the
-self-distillation coefficient. The state object exposes three operations:
-
-  - `observe(norm_sq, cos_value)`  -- writes a fresh per-micro-batch
-    gradient statistic into a PENDING slot. Does NOT change the committed
-    EMA.
-  - `commit(rl_grad_norm_sq, sd_grad_norm_sq, cos_value)` -- called once
-    per optimizer step, AFTER `optimizer.step()`. Updates the committed
-    EMA using the pending observation (or, if no observation was made
-    this step, no-op).
-  - `compute_lambda_effective(base_lambda)` -- reads the COMMITTED EMA
-    (not the pending one), so a micro-batch's gradient cannot influence
-    the coefficient used in its own optimizer step.
-
-That separation is the structural rule the plan calls out: it has to be
-enforced by the API (this object), not by hope-and-discipline on the
-caller's side. A micro-batch and an optimizer step are not the same
-event when `grad_accum_steps > 1`.
+The implementation aggregates every micro-batch observation in an optimizer
+step before committing one EMA update. Earlier versions used "last write
+wins", making the controller depend on ``grad_accum_steps`` and discarding
+most of the applied gradient statistics.
 """
 from __future__ import annotations
 
@@ -30,116 +16,101 @@
 def observe_grad_stats(
     g_rl: torch.Tensor, g_sd: torch.Tensor, eps: float = 1e-12
 ) -> tuple[float, float]:
-    """Return (|g_rl|^2, cos(g_rl, g_sd)) for the just-computed gradients.
-
-    Inputs may be the flat gradient vectors from the asymmetric PC-Grad
-    helper or any same-shape tensors. Returns plain floats. With a
-    zero-norm g_sd, `cos` is reported as 0 (so the cosine EMA updates to
-    a neutral value, not a NaN).
-    """
     rl_norm_sq = float(g_rl.detach().pow(2).sum().item())
-    sd_norm = g_sd.detach().norm()
+    rl_norm = float(g_rl.detach().norm().item())
+    sd_norm = float(g_sd.detach().norm().item())
     cos_value = 0.0
-    if float(sd_norm.item()) > 0.0:
-        rl_norm = g_rl.detach().norm()
-        if float(rl_norm.item()) > 0.0:
-            cos_value = float(torch.dot(g_rl.detach(), g_sd.detach()).item()
-                              / (float(rl_norm.item()) * float(sd_norm.item()) + eps))
-            # Clamp the magnitude to the unit interval to absorb tiny
-            # numerical overflow from the float32 matmuls that produced it.
-            cos_value = max(-1.0, min(1.0, cos_value))
+    if rl_norm > 0.0 and sd_norm > 0.0:
+        cos_value = float(
+            torch.dot(g_rl.detach(), g_sd.detach()).item()
+            / (rl_norm * sd_norm + eps)
+        )
+        cos_value = max(-1.0, min(1.0, cos_value))
     return rl_norm_sq, cos_value
 
 
 @dataclass
 class VacsAdaptiveState:
-    """EMA-based adaptive mixing state for VACS's lambda_eff.
-
-    Defaults reproduce the document's named values:
-      - ema_decay = 0.95 (smooth but responsive),
-      - multiplier_min = 0.10, multiplier_max = 5.0 (bounded).
-
-    The state is JSON-serializable via `state_dict` / `load_state_dict`,
-    so it can ride along in the project's checkpoint alongside the
-    adapter state.
-    """
-
     ema_decay: float = 0.95
     multiplier_min: float = 0.10
     multiplier_max: float = 5.0
-    # Disabled flag: when True, compute_lambda_effective returns the
-    # raw base_lambda (no variance ratio, no cosine weighting).
     disabled: bool = False
 
+    rl_grad_norm_sq_ema: float = 0.0
+    sd_grad_norm_sq_ema: float = 0.0
+    cos_ema: float = 0.0
+
+    _pending_rl_sum: float = 0.0
+    _pending_sd_sum: float = 0.0
+    _pending_cos_sum: float = 0.0
+    _pending_count: int = 0
+
     def __post_init__(self):
         if not 0.0 <= self.ema_decay <= 1.0:
             raise ValueError(f"ema_decay must be in [0, 1]; got {self.ema_decay}")
         if self.multiplier_min <= 0.0:
-            raise ValueError(f"multiplier_min must be positive; got {self.multiplier_min}")
+            raise ValueError("multiplier_min must be positive")
         if self.multiplier_max < self.multiplier_min:
-            raise ValueError(
-                f"multiplier_max ({self.multiplier_max}) must be >= multiplier_min "
-                f"({self.multiplier_min})"
-            )
+            raise ValueError("multiplier_max must be >= multiplier_min")
 
-    # ---- Committed EMA values (used by compute_lambda_effective) ----
-    rl_grad_norm_sq_ema: float = 0.0
-    sd_grad_norm_sq_ema: float = 0.0
-    cos_ema: float = 0.0
-    # Pending slot: written by observe(), promoted to committed by commit().
-    _pending_rl_norm_sq: float = 0.0
-    _pending_sd_norm_sq: float = 0.0
-    _pending_cos: float = 0.0
-    _has_pending: bool = False
-
-    def observe(self, norm_sq: float, cos_value: float, sd_norm_sq: float | None = None) -> None:
-        """Record a per-micro-batch gradient statistic into the pending slot.
-
-        Does NOT change the committed EMA. Multiple calls before a commit
-        overwrite the pending slot (last write wins), which is the
-        intended behavior when `grad_accum_steps > 1`: the last
-        micro-batch's statistics are the ones committed at the next
-        optimizer step. (If the caller wants to accumulate statistics
-        across micro-batches instead, it can compute them itself and
-        call `commit` directly.)
-        """
-        self._pending_rl_norm_sq = float(norm_sq)
-        if sd_norm_sq is not None:
-            self._pending_sd_norm_sq = float(sd_norm_sq)
-        else:
-            # When observe is called with just (norm_sq, cos_value), we
-            # default to assuming the new RL norm squared is the dominant
-            # signal. SD-norm-squared is initialized from observation too,
-            # which keeps the API simple for tests but lets `commit` carry
-            # both sides for the actual loop.
-            self._pending_sd_norm_sq = float(norm_sq)
-        self._pending_cos = float(cos_value)
-        self._has_pending = True
+    # Compatibility accessors used by older hooks/tests.
+    @property
+    def _pending_rl_norm_sq(self) -> float:
+        return self.pending_means()[0]
+
+    @property
+    def _pending_sd_norm_sq(self) -> float:
+        return self.pending_means()[1]
+
+    @property
+    def _pending_cos(self) -> float:
+        return self.pending_means()[2]
+
+    @property
+    def _has_pending(self) -> bool:
+        return self._pending_count > 0
 
-    def commit(
+    def observe(
         self,
-        rl_grad_norm_sq: float,
-        sd_grad_norm_sq: float,
+        norm_sq: float,
         cos_value: float,
+        sd_norm_sq: float | None = None,
     ) -> None:
-        """Promote a fresh observation into the committed EMA.
-
-        Called once per optimizer step (after `optimizer.step()`). Uses
-        the EMA decay formula:
+        self._pending_rl_sum += float(norm_sq)
+        self._pending_sd_sum += float(norm_sq if sd_norm_sq is None else sd_norm_sq)
+        self._pending_cos_sum += float(cos_value)
+        self._pending_count += 1
+
+    def pending_means(self) -> tuple[float, float, float]:
+        if self._pending_count == 0:
+            return 0.0, 0.0, 0.0
+        count = float(self._pending_count)
+        return (
+            self._pending_rl_sum / count,
+            self._pending_sd_sum / count,
+            self._pending_cos_sum / count,
+        )
 
-            ema_new = decay * ema_old + (1 - decay) * observation
+    def clear_pending(self) -> None:
+        self._pending_rl_sum = 0.0
+        self._pending_sd_sum = 0.0
+        self._pending_cos_sum = 0.0
+        self._pending_count = 0
 
-        If `rl_grad_norm_sq` is zero (the case when an example set
-        produced a constant gradient or the SD gate zeroed everything
-        out), the EMA just keeps its current value -- the document's
-        safe-fallback rule.
-        """
-        if rl_grad_norm_sq > 0.0:
+    def commit(
+        self,
+        rl_grad_norm_sq: float | None = None,
+        sd_grad_norm_sq: float | None = None,
+        cos_value: float | None = None,
+    ) -> None:
+        if rl_grad_norm_sq is None or sd_grad_norm_sq is None or cos_value is None:
+            rl_grad_norm_sq, sd_grad_norm_sq, cos_value = self.pending_means()
+        if float(rl_grad_norm_sq) > 0.0:
             self.rl_grad_norm_sq_ema = (
                 self.ema_decay * self.rl_grad_norm_sq_ema
                 + (1.0 - self.ema_decay) * float(rl_grad_norm_sq)
             )
-        if sd_grad_norm_sq > 0.0:
+        if float(sd_grad_norm_sq) > 0.0:
             self.sd_grad_norm_sq_ema = (
                 self.ema_decay * self.sd_grad_norm_sq_ema
                 + (1.0 - self.ema_decay) * float(sd_grad_norm_sq)
@@ -148,7 +119,7 @@
             self.ema_decay * self.cos_ema
             + (1.0 - self.ema_decay) * float(cos_value)
         )
-        self._has_pending = False
+        self.clear_pending()
 
     def state_dict(self) -> dict:
         return {
@@ -161,30 +132,18 @@
             "cos_ema": self.cos_ema,
         }
 
-    def load_state_dict(self, sd: dict) -> None:
-        for key, value in sd.items():
+    def load_state_dict(self, state: dict) -> None:
+        for key, value in state.items():
             setattr(self, key, value)
-        self._has_pending = False
+        self.clear_pending()
 
 
 def compute_lambda_effective(state: VacsAdaptiveState, base_lambda: float) -> float:
-    """Compute the effective SD coefficient for the next optimizer step.
-
-    Document rule:
-      variance_factor = clip(sqrt(EMA|g_rl|^2 / (EMA|g_sd|^2 + eps)), min, max)
-      cosine_factor   = (1 + clip(cos_ema, -1, 1)) / 2
-      lambda_eff      = base_lambda * variance_factor * cosine_factor
-
-    If `state.disabled`, returns `base_lambda` unchanged -- the entire
-    adaptive mixing block becomes a no-op and VACS reduces to a fixed-coef
-    RL + SD combination.
-    """
     if state.disabled:
         return float(base_lambda)
     eps = 1e-12
     ratio = state.rl_grad_norm_sq_ema / (state.sd_grad_norm_sq_ema + eps)
     raw = math.sqrt(max(ratio, 0.0))
     multiplier = max(state.multiplier_min, min(state.multiplier_max, raw))
-    cos_clip = max(-1.0, min(1.0, state.cos_ema))
-    cosine_factor = (1.0 + cos_clip) / 2.0
+    cosine_factor = (1.0 + max(-1.0, min(1.0, state.cos_ema))) / 2.0
     return float(base_lambda) * float(multiplier) * float(cosine_factor)
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/vacs_step.py /mnt/data/crisp_work/CRISP-main/crisp/training/vacs_step.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/vacs_step.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/vacs_step.py	2026-08-02 12:57:14.319727119 +0000
@@ -10,9 +10,9 @@
 
   1. Token-level VACS advantages via `vacs_token_advantages` (sign(a)-
      aware exponential credit, clipped, mixed by rho), used directly in
-     a plain per-token REINFORCE loss -- not the sequence-mean
-     PPO-clipped RL term CRISP uses (the doc's boxed RL formula for
-     VACS has no ratio/clip).
+     a plain per-token REINFORCE loss. CRISP also uses a single-pass
+     policy-gradient loss; VACS differs through the token-level credit
+     and gradient-coupling rules, not through PPO clipping.
   2. Soft reward gating via `soft_reward_gate`, not the hard `< 1.0`
      correctness gate. The plan calls out the soft gate as VACS's
      smooth version of the CRISP gate.
@@ -104,6 +104,7 @@
     credit_mix: float,
     use_token_credit: bool,
     use_soft_gate: bool,
+    advantages: torch.Tensor | None = None,
     use_asymmetric_projection: bool = True,
     state: Optional[VacsAdaptiveState] = None,
 ) -> VacsComponents:
@@ -138,7 +139,9 @@
     student_token_logps_2d = _stack(scored.student_token_logps)
 
     # ---- RL token-level advantages.
-    advantages = normalize_advantages(rewards, eps=adv_eps).to(model.device)
+    if advantages is None:
+        advantages = normalize_advantages(rewards, eps=adv_eps)
+    advantages = advantages.to(model.device)
     # `_stack` right-pads variable-length rows to a common width with
     # zeros; a mask over the true (unpadded) response lengths is needed
     # before any per-sequence mean, or shorter rollouts get diluted by
@@ -283,6 +286,7 @@
     credit_mix: float,
     use_token_credit: bool,
     use_soft_gate: bool,
+    advantages: torch.Tensor | None = None,
     use_asymmetric_projection: bool = True,
     state: Optional[VacsAdaptiveState] = None,
 ) -> VacsStepResult:
@@ -304,6 +308,7 @@
         params=params,
         rewards=rewards,
         base_lambda=base_lambda,
+        advantages=advantages,
         adv_eps=adv_eps,
         clip_epsilon=clip_epsilon,
         sd_weight=sd_weight,
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/training/vacs_train.py /mnt/data/crisp_work/CRISP-main/crisp/training/vacs_train.py
--- /mnt/data/crisp_original/CRISP-main/crisp/training/vacs_train.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/training/vacs_train.py	2026-08-02 12:54:59.363723587 +0000
@@ -1,25 +1,4 @@
-"""VACS-CRISP training entry point.
-
-`python train_launcher.py vacs --config config/vacs_7b.yaml`
-
-Builds on the shared common_loop.py -- does not fork the training
-infrastructure. The VACS-specific work lives in two places:
-
-  - `vacs_step_from_arrays` does one micro-batch's forward + loss +
-    asymmetric gradient surgery. This module owns the orchestrating
-    state (rollouts, sequence construction, reward computation) and
-    delegates the math to crisp.training.vacs_step.
-
-  - The outer training loop's `post_optimizer_step` hook is where
-    VacsAdaptiveState is committed -- the loop already calls hooks once
-    per optimizer step, which is exactly when VACS needs to update its
-    EMA (so a micro-batch cannot influence the coefficient used in its
-    own optimizer step).
-
-  - The loop's `checkpoint_state` hook returns the VacsAdaptiveState
-    serialized payload so the next run can resume with the same
-    adaptive mix.
-"""
+"""VACS-CRISP training entry point using effective-batch normalization."""
 from __future__ import annotations
 
 import argparse
@@ -30,184 +9,178 @@
 from crisp.data.build_dataset import load_train_dataset
 from crisp.data.reward import build_reward_fn
 from crisp.eval.run_eval import make_eval_fn
-from crisp.model.load import attach_lora, load_model_and_tokenizer
+from crisp.model.load import attach_or_load_lora, load_model_and_tokenizer
+from crisp.training.batch_types import CollectedBatch
 from crisp.training.common_loop import run_training_loop
-from crisp.training.crisp_step import build_teacher_student_sequences
 from crisp.training.rollout import generate_rollouts
+from crisp.training.teacher_student import build_teacher_student_sequences
 from crisp.training.vacs_state import VacsAdaptiveState
-from crisp.training.vacs_step import (
-    commit_vacs_state,
-    vacs_step_from_arrays,
-)
+from crisp.training.vacs_step import vacs_step_from_arrays
 from crisp.utils.config import load_config
+from crisp.utils.distributed import all_reduce_scalar, init_distributed
 from crisp.utils.seeding import set_seed
 
 
-def vacs_step_fn_factory(model, tokenizer, params, cfg, state):
-    """Build a `step_fn(model, tokenizer, prompts, answers, global_step)`
-    closure matching the common-loop contract: returns (grads, metrics).
-
-    The closure captures the trained model, tokenizer, trainable params,
-    config, and VACS adaptive state so each call from the loop can
-    produce rollouts + scores + VACS gradients without re-reading them.
-    """
-    sys_prompt = cfg.model.system_prompt
-
-    # Convenience: clip / advantage / mixing / ablation knobs.
-    adv_eps = cfg.training.adv_eps
-    clip_epsilon = cfg.training.clip_epsilon
-    sd_weight = cfg.vacs.sd_weight
-    soft_gate_slope = cfg.vacs.soft_gate_slope
-    reward_threshold = cfg.vacs.reward_threshold
-    credit_mix = cfg.vacs.token_credit_mix
-    use_token_credit = bool(credit_mix > 0.0)
-    use_soft_gate = True
-    use_asymmetric_projection = True
-    base_lambda = cfg.training.lambda_max
-
-    # Apply adaptive-mix disable flag once at factory time (cheap, idempotent).
-    if not cfg.vacs.adaptive_mix.enabled:
-        state.disabled = True
-
-    def step_fn(model_, tokenizer_, prompts, answers, global_step):
-        model_.eval()
-        n = len(prompts)
-
-        # 1. Sample on-policy rollouts from the STUDENT context.
-        rollouts = generate_rollouts(
-            model_, tokenizer_, prompts, sys_prompt,
-            max_prompt_length=cfg.rollout.max_prompt_length,
-            max_new_tokens=cfg.rollout.max_new_tokens,
-            temperature=cfg.rollout.temperature, top_p=cfg.rollout.top_p,
-            do_sample=cfg.rollout.do_sample, num_return_sequences=1,
-        )
+def collect_vacs_batch(model, tokenizer, prompts, answers, reward_fn, cfg) -> CollectedBatch:
+    model.eval()
+    rollouts = generate_rollouts(
+        model,
+        tokenizer,
+        prompts,
+        cfg.model.system_prompt,
+        max_prompt_length=cfg.rollout.max_prompt_length,
+        max_new_tokens=cfg.rollout.max_new_tokens,
+        temperature=cfg.rollout.temperature,
+        top_p=cfg.rollout.top_p,
+        do_sample=cfg.rollout.do_sample,
+        num_return_sequences=1,
+    )
+    rewards = torch.tensor(
+        [reward_fn(r.response_text, answer) for r, answer in zip(rollouts, answers)],
+        dtype=torch.float32,
+    )
+    batch = CollectedBatch(
+        list(prompts),
+        list(answers),
+        rollouts,
+        rewards,
+        metadata={
+            "generations": [
+                {"prompt": p, "response_text": r.response_text, "reward": float(reward)}
+                for p, r, reward in zip(prompts, rollouts, rewards.tolist())
+            ]
+        },
+    )
+    batch.validate()
+    return batch
 
-        # 2. Verifiable reward per rollout.
-        reward_fn = build_reward_fn(cfg.reward)
-        rewards_tensor = torch.tensor(
-            [reward_fn(r.response_text, a) for r, a in zip(rollouts, answers)],
-            dtype=torch.float32,
-        )
 
-        # 3. Build paired teacher+student sequences.
+def vacs_score_fn_factory(tokenizer, params, cfg, state):
+    def score_fn(model, tokenizer_, collected, advantages, global_step):
+        valid = [i for i, rollout in enumerate(collected.rollouts) if len(rollout.response_ids) > 0]
+        if not valid:
+            raise RuntimeError("Every VACS rollout was empty.")
+        prompts = [collected.prompts[i] for i in valid]
+        answers = [collected.answers[i] for i in valid]
+        rollouts = [collected.rollouts[i] for i in valid]
+        rewards = collected.rewards[valid]
+        advantages_valid = advantages[valid]
+
         teacher_seqs, student_seqs = build_teacher_student_sequences(
-            prompts, answers, rollouts, tokenizer=tokenizer_,
-            sys_prompt=sys_prompt, hint_template=cfg.data.teacher_hint_template,
+            prompts,
+            answers,
+            rollouts,
+            tokenizer=tokenizer_,
+            sys_prompt=cfg.model.system_prompt,
+            hint_template=cfg.data.teacher_hint_template,
         )
-
-        # 4. Run the VACS step end-to-end.
         result = vacs_step_from_arrays(
-            model=model_,
+            model=model,
             teacher_sequences=teacher_seqs,
             student_sequences=student_seqs,
             pad_id=tokenizer_.pad_token_id,
             params=params,
-            rewards=rewards_tensor,
-            base_lambda=base_lambda,
-            adv_eps=adv_eps,
-            clip_epsilon=clip_epsilon,
-            sd_weight=sd_weight,
-            soft_gate_slope=soft_gate_slope,
-            reward_threshold=reward_threshold,
-            credit_mix=credit_mix,
-            use_token_credit=use_token_credit,
-            use_soft_gate=use_soft_gate,
-            use_asymmetric_projection=use_asymmetric_projection,
+            rewards=rewards,
+            base_lambda=cfg.training.lambda_max,
+            adv_eps=cfg.training.adv_eps,
+            clip_epsilon=cfg.vacs.get("token_weight_epsilon", cfg.training.clip_epsilon),
+            sd_weight=cfg.vacs.sd_weight,
+            soft_gate_slope=cfg.vacs.soft_gate_slope,
+            reward_threshold=cfg.vacs.reward_threshold,
+            credit_mix=cfg.vacs.token_credit_mix,
+            use_token_credit=bool(cfg.vacs.token_credit_mix > 0.0),
+            use_soft_gate=bool(cfg.vacs.get("use_soft_gate", True)),
+            advantages=advantages_valid,
+            use_asymmetric_projection=bool(cfg.vacs.get("use_asymmetric_projection", True)),
             state=state,
         )
-        # Aggregate step metrics: average over micro-batches inside the
-        # common loop already, so just return the latest metrics dict
-        # plus reward/response stats.
-        step_metrics = dict(result.metrics)
-        step_metrics["reward_mean"] = float(rewards_tensor.mean().item())
-        step_metrics["reward_std"] = float(rewards_tensor.std(unbiased=False).item())
-        step_metrics["response_len_mean"] = (
-            sum(len(r.response_ids) for r in rollouts) / max(n, 1)
-        )
-        step_metrics["_generations"] = [
-            {"prompt": p, "response_text": r.response_text, "reward": float(rw)}
-            for p, r, rw in zip(prompts, rollouts, rewards_tensor.tolist())
-        ]
-        return result.gradients, step_metrics
+        metrics = dict(result.metrics)
+        metrics.update({
+            "reward_mean": float(rewards.mean().item()),
+            "reward_std": float(rewards.std(unbiased=False).item()),
+            "response_len_mean": sum(len(r.response_ids) for r in rollouts) / len(rollouts),
+            "empty_response_fraction": 1.0 - len(valid) / max(len(collected.rollouts), 1),
+            "advantage_abs_mean": float(advantages_valid.abs().mean().item()),
+        })
+        return result.gradients, metrics
 
-    return step_fn
+    return score_fn
 
 
 def main(config_path: str, overrides: Optional[dict] = None) -> None:
     cfg = load_config(config_path, overrides)
+    init_distributed()
     set_seed(cfg.seed)
 
     model, tokenizer = load_model_and_tokenizer(cfg.model)
-    model, _is_peft = attach_lora(model, cfg.lora)
+    initial_adapter = cfg.model.get("initial_adapter") or cfg.training.get("resume_from")
+    model, _ = attach_or_load_lora(model, cfg.lora, initial_adapter=initial_adapter)
     params = [p for p in model.parameters() if p.requires_grad]
     if not params:
-        raise RuntimeError(
-            "No trainable parameters found. If lora.enabled is false this does a full "
-            "finetune and every parameter should already require grad; if lora.enabled is "
-            "true, check that attach_lora actually ran (see crisp/model/load.py)."
-        )
+        raise RuntimeError("No trainable parameters found.")
 
     train_dataset = load_train_dataset(
         cfg.data.train_dataset,
         split=cfg.data.train_split,
         train_subset_size=cfg.data.train_subset_size,
     )
-    eval_fn = make_eval_fn(cfg) if cfg.data.eval_datasets else None
-
-    # VACS adaptive mixing state. Survives across optimizer steps via
-    # the post_optimizer_step lifecycle hook (so it observes the
-    # JUST-STEPPED gradient norms and computes lambda_eff for the NEXT
-    # step from the COMMITTED EMA -- never the current micro-batch).
+    reward_fn = build_reward_fn(cfg.reward)
     state = VacsAdaptiveState(
         ema_decay=cfg.vacs.adaptive_mix.ema_decay,
         multiplier_min=cfg.vacs.adaptive_mix.multiplier_min,
         multiplier_max=cfg.vacs.adaptive_mix.multiplier_max,
         disabled=not cfg.vacs.adaptive_mix.enabled,
     )
-    step_fn = vacs_step_fn_factory(model, tokenizer, params, cfg, state)
+
+    def collect_fn(model_, tokenizer_, prompts, answers, global_step):
+        return collect_vacs_batch(model_, tokenizer_, prompts, answers, reward_fn, cfg)
+
+    score_fn = vacs_score_fn_factory(tokenizer, params, cfg, state)
 
     def post_optimizer_step(model_, aggregated_step_metrics):
-        # The just-computed step_fn already wrote into `state._pending`
-        # (via vacs_step_from_arrays -> state.observe). Promote the
-        # pending observation to the committed EMA. With grad_accum > 1
-        # the last micro-batch's statistics are the ones committed here
-        # (which is what the state object intends).
-        rl_norm_sq = float(state._pending_rl_norm_sq)
-        sd_norm_sq = float(state._pending_sd_norm_sq)
-        cos_value = float(state._pending_cos)
-        if rl_norm_sq > 0.0 or sd_norm_sq > 0.0 or cos_value != 0.0:
-            commit_vacs_state(
-                state,
-                rl_norm_sq=rl_norm_sq,
-                sd_norm_sq=sd_norm_sq,
+        if state._has_pending:
+            # Every rank must use the same adaptive coefficient on the next
+            # optimizer step. Aggregate all micro-batch statistics globally
+            # before committing the EMA state.
+            rl_sq, sd_sq, cos_value = state.pending_means()
+            device = next(model_.parameters()).device
+            rl_sq = all_reduce_scalar(rl_sq, device)
+            sd_sq = all_reduce_scalar(sd_sq, device)
+            cos_value = all_reduce_scalar(cos_value, device)
+            state.commit(
+                rl_grad_norm_sq=rl_sq,
+                sd_grad_norm_sq=sd_sq,
                 cos_value=cos_value,
             )
 
     def checkpoint_state() -> dict:
         return {"vacs_adaptive_state": state.state_dict()}
 
+    def restore_method_state(payload: dict) -> None:
+        if payload and "vacs_adaptive_state" in payload:
+            state.load_state_dict(payload["vacs_adaptive_state"])
+
     run_training_loop(
         model=model,
         tokenizer=tokenizer,
         params=params,
         train_dataset=train_dataset,
-        step_fn=step_fn,
+        step_fn=None,
+        collect_fn=collect_fn,
+        score_fn=score_fn,
         cfg=cfg,
         run_name=cfg.logging.run_name or "vacs",
-        eval_fn=eval_fn,
+        eval_fn=make_eval_fn(cfg) if cfg.data.eval_datasets else None,
         post_optimizer_step=post_optimizer_step,
         checkpoint_state=checkpoint_state,
+        restore_method_state=restore_method_state,
     )
 
 
 if __name__ == "__main__":
     parser = argparse.ArgumentParser()
     parser.add_argument("--config", required=True)
-    parser.add_argument(
-        "--override", action="append", default=[],
-        metavar="key.path=value",
-        help="override a config value; repeatable",
-    )
+    parser.add_argument("--override", action="append", default=[])
     args = parser.parse_args()
     from train_launcher import parse_overrides
     main(args.config, overrides=parse_overrides(args.override))
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/utils/config.py /mnt/data/crisp_work/CRISP-main/crisp/utils/config.py
--- /mnt/data/crisp_original/CRISP-main/crisp/utils/config.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/utils/config.py	2026-08-02 12:42:51.375704536 +0000
@@ -97,12 +97,14 @@
         # succeeded, so a flaky connection can never strand a training run
         # mid-step. See the README's "Model download is slow or hangs" section.
         "local_files_only": False,
+        # Optional LoRA adapter used as the trainable initialization (e.g. SFT -> GRPO).
+        "initial_adapter": None,
     },
     "lora": {
         "enabled": True,
         "r": 64,
         "alpha": 128,
-        "dropout": 0.05,
+        "dropout": 0.0,
         "target_modules": ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"],
     },
     "optimizer": {
@@ -119,6 +121,10 @@
         "grad_accum_steps": 1,
         "clip_epsilon": 0.2,
         "lambda_max": 1.0,
+        "lambda_schedule": "cosine",
+        "use_pcgrad": True,
+        "correctness_gate": True,
+        "resume_from": None,
         "adv_eps": 1.0e-8,
         "pcgrad_eps": 1.0e-12,
         "save_every": 200,
@@ -168,6 +174,16 @@
         "eval_subset_size": None,
         "n_bootstrap": 1000,
         "max_new_tokens": 512,
+        "bootstrap_seed": 0,
+    },
+    "sft": {
+        "max_sequence_length": 2048,
+        "num_train_epochs": 1.0,
+        "completion_only_loss": True,
+    },
+    "distributed": {
+        "enabled": False,
+        "sync_task_gradients_before_surgery": True,
     },
     "vacs": {
         "token_credit_mix": 0.25,
@@ -175,6 +191,8 @@
         "sd_weight": 1.0,
         "soft_gate_slope": 12.0,
         "reward_threshold": 0.5,
+        "use_soft_gate": True,
+        "use_asymmetric_projection": True,
         "adaptive_mix": {
             "enabled": True,
             "ema_decay": 0.95,
@@ -195,6 +213,7 @@
         "ib_reduction": "sequence_sum",
         "soft_gate_slope": 12.0,
         "reward_threshold": 0.5,
+        "use_soft_gate": True,
         "anchor_alpha": 0.01,
         "anchor_ema_decay": 0.995,
         "anchor_storage": "same",
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/utils/device.py /mnt/data/crisp_work/CRISP-main/crisp/utils/device.py
--- /mnt/data/crisp_original/CRISP-main/crisp/utils/device.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/utils/device.py	2026-08-02 12:43:35.919705702 +0000
@@ -38,7 +38,8 @@
     if os.environ.get("CRISP_FORCE_CPU", "") == "1":
         return torch.device("cpu")
     if torch.cuda.is_available():
-        return torch.device("cuda")
+        local_rank = int(os.environ.get("LOCAL_RANK", "0"))
+        return torch.device(f"cuda:{local_rank}")
     mps = getattr(torch.backends, "mps", None)
     if mps is not None and mps.is_available():
         return torch.device("mps")
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/crisp/utils/distributed.py /mnt/data/crisp_work/CRISP-main/crisp/utils/distributed.py
--- /mnt/data/crisp_original/CRISP-main/crisp/utils/distributed.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/crisp/utils/distributed.py	2026-08-02 12:41:56.123703090 +0000
@@ -0,0 +1,82 @@
+"""Small torch.distributed helpers for manual-gradient training.
+
+CRISP and VACS use ``torch.autograd.grad`` plus gradient surgery, so wrapping
+the model in DDP is not sufficient by itself. This module instead synchronizes
+reward statistics and final per-parameter gradients explicitly.
+"""
+from __future__ import annotations
+
+import os
+from datetime import timedelta
+
+import torch
+import torch.distributed as dist
+
+
+def init_distributed() -> bool:
+    world = int(os.environ.get("WORLD_SIZE", "1"))
+    if world <= 1:
+        return False
+    if not dist.is_available():
+        raise RuntimeError("WORLD_SIZE>1 but torch.distributed is unavailable.")
+    local_rank = int(os.environ.get("LOCAL_RANK", "0"))
+    if torch.cuda.is_available():
+        torch.cuda.set_device(local_rank)
+    if not dist.is_initialized():
+        backend = "nccl" if torch.cuda.is_available() else "gloo"
+        dist.init_process_group(backend=backend, timeout=timedelta(minutes=30))
+    return True
+
+
+def is_initialized() -> bool:
+    return dist.is_available() and dist.is_initialized()
+
+
+def rank() -> int:
+    return dist.get_rank() if is_initialized() else 0
+
+
+def world_size() -> int:
+    return dist.get_world_size() if is_initialized() else 1
+
+
+def local_rank() -> int:
+    return int(os.environ.get("LOCAL_RANK", "0")) if is_initialized() else 0
+
+
+def is_main_process() -> bool:
+    return rank() == 0
+
+
+def barrier() -> None:
+    if is_initialized():
+        dist.barrier()
+
+
+def gather_1d_equal(tensor: torch.Tensor) -> torch.Tensor:
+    """All-gather equal-length 1-D tensors and concatenate them."""
+    if not is_initialized():
+        return tensor
+    if tensor.dim() != 1:
+        raise ValueError(f"Expected a 1-D tensor, got {tuple(tensor.shape)}")
+    gathered = [torch.empty_like(tensor) for _ in range(world_size())]
+    dist.all_gather(gathered, tensor.contiguous())
+    return torch.cat(gathered, dim=0)
+
+
+def all_reduce_gradients(grads: list[torch.Tensor]) -> list[torch.Tensor]:
+    if not is_initialized():
+        return grads
+    size = float(world_size())
+    for grad in grads:
+        dist.all_reduce(grad, op=dist.ReduceOp.SUM)
+        grad.div_(size)
+    return grads
+
+
+def all_reduce_scalar(value: float, device: torch.device | str) -> float:
+    t = torch.tensor(float(value), device=device, dtype=torch.float64)
+    if is_initialized():
+        dist.all_reduce(t, op=dist.ReduceOp.SUM)
+        t.div_(world_size())
+    return float(t.item())
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/docs/ablations.md /mnt/data/crisp_work/CRISP-main/docs/ablations.md
--- /mnt/data/crisp_original/CRISP-main/docs/ablations.md	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/docs/ablations.md	2026-08-02 12:56:36.527726130 +0000
@@ -31,7 +31,10 @@
   project.
 - `reinforce_pp`: REINFORCE++ baseline (no self-distillation).
 - `opsd`: OPSD baseline (self-distillation only, no RL).
-- `grpo_k1`, `grpo_k8`: GRPO with k=1 / k=8 rollouts per prompt.
+- `grpo_k1`: a single-rollout, global-normalized policy-gradient budget
+  control. It is retained under the historical manifest key but is not
+  described as genuine group-relative GRPO.
+- `grpo_k8`: genuine GRPO with eight rollouts per prompt.
 - `crisp_lambda_sweep`: CRISP with `lambda_max = 0.5` instead of
   the default 1.0. The plan calls out that the original CRISP's
   sensitivity to lambda is itself worth characterising.
@@ -52,15 +55,16 @@
   contributes a measurable share of the gains; without it, VACS
   should look more like CRISP. **Constraint**: only one knob
   changed; other knobs still at the full-variant defaults.
-- `no_soft_gate`: `vacs.soft_gate_slope: 0.0`. The gate collapses
-  to 1.0 for every rollout. **Hypothesis**: the smooth sigmoid
+- `no_soft_gate`: `vacs.use_soft_gate: false`. The gate is explicitly
+  replaced by 1.0 for every rollout. **Hypothesis**: the smooth sigmoid
   gate is necessary; a hard "always on" gate should look like
   plain CRISP SD loss.
 - `fixed_mixing`: `vacs.adaptive_mix.enabled: false`. `lambda_eff =
   lambda_max` constant. **Hypothesis**: the variance ratio + cosine
   scaling of the SD coefficient help; without them, VACS should
   look like CRISP at the same lambda.
-- `no_asym_surgery`: no asymmetric PC-Grad. **Hypothesis**:
+- `no_asym_surgery`: `vacs.use_asymmetric_projection: false`; gradients
+  are added without projection. **Hypothesis**:
   protecting the RL direction is necessary; without it, the
   expected gain disappears.
 - `no_sd`: `vacs.sd_weight: 0.0`. Reduces to plain RL with token
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/docs/extended_experiments.md /mnt/data/crisp_work/CRISP-main/docs/extended_experiments.md
--- /mnt/data/crisp_original/CRISP-main/docs/extended_experiments.md	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/docs/extended_experiments.md	2026-08-02 12:51:26.427718015 +0000
@@ -0,0 +1,71 @@
+# Extended experiment support
+
+## Matched paper configuration
+
+`config/paper_7b.yaml` is the canonical shared configuration for all RL/SD
+methods. It keeps model, data, optimizer, rollout budget, evaluation suite,
+and effective batch settings fixed. Only method-specific sections should be
+overridden.
+
+## HMMT February 2025
+
+Add `hmmt2025` to `data.eval_datasets`. The loader uses
+`MathArena/hmmt_feb_2025` and normalizes the `problem` and `answer` fields.
+
+## SFT -> GRPO / CRISP / VACS / CIBO
+
+```bash
+python train_launcher.py sft --config config/sft_7b.yaml
+python train_launcher.py grpo --config config/paper_7b.yaml --k 8 \
+  --override model.initial_adapter=runs/sft_7b/final
+```
+
+The same `model.initial_adapter` override works for CRISP, VACS, CIBO, OPSD,
+and REINFORCE++. It loads the existing adapter as trainable instead of
+stacking a fresh adapter.
+
+## HumanEval+
+
+Merge an adapter, install the official EvalPlus package, then run the wrapper:
+
+```bash
+python scripts/merge_adapter.py \
+  --base-model Qwen/Qwen2.5-Math-7B-Instruct \
+  --adapter runs/crisp/final \
+  --output merged/crisp
+pip install -U evalplus
+python scripts/eval_humaneval_plus.py --model merged/crisp --backend hf
+```
+
+## LiveCodeBench
+
+Use the official LiveCodeBench environment and a pinned release. After merging
+the adapter:
+
+```bash
+python scripts/eval_livecodebench.py \
+  --local-model-path merged/crisp \
+  --model-style qwen \
+  --release-version release_v6
+```
+
+## Model scaling
+
+The `config/scaling/` directory contains matched Qwen2.5-Instruct configs at
+1.5B, 3B, 7B, and 14B. Run the reduced matrix first:
+
+```bash
+python scripts/run_scaling.py --methods reinforce_pp,crisp,cibo-v2 --seeds 0,1,2
+```
+
+## Distributed training
+
+The manual-gradient loop supports `torchrun`. Each rank collects a disjoint
+data shard, reward statistics are gathered globally, and final gradients are
+averaged explicitly. CRISP/VACS currently perform gradient surgery locally
+and then average the final gradients; report this choice in the paper.
+
+```bash
+NUM_GPUS=2 METHOD=crisp CONFIG=config/paper_7b.yaml \
+  bash scripts/launch_distributed.sh
+```
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/docs/methods.md /mnt/data/crisp_work/CRISP-main/docs/methods.md
--- /mnt/data/crisp_original/CRISP-main/docs/methods.md	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/docs/methods.md	2026-08-02 12:57:14.319727119 +0000
@@ -114,9 +114,10 @@
   no ratio/clip, unlike CRISP's own RL term).
 - `loss_ib = sum_t forward_kl(q_{i,t} || p_{i,t})`
   where `q_{i,t}` is detached.
-- `loss_anchor = -mean_t EMA_target_logp(student_response_tokens)`
-  computed under `ema_target_forward` (which swaps the live
-  LoRA params for the EMA values inside `torch.no_grad()`).
+- `loss_anchor = H(p_EMA, p_live)` over the full response-token
+  vocabulary distributions. The EMA target is computed under
+  `ema_target_forward` and detached; gradients flow only through the
+  live student distribution.
 - `loss_total = loss_rl + beta * loss_ib + alpha * loss_anchor`.
 
 One single `compute_grads(loss_total, params)` call; the optimizer
@@ -146,18 +147,17 @@
 The controller holds a running reference KL EMA (frozen after a
 warm-up window) plus fast reward/KL EMAs, and updates `beta` once
 per optimizer step (not per micro-batch) per doc Section 7's
-rate-distortion rule -- a ratio of reward-improvement to
-leakage-change, not a KL-only signal:
+bounded sign controller comparing leakage change with reward change:
 
 - `baselined_kl = kl_ema - reference_kl`
 - `delta_reward = reward_ema - reward_ema_prev_step`
 - `delta_baselined_kl = baselined_kl - baselined_kl_prev_step`
-- `ratio = delta_reward / (delta_baselined_kl + epsilon)`
-- `beta = clip(beta * (1 + step_size * sign(ratio - 1)), beta_min, beta_max)`
+- `signal = delta_baselined_kl - delta_reward`
+- `beta = clip(beta * (1 + step_size * sign(signal)), beta_min, beta_max)`
 
-Only the *sign* of `(ratio - 1)` drives the update, so a noisy or
-near-zero denominator (guarded by `epsilon`) can only ever produce a
-small, bounded step. No update is attempted until there are two
+Only the sign of the baselined difference drives the update, and a small
+tolerance suppresses numerical-noise updates. The multiplicative step is
+bounded by `beta_min` and `beta_max`. No update is attempted until there are two
 post-warm-up observations to form deltas from.
 
 The reference EMA is built during a warm-up window
@@ -176,8 +176,8 @@
 - `cibo/beta_reference_kl` — the controller's reference EMA.
 - `cibo/baselined_kl` — current KL minus reference.
 - `cibo/credit_multiplier_mean` — mean of `1 + lambda * tanh(c_t)`.
-- `cibo/ib_gate_mean` — mean soft gate applied to IB (1.0 if
-  slope=0).
+- `cibo/ib_gate_mean` — mean gate applied to IB (1.0 when
+  `use_soft_gate: false`).
 - `cibo/kl_per_token_mean` — average per-token KL.
 - `cibo/anchor_alpha`, `cibo/ema_decay` — the anchor config
   values, logged once.
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/experiments/ablations.yaml /mnt/data/crisp_work/CRISP-main/experiments/ablations.yaml
--- /mnt/data/crisp_original/CRISP-main/experiments/ablations.yaml	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/experiments/ablations.yaml	2026-08-02 12:49:44.451715346 +0000
@@ -1,303 +1,381 @@
-# experiments/ablations.yaml
-#
-# Reproducible ablation manifest for VACS-CRISP and CIBO-CRISP v2.
-#
-# Every variant in every suite defines:
-#   method       -- one of SUPPORTED_METHODS in train_launcher.py
-#   base_config  -- a *.yaml file under config/
-#   overrides    -- a nested dict of dot-path key/value pairs. The
-#                   manifest unit test enforces that every key in
-#                   overrides is on the method's allow-list, so each
-#                   ablation differs from the full variant by exactly
-#                   one factor at a time.
-#   metric_keys  -- the run logger keys this variant is expected to
-#                   emit. Used by the runner's post-run sanity check.
-#   slug         -- a short identifier used in the run directory name
-#                   (combined with the method and seed).
-#   description  -- human-readable explanation of what the ablation
-#                   isolates and what the comparison tests for.
-#
-# Seeds are NOT stored in each variant -- the runner injects them at
-# invocation time, which makes a single suite cleanly reproducible
-# across multiple seeds without editing the manifest.
-#
-# Comparability discipline:
-#   * CIBO fixed-beta and CIBO adaptive-beta live in separate
-#     suites. A comparison between them uses matched controls within
-#     each suite, not cross-suite mixing.
-#   * The "full" variant in each suite is the one the ablation is
-#     measured against; it MUST use the suite's documented method
-#     and config.
-
-# ---------------------------------------------------------------------------
-# Suite: controls -- baseline methods the VACS and CIBO results are
-# benchmarked against. The plan requires CRISP, REINFORCE++, OPSD, and
-# GRPO (k=1 and k=8) to be available. Existing baselines already live
-# in the train_launcher, so this suite is just a routing entry point.
-# ---------------------------------------------------------------------------
 controls:
-  description: >
-    Baseline methods the new VACS and CIBO results are benchmarked
-    against. The lambda sweep on crisp is a one-factor ablation over
-    the original CRISP method; everything else is a single-config
-    baseline.
+  description: 'Baseline methods the new VACS and CIBO results are benchmarked against.
+    The lambda sweep on crisp is a one-factor ablation over the original CRISP method;
+    everything else is a single-config baseline.
+
+    '
   full_variant: crisp
   variants:
     crisp:
       method: crisp
       base_config: crisp_7b
       slug: crisp
-      description: "Original CRISP -- symmetric PC-Grad on RL + correctness-gated forward KL."
-      metric_keys: [loss_rl, loss_sd, lambda, grad_conflicted, reward_mean]
+      description: Original CRISP -- symmetric PC-Grad on RL + correctness-gated forward
+        KL.
+      metric_keys:
+      - loss_rl
+      - loss_sd
+      - lambda
+      - grad_conflicted
+      - reward_mean
       overrides: {}
     reinforce_pp:
       method: reinforce_pp
       base_config: crisp_7b
       slug: reinforce_pp
-      description: "REINFORCE++ baseline (no self-distillation)."
-      metric_keys: [loss, reward_mean]
+      description: REINFORCE++ baseline (no self-distillation).
+      metric_keys:
+      - loss_rl
+      - reward_mean
       overrides: {}
     opsd:
       method: opsd
       base_config: crisp_7b
       slug: opsd
-      description: "OPSD baseline (self-distillation only, no RL branch)."
-      metric_keys: [loss_sd, reward_mean]
+      description: OPSD baseline (self-distillation only, no RL branch).
+      metric_keys:
+      - loss_sd
+      - reward_mean
       overrides: {}
     grpo_k1:
       method: grpo
       base_config: crisp_7b
       slug: grpo_k1
-      description: "GRPO with k=1 rollouts per prompt."
-      metric_keys: [loss, reward_mean]
-      overrides: {}
+      description: Single-rollout global-PG budget control (not genuine group-relative
+        GRPO).
+      metric_keys:
+      - loss_rl
+      - reward_mean
+      - grpo_is_group_relative
+      overrides:
+        rollout:
+          k: 1
     grpo_k8:
       method: grpo
       base_config: crisp_7b
       slug: grpo_k8
-      description: "GRPO with k=8 rollouts per prompt (the typical setting)."
-      metric_keys: [loss, reward_mean]
-      overrides: {rollout: {k: 8}}
+      description: GRPO with k=8 rollouts per prompt (the typical setting).
+      metric_keys:
+      - loss_rl
+      - reward_mean
+      - grpo_is_group_relative
+      overrides:
+        rollout:
+          k: 8
     crisp_lambda_sweep:
       method: crisp
       base_config: crisp_7b
       slug: crisp_lambda_sweep
-      description: >
-        CRISP lambda sweep -- one ablation in the controls suite
-        showing the original method's sensitivity to the SD
-        coefficient. Not a VACS / CIBO comparison; included so
-        existing CRISP behavior is itself characterised.
-      metric_keys: [lambda, loss_rl, loss_sd, grad_conflicted]
-      overrides: {training: {lambda_max: 0.5}}
-
-# ---------------------------------------------------------------------------
-# Suite: vacs-core -- the VACS full method and one-factor ablations.
-# The full variant in this suite uses config/vacs_7b.yaml (rho=0.25,
-# soft gate on, adaptive mixing on, asymmetric surgery on, sd_weight=1).
-# Every other variant differs in exactly one knob.
-# ---------------------------------------------------------------------------
+      description: 'CRISP lambda sweep -- one ablation in the controls suite showing
+        the original method''s sensitivity to the SD coefficient. Not a VACS / CIBO
+        comparison; included so existing CRISP behavior is itself characterised.
+
+        '
+      metric_keys:
+      - lambda
+      - loss_rl
+      - loss_sd
+      - grad_conflicted
+      overrides:
+        training:
+          lambda_max: 0.5
 vacs-core:
-  description: >
-    VACS full method vs one-factor ablations. Each ablation toggles
-    exactly one VACS component: token credit, soft gate, adaptive
-    mixing, asymmetric surgery, or self-distillation. Comparing
-    each ablation to the full variant isolates that component's
-    contribution to the final loss.
+  description: 'VACS full method vs one-factor ablations. Each ablation toggles exactly
+    one VACS component: token credit, soft gate, adaptive mixing, asymmetric surgery,
+    or self-distillation. Comparing each ablation to the full variant isolates that
+    component''s contribution to the final loss.
+
+    '
   full_variant: full
   variants:
     full:
       method: vacs
       base_config: vacs_7b
       slug: vacs-full
-      description: >
-        Full VACS: token credit (rho=0.25), soft gate (slope 12,
-        threshold 0.5), adaptive mixing (EMA decay 0.95, multiplier
-        clamp [0.1, 5.0]), asymmetric PC-Grad, self-distillation
-        weight 1.0.
-      metric_keys: [vacs/loss_rl, vacs/loss_sd, vacs/lambda_eff,
-                   vacs/cos_rl_sd, vacs/conflicted, vacs/rl_grad_norm,
-                   vacs/sd_grad_norm, reward_mean, response_len_mean]
+      description: 'Full VACS: token credit (rho=0.25), soft gate (slope 12, threshold
+        0.5), adaptive mixing (EMA decay 0.95, multiplier clamp [0.1, 5.0]), asymmetric
+        PC-Grad, self-distillation weight 1.0.
+
+        '
+      metric_keys:
+      - vacs/loss_rl
+      - vacs/loss_sd
+      - vacs/lambda_eff
+      - vacs/cos_rl_sd
+      - vacs/conflicted
+      - vacs/rl_grad_norm
+      - vacs/sd_grad_norm
+      - reward_mean
+      - response_len_mean
       overrides: {}
     no_token_credit:
       method: vacs
       base_config: vacs_7b
       slug: vacs-no-token-credit
-      description: >
-        VACS with token credit disabled (rho=0). RL reduces to the
-        CRISP sequence-mean advantage. Isolates the contribution
-        of token-level credit to the RL signal.
-      metric_keys: [vacs/loss_rl, vacs/loss_sd, vacs/token_weight_mean,
-                   reward_mean, response_len_mean]
-      overrides: {vacs: {token_credit_mix: 0.0}}
+      description: 'VACS with token credit disabled (rho=0). RL reduces to the CRISP
+        sequence-mean advantage. Isolates the contribution of token-level credit to
+        the RL signal.
+
+        '
+      metric_keys:
+      - vacs/loss_rl
+      - vacs/loss_sd
+      - vacs/token_weight_mean
+      - reward_mean
+      - response_len_mean
+      overrides:
+        vacs:
+          token_credit_mix: 0.0
     no_soft_gate:
       method: vacs
       base_config: vacs_7b
       slug: vacs-no-soft-gate
-      description: >
-        VACS with the soft reward gate replaced by a unit gate
-        (gate=1 for every rollout). Tests whether the smooth
-        sigmoid gate is necessary or whether a hard "always on"
-        gate suffices.
-      metric_keys: [vacs/loss_rl, vacs/loss_sd, vacs/gate_mean,
-                   reward_mean, response_len_mean]
-      overrides: {vacs: {soft_gate_slope: 0.0}}
+      description: 'VACS with the soft reward gate replaced by a unit gate (gate=1
+        for every rollout). Tests whether the smooth sigmoid gate is necessary or
+        whether a hard "always on" gate suffices.
+
+        '
+      metric_keys:
+      - vacs/loss_rl
+      - vacs/loss_sd
+      - vacs/gate_mean
+      - reward_mean
+      - response_len_mean
+      overrides:
+        vacs:
+          use_soft_gate: false
     fixed_mixing:
       method: vacs
       base_config: vacs_7b
       slug: vacs-fixed-mixing
-      description: >
-        VACS with adaptive mixing disabled (lambda_eff =
-        lambda_max constant). Tests whether the EMA-based variance
-        ratio and cosine scaling of the SD coefficient help.
-      metric_keys: [vacs/loss_rl, vacs/loss_sd, vacs/lambda_eff,
-                   vacs/variance_multiplier, reward_mean, response_len_mean]
-      overrides: {vacs: {adaptive_mix: {enabled: false}}}
+      description: 'VACS with adaptive mixing disabled (lambda_eff = lambda_max constant).
+        Tests whether the EMA-based variance ratio and cosine scaling of the SD coefficient
+        help.
+
+        '
+      metric_keys:
+      - vacs/loss_rl
+      - vacs/loss_sd
+      - vacs/lambda_eff
+      - vacs/variance_multiplier
+      - reward_mean
+      - response_len_mean
+      overrides:
+        vacs:
+          adaptive_mix:
+            enabled: false
     no_asym_surgery:
       method: vacs
       base_config: vacs_7b
       slug: vacs-no-asym-surgery
-      description: >
-        VACS without asymmetric PC-Grad: combined = g_rl + lam * g_sd
-        (no projection). Tests whether protecting the RL direction
-        is necessary.
-      metric_keys: [vacs/loss_rl, vacs/loss_sd, vacs/conflicted,
-                   vacs/rl_grad_norm, vacs/sd_grad_norm,
-                   reward_mean, response_len_mean]
-      overrides: {}
+      description: 'VACS without asymmetric PC-Grad: combined = g_rl + lam * g_sd
+        (no projection). Tests whether protecting the RL direction is necessary.
+
+        '
+      metric_keys:
+      - vacs/loss_rl
+      - vacs/loss_sd
+      - vacs/conflicted
+      - vacs/rl_grad_norm
+      - vacs/sd_grad_norm
+      - reward_mean
+      - response_len_mean
+      overrides:
+        vacs:
+          use_asymmetric_projection: false
     no_sd:
       method: vacs
       base_config: vacs_7b
       slug: vacs-no-sd
-      description: >
-        VACS with self-distillation weight zero (sd_weight=0).
-        Reduces to plain RL with token credit. Tests whether the
-        SD term is the primary source of the documented gains.
-      metric_keys: [vacs/loss_rl, vacs/loss_sd, reward_mean,
-                   response_len_mean]
-      overrides: {vacs: {sd_weight: 0.0}}
-
-# ---------------------------------------------------------------------------
-# Suite: cibo-fixed -- CIBO v2 in FIXED beta mode plus one-factor
-# ablations. The plan's fixed-beta reference config is
-# config/cibo_v2_7b.yaml. Mixing fixed and adaptive modes under
-# the same label is forbidden, so cibo-fixed and cibo-adaptive are
-# separate suites.
-# ---------------------------------------------------------------------------
+      description: 'VACS with self-distillation weight zero (sd_weight=0). Reduces
+        to plain RL with token credit. Tests whether the SD term is the primary source
+        of the documented gains.
+
+        '
+      metric_keys:
+      - vacs/loss_rl
+      - vacs/loss_sd
+      - reward_mean
+      - response_len_mean
+      overrides:
+        vacs:
+          sd_weight: 0.0
 cibo-fixed:
-  description: >
-    CIBO v2 (fixed beta mode) full method vs one-factor ablations.
-    The full variant uses config/cibo_v2_7b.yaml (beta=0.10,
-    credit_lambda=0.25, anchor_alpha=0.01, ib_reduction=sequence_sum).
+  description: 'CIBO v2 (fixed beta mode) full method vs one-factor ablations. The
+    full variant uses config/cibo_v2_7b.yaml (beta=0.10, credit_lambda=0.25, anchor_alpha=0.01,
+    ib_reduction=sequence_sum).
+
+    '
   full_variant: full
   variants:
     full:
       method: cibo-v2
       base_config: cibo_v2_7b
       slug: cibo-fixed-full
-      description: >
-        Full CIBO v2 (fixed beta): single composite loss
-        loss_rl + beta * loss_ib + alpha * loss_anchor, no PC-Grad,
-        tanh credit on the token advantage, EMA-anchored regularizer.
-      metric_keys: [cibo/loss_total, cibo/loss_rl, cibo/loss_ib,
-                   cibo/loss_anchor, cibo/beta, cibo/beta_mode,
-                   cibo/credit_multiplier_mean, cibo/ib_gate_mean,
-                   cibo/kl_per_token_mean, cibo/anchor_alpha,
-                   cibo/ema_decay, reward_mean, response_len_mean]
+      description: 'Full CIBO v2 (fixed beta): single composite loss loss_rl + beta
+        * loss_ib + alpha * loss_anchor, no PC-Grad, tanh credit on the token advantage,
+        EMA-anchored regularizer.
+
+        '
+      metric_keys:
+      - cibo/loss_total
+      - cibo/loss_rl
+      - cibo/loss_ib
+      - cibo/loss_anchor
+      - cibo/beta
+      - cibo/beta_mode
+      - cibo/credit_multiplier_mean
+      - cibo/ib_gate_mean
+      - cibo/kl_per_token_mean
+      - cibo/anchor_alpha
+      - cibo/ema_decay
+      - reward_mean
+      - response_len_mean
       overrides: {}
     no_token_credit:
       method: cibo-v2
       base_config: cibo_v2_7b
       slug: cibo-fixed-no-token-credit
-      description: >
-        CIBO v2 with credit_lambda=0. RL advantage is the global
-        normalized advantage (no per-token scaling). Tests whether
-        the tanh credit is necessary.
-      metric_keys: [cibo/loss_total, cibo/loss_rl, cibo/loss_ib,
-                   cibo/credit_multiplier_mean, reward_mean]
-      overrides: {cibo_v2: {credit_lambda: 0.0}}
+      description: 'CIBO v2 with credit_lambda=0. RL advantage is the global normalized
+        advantage (no per-token scaling). Tests whether the tanh credit is necessary.
+
+        '
+      metric_keys:
+      - cibo/loss_total
+      - cibo/loss_rl
+      - cibo/loss_ib
+      - cibo/credit_multiplier_mean
+      - reward_mean
+      overrides:
+        cibo_v2:
+          credit_lambda: 0.0
     no_ib:
       method: cibo-v2
       base_config: cibo_v2_7b
       slug: cibo-fixed-no-ib
-      description: >
-        CIBO v2 with beta=0. The information-bottleneck term
-        contributes nothing. Tests whether the IB term is the
-        primary source of the documented gains.
-      metric_keys: [cibo/loss_total, cibo/loss_rl, cibo/loss_anchor,
-                   cibo/beta, reward_mean]
-      overrides: {cibo_v2: {beta: 0.0}}
+      description: 'CIBO v2 with beta=0. The information-bottleneck term contributes
+        nothing. Tests whether the IB term is the primary source of the documented
+        gains.
+
+        '
+      metric_keys:
+      - cibo/loss_total
+      - cibo/loss_rl
+      - cibo/loss_anchor
+      - cibo/beta
+      - reward_mean
+      overrides:
+        cibo_v2:
+          beta: 0.0
     no_anchor:
       method: cibo-v2
       base_config: cibo_v2_7b
       slug: cibo-fixed-no-anchor
-      description: >
-        CIBO v2 with anchor_alpha=0. The EMA-anchored regularizer
-        contributes nothing. Tests whether the anchor is necessary.
-      metric_keys: [cibo/loss_total, cibo/loss_rl, cibo/loss_ib,
-                   cibo/loss_anchor, cibo/anchor_alpha, reward_mean]
-      overrides: {cibo_v2: {anchor_alpha: 0.0}}
+      description: 'CIBO v2 with anchor_alpha=0. The EMA-anchored regularizer contributes
+        nothing. Tests whether the anchor is necessary.
+
+        '
+      metric_keys:
+      - cibo/loss_total
+      - cibo/loss_rl
+      - cibo/loss_ib
+      - cibo/loss_anchor
+      - cibo/anchor_alpha
+      - reward_mean
+      overrides:
+        cibo_v2:
+          anchor_alpha: 0.0
     token_mean_ib:
       method: cibo-v2
       base_config: cibo_v2_7b
       slug: cibo-fixed-token-mean-ib
-      description: >
-        CIBO v2 with ib_reduction=token_mean instead of the default
-        sequence_sum. Tests the documented "no silent substitution"
-        rule: token_mean and sequence_sum MUST be reported as
-        distinct reductions.
-      metric_keys: [cibo/loss_total, cibo/loss_ib, cibo/kl_per_token_mean,
-                   reward_mean]
-      overrides: {cibo_v2: {ib_reduction: "token_mean"}}
-
-# ---------------------------------------------------------------------------
-# Suite: cibo-adaptive -- CIBO v2 in ADAPTIVE beta mode plus a matched
-# fixed-beta control + ablations. The reference config is
-# config/cibo_v2_adaptive_beta_7b.yaml. The matched fixed-beta control
-# lets a single suite compare the two modes on equal footing.
-# ---------------------------------------------------------------------------
+      description: 'CIBO v2 with ib_reduction=token_mean instead of the default sequence_sum.
+        Tests the documented "no silent substitution" rule: token_mean and sequence_sum
+        MUST be reported as distinct reductions.
+
+        '
+      metric_keys:
+      - cibo/loss_total
+      - cibo/loss_ib
+      - cibo/kl_per_token_mean
+      - reward_mean
+      overrides:
+        cibo_v2:
+          ib_reduction: token_mean
+    no_soft_gate:
+      method: cibo-v2
+      base_config: cibo_v2_7b
+      slug: cibo-fixed-no-soft-gate
+      description: CIBO v2 with a unit IB gate for every rollout.
+      metric_keys:
+      - cibo/loss_total
+      - cibo/loss_ib
+      - cibo/ib_gate_mean
+      - reward_mean
+      overrides:
+        cibo_v2:
+          use_soft_gate: false
 cibo-adaptive:
-  description: >
-    CIBO v2 in adaptive-beta mode vs a matched fixed-beta control
-    + one-factor ablations. The plan calls out that a comparison
-    between fixed and adaptive is only valid if both are in the
-    SAME suite (so seed, config layout, and metric collection are
-    identical).
+  description: 'CIBO v2 in adaptive-beta mode vs a matched fixed-beta control + one-factor
+    ablations. The plan calls out that a comparison between fixed and adaptive is
+    only valid if both are in the SAME suite (so seed, config layout, and metric collection
+    are identical).
+
+    '
   full_variant: full
   variants:
     full:
       method: cibo-v2
       base_config: cibo_v2_adaptive_beta_7b
       slug: cibo-adaptive-full
-      description: >
-        Full CIBO v2 (adaptive_sign beta): the controller's
-        sign-based update grows/shrinks beta each step within
-        [beta_min, beta_max]. Reference KL EMA is built during a
-        20-step warm-up, then frozen.
-      metric_keys: [cibo/loss_total, cibo/loss_rl, cibo/loss_ib,
-                   cibo/loss_anchor, cibo/beta, cibo/beta_mode,
-                   cibo/beta_reference_kl, cibo/baselined_kl,
-                   reward_mean, response_len_mean]
+      description: 'Full CIBO v2 (adaptive_sign beta): the controller''s sign-based
+        update grows/shrinks beta each step within [beta_min, beta_max]. Reference
+        KL EMA is built during a 20-step warm-up, then frozen.
+
+        '
+      metric_keys:
+      - cibo/loss_total
+      - cibo/loss_rl
+      - cibo/loss_ib
+      - cibo/loss_anchor
+      - cibo/beta
+      - cibo/beta_mode
+      - cibo/beta_reference_kl
+      - cibo/baselined_kl
+      - reward_mean
+      - response_len_mean
       overrides: {}
     matched_fixed:
       method: cibo-v2
       base_config: cibo_v2_7b
       slug: cibo-adaptive-vs-fixed
-      description: >
-        Matched fixed-beta control for the adaptive variant. Same
-        seed, same model, same data; only beta_mode differs. The
-        comparison tests whether the controller's updates help.
-      metric_keys: [cibo/loss_total, cibo/loss_rl, cibo/loss_ib,
-                   cibo/beta, cibo/beta_mode, reward_mean]
+      description: 'Matched fixed-beta control for the adaptive variant. Same seed,
+        same model, same data; only beta_mode differs. The comparison tests whether
+        the controller''s updates help.
+
+        '
+      metric_keys:
+      - cibo/loss_total
+      - cibo/loss_rl
+      - cibo/loss_ib
+      - cibo/beta
+      - cibo/beta_mode
+      - reward_mean
       overrides: {}
     controller_disabled:
       method: cibo-v2
       base_config: cibo_v2_adaptive_beta_7b
       slug: cibo-adaptive-disabled-after-warmup
-      description: >
-        CIBO v2 adaptive mode with the controller's step() called
-        0 times across the run (forced via beta_step_size=0 after
-        warmup completes). Isolates the contribution of post-warmup
-        adaptive updates.
-      metric_keys: [cibo/loss_total, cibo/beta, cibo/beta_reference_kl,
-                   reward_mean]
-      overrides: {cibo_v2: {beta_step_size: 0.0}}
+      description: 'CIBO v2 adaptive mode with the controller''s step() called 0 times
+        across the run (forced via beta_step_size=0 after warmup completes). Isolates
+        the contribution of post-warmup adaptive updates.
+
+        '
+      metric_keys:
+      - cibo/loss_total
+      - cibo/beta
+      - cibo/beta_reference_kl
+      - reward_mean
+      overrides:
+        cibo_v2:
+          beta_step_size: 0.0
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/scripts/eval_humaneval_plus.py /mnt/data/crisp_work/CRISP-main/scripts/eval_humaneval_plus.py
--- /mnt/data/crisp_original/CRISP-main/scripts/eval_humaneval_plus.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/scripts/eval_humaneval_plus.py	2026-08-02 12:56:36.527726130 +0000
@@ -0,0 +1,35 @@
+#!/usr/bin/env python
+"""Run the official EvalPlus HumanEval+ evaluator on a merged model."""
+from __future__ import annotations
+
+import argparse
+import shutil
+import subprocess
+
+
+def main() -> None:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--model", required=True, help="Merged local model directory or HF model ID")
+    parser.add_argument("--backend", default="hf", choices=["hf", "vllm"])
+    parser.add_argument("--dry-run", action="store_true")
+    args = parser.parse_args()
+
+    executable = shutil.which("evalplus.evaluate") or "evalplus.evaluate"
+    cmd = [
+        executable,
+        "--model", args.model,
+        "--dataset", "humaneval",
+        "--backend", args.backend,
+        "--greedy",
+    ]
+    print(" ".join(cmd))
+    if not args.dry_run:
+        if shutil.which("evalplus.evaluate") is None:
+            raise RuntimeError(
+                "EvalPlus is not installed. Install its official package first: pip install -U evalplus"
+            )
+        subprocess.run(cmd, check=True)
+
+
+if __name__ == "__main__":
+    main()
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/scripts/eval_livecodebench.py /mnt/data/crisp_work/CRISP-main/scripts/eval_livecodebench.py
--- /mnt/data/crisp_original/CRISP-main/scripts/eval_livecodebench.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/scripts/eval_livecodebench.py	2026-08-02 12:51:26.423718015 +0000
@@ -0,0 +1,40 @@
+#!/usr/bin/env python
+"""Launch the official LiveCodeBench code-generation harness.
+
+Run this from an installed/cloned LiveCodeBench environment. The wrapper keeps
+release, scenario, prompt style, and local checkpoint path explicit in the run
+metadata instead of silently depending on harness defaults.
+"""
+from __future__ import annotations
+
+import argparse
+import subprocess
+import sys
+
+
+def main() -> None:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--local-model-path", required=True)
+    parser.add_argument("--model-style", default="qwen")
+    parser.add_argument("--release-version", default="release_v6")
+    parser.add_argument("--scenario", default="codegeneration")
+    parser.add_argument("--dry-run", action="store_true")
+    args = parser.parse_args()
+
+    cmd = [
+        sys.executable,
+        "-m", "lcb_runner.runner.main",
+        "--model", args.model_style,
+        "--local_model_path", args.local_model_path,
+        "--scenario", args.scenario,
+        "--release_version", args.release_version,
+        "--evaluate",
+        "--use_cache",
+    ]
+    print(" ".join(cmd))
+    if not args.dry_run:
+        subprocess.run(cmd, check=True)
+
+
+if __name__ == "__main__":
+    main()
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/scripts/launch_distributed.sh /mnt/data/crisp_work/CRISP-main/scripts/launch_distributed.sh
--- /mnt/data/crisp_original/CRISP-main/scripts/launch_distributed.sh	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/scripts/launch_distributed.sh	2026-08-02 12:51:26.423718015 +0000
@@ -0,0 +1,11 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+# Example:
+#   NUM_GPUS=2 METHOD=crisp CONFIG=config/paper_7b.yaml bash scripts/launch_distributed.sh
+NUM_GPUS="${NUM_GPUS:-2}"
+METHOD="${METHOD:-crisp}"
+CONFIG="${CONFIG:-config/paper_7b.yaml}"
+
+exec torchrun --standalone --nproc_per_node="$NUM_GPUS" \
+  train_launcher.py "$METHOD" --config "$CONFIG" "${@:1}"
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/scripts/merge_adapter.py /mnt/data/crisp_work/CRISP-main/scripts/merge_adapter.py
--- /mnt/data/crisp_original/CRISP-main/scripts/merge_adapter.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/scripts/merge_adapter.py	2026-08-02 12:51:26.423718015 +0000
@@ -0,0 +1,43 @@
+#!/usr/bin/env python
+"""Merge a PEFT adapter into its base model for external evaluators."""
+from __future__ import annotations
+
+import argparse
+from pathlib import Path
+
+import torch
+from peft import PeftModel
+from transformers import AutoModelForCausalLM, AutoTokenizer
+
+
+def main() -> None:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--base-model", required=True)
+    parser.add_argument("--adapter", required=True)
+    parser.add_argument("--output", required=True)
+    parser.add_argument("--dtype", choices=["bfloat16", "float16", "float32"], default="bfloat16")
+    args = parser.parse_args()
+
+    dtype = {
+        "bfloat16": torch.bfloat16,
+        "float16": torch.float16,
+        "float32": torch.float32,
+    }[args.dtype]
+    output = Path(args.output)
+    output.mkdir(parents=True, exist_ok=True)
+
+    tokenizer = AutoTokenizer.from_pretrained(args.base_model)
+    base = AutoModelForCausalLM.from_pretrained(
+        args.base_model,
+        torch_dtype=dtype,
+        device_map="cpu",
+    )
+    peft_model = PeftModel.from_pretrained(base, args.adapter)
+    merged = peft_model.merge_and_unload()
+    merged.save_pretrained(output, safe_serialization=True, max_shard_size="5GB")
+    tokenizer.save_pretrained(output)
+    print(output)
+
+
+if __name__ == "__main__":
+    main()
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/scripts/run_ablations.py /mnt/data/crisp_work/CRISP-main/scripts/run_ablations.py
--- /mnt/data/crisp_original/CRISP-main/scripts/run_ablations.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/scripts/run_ablations.py	2026-08-02 12:49:45.003715361 +0000
@@ -116,11 +116,16 @@
         method,
         "--config",
         f"config/{base_config}.yaml",
+    ]
+    if method == "grpo":
+        grpo_k = int(merged.get("rollout", {}).get("k", 8))
+        cmd.extend(["--k", str(grpo_k)])
+    cmd.extend([
         *_resolve_overrides_to_args(merged),
         f"--override=seed={seed}",
         f"--override=training.output_dir={output_dir.as_posix()}",
         f"--override=logging.run_name={output_dir.name}",
-    ]
+    ])
     return cmd
 
 
@@ -219,6 +224,33 @@
     return proc.returncode
 
 
+def _validate_completed_run(run_dir: Path, expected_keys: list[str]) -> None:
+    import math
+
+    if not (run_dir / "_SUCCESS").exists():
+        raise RuntimeError(f"Run has no _SUCCESS marker: {run_dir}")
+    metric_files = [
+        path for path in run_dir.glob("*.jsonl")
+        if path.name != "generations.jsonl"
+    ]
+    if not metric_files:
+        raise RuntimeError(f"No metric JSONL produced in {run_dir}")
+    records = []
+    for line in metric_files[0].read_text(encoding="utf-8").splitlines():
+        if line.strip():
+            records.append(json.loads(line))
+    if not records:
+        raise RuntimeError(f"Metric JSONL is empty in {run_dir}")
+    seen = set().union(*(record.keys() for record in records))
+    missing = set(expected_keys) - seen
+    if missing:
+        raise RuntimeError(f"Missing expected metrics {sorted(missing)} in {run_dir}")
+    for record in records:
+        for key, value in record.items():
+            if isinstance(value, float) and not math.isfinite(value):
+                raise RuntimeError(f"Non-finite metric {key}={value} in {run_dir}")
+
+
 def main(argv: Optional[list[str]] = None) -> int:
     parser = argparse.ArgumentParser(
         description="Run VACS/CIBO ablation suites one variant+seed at a time."
@@ -340,7 +372,13 @@
             print(f"  FAIL: exit={rc}; log preserved at {log_path}", file=sys.stderr)
             failed.append((variant_name, seed, rc))
         else:
-            print(f"  ok")
+            try:
+                _validate_completed_run(run_dir, variant.get("metric_keys", []))
+            except Exception as exc:
+                print(f"  FAIL validation: {exc}", file=sys.stderr)
+                failed.append((variant_name, seed, 4))
+            else:
+                print(f"  ok")
 
     if failed:
         print(f"\n{len(failed)} job(s) failed:", file=sys.stderr)
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/scripts/run_scaling.py /mnt/data/crisp_work/CRISP-main/scripts/run_scaling.py
--- /mnt/data/crisp_original/CRISP-main/scripts/run_scaling.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/scripts/run_scaling.py	2026-08-02 12:51:26.423718015 +0000
@@ -0,0 +1,50 @@
+#!/usr/bin/env python
+"""Run a matched model-scaling matrix serially."""
+from __future__ import annotations
+
+import argparse
+import subprocess
+import sys
+from pathlib import Path
+
+
+DEFAULT_CONFIGS = [
+    "config/scaling/qwen25_1p5b.yaml",
+    "config/scaling/qwen25_3b.yaml",
+    "config/scaling/qwen25_7b.yaml",
+    "config/scaling/qwen25_14b.yaml",
+]
+
+
+def main() -> None:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--methods", default="reinforce_pp,crisp,cibo-v2")
+    parser.add_argument("--seeds", default="0,1,2")
+    parser.add_argument("--configs", nargs="*", default=DEFAULT_CONFIGS)
+    parser.add_argument("--dry-run", action="store_true")
+    args = parser.parse_args()
+
+    methods = [x.strip() for x in args.methods.split(",") if x.strip()]
+    seeds = [int(x) for x in args.seeds.split(",") if x.strip()]
+    for config in args.configs:
+        config_path = Path(config)
+        scale_name = config_path.stem
+        for method in methods:
+            for seed in seeds:
+                output = f"runs/scaling/{scale_name}/{method}/seed_{seed}"
+                cmd = [
+                    sys.executable, "train_launcher.py", method,
+                    "--config", str(config_path),
+                    "--override", f"seed={seed}",
+                    "--override", f"training.output_dir={output}",
+                    "--override", f"logging.run_name={scale_name}-{method}-seed-{seed}",
+                ]
+                if method == "grpo":
+                    cmd += ["--k", "8"]
+                print(" ".join(cmd))
+                if not args.dry_run:
+                    subprocess.run(cmd, check=True)
+
+
+if __name__ == "__main__":
+    main()
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/tests/test_ablation_manifest.py /mnt/data/crisp_work/CRISP-main/tests/test_ablation_manifest.py
--- /mnt/data/crisp_original/CRISP-main/tests/test_ablation_manifest.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/tests/test_ablation_manifest.py	2026-08-02 12:53:05.035720595 +0000
@@ -50,6 +50,7 @@
     "vacs": {
         "vacs.token_credit_mix", "vacs.soft_gate_slope",
         "vacs.reward_threshold", "vacs.sd_weight",
+        "vacs.use_soft_gate", "vacs.use_asymmetric_projection",
         "vacs.adaptive_mix.enabled", "vacs.adaptive_mix.ema_decay",
         "vacs.adaptive_mix.multiplier_min", "vacs.adaptive_mix.multiplier_max",
         "cibo_v2.anchor_alpha",
@@ -57,10 +58,12 @@
     "cibo-v2": {
         "cibo_v2.credit_lambda", "cibo_v2.beta", "cibo_v2.beta_mode",
         "cibo_v2.anchor_alpha", "cibo_v2.ib_reduction",
-        "cibo_v2.beta_step_size",
+        "cibo_v2.beta_step_size", "cibo_v2.use_soft_gate",
+        "cibo_v2.soft_gate_slope", "cibo_v2.reward_threshold",
     },
     "crisp": {
-        "training.lambda_max",
+        "training.lambda_max", "training.lambda_schedule",
+        "training.use_pcgrad", "training.correctness_gate",
     },
     "reinforce_pp": set(),
     "opsd": set(),
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/tests/test_cibo_v2_step.py /mnt/data/crisp_work/CRISP-main/tests/test_cibo_v2_step.py
--- /mnt/data/crisp_original/CRISP-main/tests/test_cibo_v2_step.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/tests/test_cibo_v2_step.py	2026-08-02 12:53:05.035720595 +0000
@@ -363,12 +363,18 @@
         self.assertAlmostEqual(ctrl.beta, beta_before, places=6)
         # The exposed cibo/beta metric in comp.metrics equals beta_before.
         self.assertAlmostEqual(comp.metrics["cibo/beta"], beta_before, places=6)
-        # Simulate the post-step hook: feed a kl observation and call
-        # step(). With kl >> reference_kl, beta should grow.
+        # The first post-warmup observation initializes the delta
+        # reference and intentionally does not update beta.
+        ctrl.observe(reward=0.0, kl_value=2.0)
+        ctrl.step()
+        self.assertAlmostEqual(ctrl.beta, beta_before, places=6)
+
+        # A second observation creates a positive leakage-vs-reward
+        # delta, so the bounded sign controller increases beta.
         ctrl.observe(reward=0.0, kl_value=100.0)
         ctrl.step()
         self.assertGreater(ctrl.beta, beta_before,
-                            msg=f"post-step: beta did not grow; beta={ctrl.beta}")
+                           msg=f"post-step: beta did not grow; beta={ctrl.beta}")
 
 
 @unittest.skipUnless(TORCH_AVAILABLE, "torch is not installed in this environment")
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/tests/test_effective_batch_loop.py /mnt/data/crisp_work/CRISP-main/tests/test_effective_batch_loop.py
--- /mnt/data/crisp_original/CRISP-main/tests/test_effective_batch_loop.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/tests/test_effective_batch_loop.py	2026-08-02 12:57:27.799727472 +0000
@@ -0,0 +1,85 @@
+from __future__ import annotations
+
+import tempfile
+from pathlib import Path
+
+import torch
+
+from crisp.training.batch_types import CollectedBatch
+from crisp.training.common_loop import run_training_loop
+from crisp.utils.config import DotDict
+
+
+def test_advantages_use_all_accumulated_microbatches():
+    """[0,0] and [1,1] must normalize together, not become four zeros."""
+    with tempfile.TemporaryDirectory() as td:
+        model = torch.nn.Linear(1, 1, bias=False)
+        param = next(model.parameters())
+        observed: list[torch.Tensor] = []
+        collect_calls = 0
+
+        def collect_fn(model_, tokenizer_, prompts, answers, global_step):
+            nonlocal collect_calls
+            reward = 0.0 if collect_calls == 0 else 1.0
+            collect_calls += 1
+            rewards = torch.full((len(prompts),), reward)
+            return CollectedBatch(
+                prompts=list(prompts),
+                answers=list(answers),
+                rollouts=[object() for _ in prompts],
+                rewards=rewards,
+            )
+
+        def score_fn(model_, tokenizer_, collected, advantages, global_step):
+            observed.append(advantages.detach().clone())
+            # A correctly shaped synthetic manual gradient is sufficient to
+            # exercise common_loop's accumulation and optimizer plumbing.
+            grad = torch.ones_like(param) * advantages.mean().to(param.device)
+            return [grad], {"loss_rl": 0.0}
+
+        cfg = DotDict({
+            "seed": 0,
+            "optimizer": {
+                "lr": 1e-3,
+                "betas": [0.9, 0.999],
+                "eps": 1e-8,
+                "weight_decay": 0.0,
+                "warmup_steps": 1,
+                "max_grad_norm": 1.0,
+            },
+            "training": {
+                "output_dir": td,
+                "total_steps": 1,
+                "micro_batch_size": 2,
+                "grad_accum_steps": 2,
+                "adv_eps": 1e-8,
+                "log_every": 0,
+                "eval_every": 0,
+                "save_every": 0,
+                "resume_from": None,
+            },
+            "checkpointing": {"save_adapter_only": False},
+            "logging": {"backend": "none"},
+        })
+        dataset = [
+            {"prompt": f"p{i}", "answer": "a"}
+            for i in range(8)
+        ]
+
+        run_training_loop(
+            model=model,
+            tokenizer=None,
+            params=[param],
+            train_dataset=dataset,
+            step_fn=None,
+            collect_fn=collect_fn,
+            score_fn=score_fn,
+            cfg=cfg,
+            run_name="effective-batch-test",
+        )
+
+        assert len(observed) == 2
+        assert torch.allclose(observed[0], torch.tensor([-1.0, -1.0]))
+        assert torch.allclose(observed[1], torch.tensor([1.0, 1.0]))
+        assert (Path(td) / "_SUCCESS").exists()
+        assert (Path(td) / "final" / "training_state.pt").exists()
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/tests/test_objectives.py /mnt/data/crisp_work/CRISP-main/tests/test_objectives.py
--- /mnt/data/crisp_original/CRISP-main/tests/test_objectives.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/tests/test_objectives.py	2026-08-02 12:52:24.423719533 +0000
@@ -107,17 +107,18 @@
                 self.assertAlmostEqual(x, 1.2, places=5)
 
     def test_negative_advantage_with_positive_credit_yields_smaller_magnitude(self):
-        # Negative advantage times positive credit (>1) means: the student
-        # gets penalized MORE where it underweights the teacher. The VACS
-        # token advantage magnitude must grow vs the global one.
+        # VACS uses sign(advantage) inside the exponential. For a negative
+        # trajectory advantage and positive teacher credit, the weight is
+        # below one, so a teacher-endorsed token is penalized less strongly.
         adv = torch.tensor([-0.5])
         t = torch.tensor([[1.0, 1.0]])
         s = torch.tensor([[0.0, 0.0]])
         token_adv = vacs_token_advantages(adv, t, s, credit_mix=1.0, clip_epsilon=0.2)
-        # weight = 1.2, token_adv = -0.5 * 1.2 = -0.6
+        # sign(a)=-1, so weight=clip(exp(-1), 0.8, 1.2)=0.8.
+        # token_adv = -0.5 * 0.8 = -0.4.
         for v in token_adv.tolist():
             for x in v:
-                self.assertAlmostEqual(x, -0.6, places=5)
+                self.assertAlmostEqual(x, -0.4, places=5)
 
     def test_credit_mix_zero_disables_token_credit(self):
         # rho = 0 -> token advantage == global advantage.
@@ -129,15 +130,19 @@
             for x in v:
                 self.assertAlmostEqual(x, 0.3, places=5)
 
-    def test_teacher_logps_are_detached_from_graph(self):
+    def test_credit_weight_is_fully_stop_gradient(self):
         t_param = torch.nn.Parameter(torch.tensor([[1.0, 1.0]]))
         s_param = torch.nn.Parameter(torch.tensor([[0.0, 0.0]]))
         adv = torch.tensor([0.5])
-        token_adv = vacs_token_advantages(adv, t_param, s_param, credit_mix=1.0, clip_epsilon=0.2)
-        # Compute a downstream loss that flows only through `s_param`.
-        token_adv.sum().backward()
-        self.assertIsNone(t_param.grad, "teacher log-probs MUST be detached from the token advantage graph")
-        self.assertIsNotNone(s_param.grad)
+        token_adv = vacs_token_advantages(
+            adv, t_param, s_param, credit_mix=1.0, clip_epsilon=0.2
+        )
+        # The specification uses sg[log p_T - log p_S]. The credit weight is
+        # therefore detached from both contexts; the policy gradient flows
+        # separately through the explicit response-token log-probabilities.
+        self.assertFalse(token_adv.requires_grad)
+        self.assertIsNone(t_param.grad)
+        self.assertIsNone(s_param.grad)
 
     def test_nonfinite_credit_rejected_with_context(self):
         # NaN in teacher log-probs -> the credit becomes NaN -> the helper
@@ -200,14 +205,16 @@
             for x in v:
                 self.assertAlmostEqual(x, 1.25, places=4)
 
-    def test_teacher_logps_are_detached_from_graph(self):
+    def test_credit_weight_is_fully_stop_gradient(self):
         t_param = torch.nn.Parameter(torch.tensor([[1.0, 1.0]]))
         s_param = torch.nn.Parameter(torch.tensor([[0.0, 0.0]]))
         adv = torch.tensor([0.5])
-        token_adv = cibo_token_advantages(adv, t_param, s_param, credit_lambda=0.25)
-        token_adv.sum().backward()
+        token_adv = cibo_token_advantages(
+            adv, t_param, s_param, credit_lambda=0.25
+        )
+        self.assertFalse(token_adv.requires_grad)
         self.assertIsNone(t_param.grad)
-        self.assertIsNotNone(s_param.grad)
+        self.assertIsNone(s_param.grad)
 
     def test_lambda_above_unit_interval_is_rejected(self):
         # Documented: 0 <= lambda_c <= 1. Out-of-range lambda must fail
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/tests/test_validation_integration.py /mnt/data/crisp_work/CRISP-main/tests/test_validation_integration.py
--- /mnt/data/crisp_original/CRISP-main/tests/test_validation_integration.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/tests/test_validation_integration.py	2026-08-02 12:53:05.035720595 +0000
@@ -217,7 +217,11 @@
         )
         # Step() did not change beta (the post-step hook owns it).
         self.assertAlmostEqual(ctrl.beta, 0.5, places=6)
-        # Simulate the post-step hook: feed a large kl_value and step().
+        # The first observation establishes the post-warmup delta
+        # reference. The second creates a positive leakage delta.
+        ctrl.observe(reward=0.0, kl_value=2.0)
+        ctrl.step()
+        self.assertAlmostEqual(ctrl.beta, 0.5, places=6)
         ctrl.observe(reward=0.0, kl_value=100.0)
         ctrl.step()
         self.assertGreater(ctrl.beta, 0.5,
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' '--exclude=runs' '--exclude=REPOSITORY_CHANGES.patch' /mnt/data/crisp_original/CRISP-main/train_launcher.py /mnt/data/crisp_work/CRISP-main/train_launcher.py
--- /mnt/data/crisp_original/CRISP-main/train_launcher.py	2026-08-02 12:35:09.000000000 +0000
+++ /mnt/data/crisp_work/CRISP-main/train_launcher.py	2026-08-02 12:49:22.239714765 +0000
@@ -38,6 +38,7 @@
     "opsd",
     "vacs",
     "cibo-v2",
+    "sft",
 )
 
 # Module import is intentionally deferred until after config validation. VACS
@@ -51,6 +52,7 @@
     "opsd": ("crisp.baselines.opsd.training_loop", "main"),
     "vacs": ("crisp.training.vacs_train", "main"),
     "cibo-v2": ("crisp.training.cibo_v2_train", "main"),
+    "sft": ("crisp.training.sft_train", "main"),
 }
 
 
@@ -85,7 +87,7 @@
     parser = argparse.ArgumentParser(description="CRISP, VACS-CRISP, and CIBO-CRISP v2 training launcher")
     parser.add_argument("method", choices=SUPPORTED_METHODS)
     parser.add_argument("--config", required=True)
-    parser.add_argument("--k", type=int, default=8, help="rollouts per prompt (GRPO only)")
+    parser.add_argument("--k", type=int, default=None, help="rollouts per prompt (GRPO only; defaults to rollout.k from config)")
     parser.add_argument(
         "--override",
         action="append",
@@ -114,11 +116,13 @@
 
     # Config validation is deliberately before _load_runner(): importing a
     # runner eventually imports torch/transformers and may load a model.
-    validate_config(load_config(args.config, overrides=overrides))
+    resolved_cfg = load_config(args.config, overrides=overrides)
+    validate_config(resolved_cfg)
     run = _load_runner(method)
 
     if method == "grpo":
-        run(args.config, k=args.k, overrides=overrides)
+        k = int(args.k if args.k is not None else resolved_cfg.rollout.k)
+        run(args.config, k=k, overrides=overrides)
     else:
         run(args.config, overrides=overrides)
 
